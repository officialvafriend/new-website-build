# 액상덕후 리디자인

전자담배 액상 전문몰 **액상덕후**의 UI/UX 리디자인 저장소입니다.
원래 키플(keyple)이 제작한 사이트를 인수해 직접 운영하는 상황입니다.

## 사이트

| | |
|---|---|
| 프로덕션 | https://duck-hoo.com (blog_id `253955323`) — **라이브 쇼핑몰** |
| 스테이징 | https://staging-fe60-tothemoone-huwyp.wpcomstaging.com (blog_id `257047909`) |
| 플랫폼 | WordPress.com Atomic · 상거래 요금제 · WordPress 7.1 / PHP 8.3 |
| 테마 | `키플_액상덕후` (표시명 "Welcome Drink") |
| 커머스 | WooCommerce 11.1 · 플러그인 52개 |

## 작업 규칙

### 1. 스테이징에서 작업하고, 프로덕션에는 배포로 옮긴다

**`동기화`(스테이징 → 프로덕션) 버튼을 쓰지 않는다.** 사이트 전체를 DB까지
덮어쓰므로 프로덕션에 쌓인 주문·회원·적립금이 사라진다.

옮길 때는 프로덕션에도 GitHub Deployments 를 따로 연결한다. 같은 저장소,
같은 브랜치, 대상 디렉터리 `/wp-content/plugins/new-website-build`.
그러면 배포되는 것은 이 플러그인 폴더 하나뿐이고 나머지는 그대로 남는다.

### 2. 필수 기능은 건드리지 않는다 — 껍데기만 입힌다

아래는 법적 의무이거나 매출·정산에 직결된다. **로직, 폼 필드 이름, DOM 구조,
데이터 경로를 바꾸지 않는다.** 스타일만 위에 얹는다.

| 기능 | 플러그인 | 왜 건드리면 안 되는가 |
|---|---|---|
| 휴대폰 본인확인 | `dreamsecurity-mobile-auth` | 19세 미만 판매 금지 품목. 성인인증은 법적 의무 |
| 주문 상태 | `keyple-order-status` | 입금전·확인필요·입금확인·배송준비중·배송중·배송완료 |
| 입금 자동확인 | `keyple-bank-auto-confirm` | 안드로이드 헬퍼 앱이 입금 SMS 를 받아 매칭 |
| 고객관리 | `keyple-customer` | 등급·적립금·그룹(승인대기/일반/사업자) |
| 쿠폰 | `keyple-coupon-manager` | |
| 송장·정산 | `keyple-order-excel-tracking`, `keyple-sales-check-v2.7`, `woocommerce-epost-shipping` | |
| 게시판 | `kboard`, `kboard-comments` | |

구체적으로 하지 않을 것:
- 위 플러그인을 비활성화하거나 파일을 수정하지 않는다
- 폼 필드의 `name` / `id` 를 바꾸지 않는다 — 입금 자동매칭이 이름으로 걸려 있다
- 성인인증 단계를 건너뛰거나 숨기지 않는다
- 결제·장바구니의 데이터 전달 경로에 개입하지 않는다

`duckhoo-front` 가 쓰는 방식이 정답이다: 원본 PPOM 필드는 `.dhx-src` 로
시각적으로만 감추고 값은 살려둔 채, 그 위에 새 UI 를 얹는다.

### 3. 결제는 무통장입금 전용이다

카드결제(PG)를 쓰지 않는다. `pgall-for-woocommerce`(심플페이)가 비활성인 것은
의도된 상태다. 활성화하지 않는다.

**입금자명이 주문자명과 일치해야 자동 입금확인이 된다.** 이름이 다르면
`확인필요` 로 넘어가 사람이 손으로 처리한다. 그래서 주문서에서 입금자명 입력을
약하게 만드는 디자인은 곧 운영 비용이다. 이 안내는 결제 단계뿐 아니라 앞단에서도
한 번 보여준다.

## 세션 시작 전 환경 설정 (중요)

이 프로젝트는 **Claude 가 사이트 화면을 직접 봐야** 제대로 굴러간다. 기본 환경은
outbound 접속이 좁은 허용 목록으로 잠겨 있어서 사이트에 접속하지 못한다.
curl 도 브라우저도 똑같이 막힌다 (`ERR_TUNNEL_CONNECTION_FAILED`).

**환경(Environment) 설정에서 네트워크 접근을 열고, 새 세션을 시작한다.**
정책은 세션 시작 시 한 번 읽히므로 기존 세션에서는 반영되지 않는다.
최소한 아래 호스트가 열려야 한다.

```
*.wpcomstaging.com
duck-hoo.com
public-api.wordpress.com
```

문서: https://code.claude.com/docs/en/claude-code-on-the-web

막혔는지 확인하는 법:

```bash
curl -sS -o /dev/null -w '%{http_code}\n' --max-time 20 https://duck-hoo.com
# 000 이면 막힌 것. 프록시 사유는 아래로 확인
curl -sS "$HTTPS_PROXY/__agentproxy/status" | python3 -m json.tool | grep -A3 recentRelayFailures
```

열려 있는지 확인되면 Chromium 이 이미 설치돼 있으므로 바로 스크린샷을 찍을 수 있다
(`/opt/pw-browsers/chromium`, `npm i playwright` 만 하면 됨).

### 네트워크가 열렸을 때의 작업 루프

1. 코드를 고쳐 `claude/ui-ux-design-refresh-p5czcx` 에 push
2. GitHub Deployments 가 스테이징에 자동 반영 (**자동 배포를 켜 둘 것**)
3. Claude 가 Playwright 로 여러 화면폭 스크린샷을 찍어 스스로 검증
4. 사람은 스테이징 URL 을 새로고침해 같은 화면을 실시간으로 확인
5. 문제 있으면 1번으로

이 루프가 돌면 사람이 스크린샷을 찍어 옮겨줄 필요가 없다.

### 네트워크가 막혀 있을 때 (차선)

배포와 콘텐츠 수정은 그대로 된다. 막히는 것은 결과 확인뿐이다.

- 배포: push → WordPress 가 GitHub 에서 당겨가므로 Claude 의 접속이 필요 없다
- 사이트 읽기: WordPress MCP 는 `mcp-proxy.anthropic.com` 으로 붙어 egress 정책을
  타지 않는다. 상품·테마 토큰·플러그인·미디어를 읽을 수 있다
- 확인: 사람이 스크린샷을 찍어 대화창에 올려야 한다
- 아티팩트 시안에는 외부 이미지를 넣을 수 없다 (CSP). 실제 상품 사진을 쓰려면
  대화창에 파일로 올려서 data URI 로 심어야 한다

## 디자인

- 한국어 · 국내 · **모바일 우선** (min-width 미디어쿼리만 사용)
- 폰트 **Pretendard** (테마가 이미 사용) → Noto Sans KR 폴백
- 포인트 컬러 **번트 오렌지 `#C2410C`**, 구조·버튼 **검정 `#111111`**
- **반투명 표면을 쓰지 않는다.** `backdrop-filter` 블러는 상품 목록처럼
  카드가 수십 개 깔리는 화면에서 스크롤을 끊는다. 전부 불투명.
- 색은 흰 카드와 바탕 `#F4F6F7` **양쪽에서** WCAG AA(4.5:1)를 넘겨야 한다.
  한쪽만 보고 정하면 캡션이 바탕 위에서 미달한다
- 상품은 묶음 구성이지만 **파는 값이 먼저다**. 병당 가격은 그 아래 캡션으로 붙인다
  (사장님 피드백 2026-09-03 — 병당 가격만 보이면 얼마를 내는지 알 수 없다).
  이름에 증정·사은품·기기·디바이스·스타터가 들어간 상품은 병 수로 나누지 않는다
- 터치 타깃 최소 44px, 주요 CTA 48px
- 한글에 `word-break: break-all` 을 쓰지 않는다 (`keep-all`), 음수 자간도 쓰지 않는다
- `100vh` 대신 `100dvh`

토큰은 `assets/tokens.css`. 요소 규칙까지 포함한 전체 기반은 `design/tokens.css`
(참고용, 배포 안 됨).

## 실제 사이트 연동 (플러그인이 홈을 그린다)

`includes/front.php` + `templates/home.php` + `assets/front.css` + `assets/front.js`.

- `template_include` 로 **첫 화면만** 우리 템플릿으로 바꾼다. `wp_head`/`wp_footer` 는
  그대로 불러 GTM · WooCommerce · PPOM 스크립트가 다 산다. 홈에서만 테마 스타일
  핸들 `welcome-drink-style` 을 뺀다 (테마의 `.dh-*` 인라인 CSS 는 테마 템플릿이
  찍는 거라 우리 템플릿에선 애초에 안 나온다)
- 목록 · 상세 · 장바구니 · 결제 · 계정 · kboard · keyple 마이페이지는 **테마 템플릿이
  그대로 돌고**, `includes/shell.php` 가 `wp_body_open` 에 우리 헤더를, `wp_footer` 에
  푸터·탭바를 넣는다. 테마의 216px 사이드 헤더(`#masthead.dh-side`) · 푸터(`#colophon`) ·
  탭바(`.dh-tabbar`) · 빠른 메뉴(`.dh-quick`) · 모바일 헤더(`.wd-mobile-header-custom`) ·
  마이페이지 상단(`.wd-custom-top-wrap`)은 `assets/shell.css` 가 숨기고, `#page.dh-shell`
  그리드를 푼다. 그 위에 `assets/duckhoo-theme.css` 로 폼·버튼·계정을 정돈한다.
  **폼 필드와 데이터 경로에 손대지 않기 위해서다** — 테마 규칙이 `!important` 라
  shell.css 도 그렇게 쓰고, 테마의 인라인 `<style>` 이 우리 파일보다 뒤에 오므로
  선택자를 전부 `html body.dhr-wrap …` 으로 한 단계 올려 뒀다 (같은 특이도면 진다)
- 상품 목록 카드는 `wc_get_template_part` 필터로 `content-product.php` 만 우리 것
  (`templates/content-product.php` → `Front\card()`)으로 바꾼다. 링크 · 상품 ID 는 같다
- 목록 제목은 테마가 h1 을 1px 로 숨기고 `.woocommerce-products-header` 도 안 찍는다.
  `Shell\archive_title()` 이 `woocommerce_before_shop_loop` 앞(5)에 제목 · 건수 · 분류 설명을
  `.dhr-arch` 로 그린다. 테마 h1 은 그대로 둔다 (접근성)
- **상품 상세 · 목록은 우리 템플릿이다** (`templates/single-product.php` · `templates/archive-product.php`,
  `Front\take_front_page()` 가 `is_product()` · `is_shop()/is_product_taxonomy()/상품 검색` 에서 바꾼다.
  필터 `duckhoo_take_product` · `duckhoo_take_archive`). 구매 폼은
  `woocommerce_template_single_add_to_cart()` 한 줄이 워드커머스 · PPOM · 키플 옵션 UI 를 그대로 그린다.
  제목 · 가격 · 사는 방법은 `includes/product.php` (`head()` · `price()` · `trust()`).
  **비로그인은 갤러리를 열지 않고 `get_image()` 한 장만** 그린다 — `wc_get_gallery_image_html()` 에는
  키플의 19 가림이 안 걸린다 (확인함). 로그인 = 본인확인 회원이라 그때만 갤러리.
- 장바구니(`.wd-cpg`) · 주문서는 키플 마크업 그대로 두고 CSS 로 카드/두 칸으로 편다
  (체크박스 · 수량 · 삭제가 name 으로 걸려 있다). 표는 `table.wd-cpg-table tr.wd-cpg-row` 처럼
  **표 리셋 규칙보다 특이도를 높여** 잡아야 한다 — 한 번 졌다
- 스테이징 배포가 늦어질 때: CSS 는 `last-modified` 헤더, PHP 는 렌더 결과(`/shop/` 의 `<h1>`)로 본다.
  2026-09-03 10:10 커밋이 40분 넘게 안 올라온 적이 있다
- **옛 프론트(duckhoo-front)의 껍데기를 걷어냈다.** `dh-shell` 이 body 에 파스텔 그라데이션을,
  `#page` 에 1240px 둥근 흰 카드와 216px 사이드 내비를 그려 우리 헤더·푸터와 겹쳤다.
  `Shell\drop_legacy_shell()` 이 등록된 핸들 `dh-shell` · `dh-shop-polish` · `dh-front` 를
  `wp_dequeue_style` 한다 (필터 `duckhoo_drop_legacy_styles`). 플러그인 파일은 건드리지 않는다.
  **상품 옵션 UI(`dh-option-ui`)는 남긴다** — 그것이 PPOM select 에 값을 넣는 쪽이다. 색과
  글자 크기만 우리 토큰으로 덮는다 (`.dhx` 안의 `--dhx-*` 를 재정의)
- 사장님 Code Snippets 가 찍는 `<style id="dh-cta-fix">` 는 **핸들이 없어 뺄 수 없다.** 게다가
  `html body.dh-shell-on.dh-shell-on` 처럼 클래스를 두 번 써서 특이도를 올리고 !important 를
  붙인다. 그래서 shell.css 는 클래스를 세 번 쓴다 — `html body.dhr.dhr-wrap.dhr-wrap` (0,3,2).
  이 스니펫을 사장님이 지우면 그 한 단계는 도로 내려도 된다
- 장바구니(`.wd-cpg`)와 주문서는 워드프레스 기본이 아니라 **키플이 만든 페이지**다.
  `duckhoo-theme.css` 의 클래식 장바구니 선택자(`.shop_table` 등)는 여기에 안 걸린다.
  주문하기(`.wd-cpg-order__btn`)·결제하기(`#place_order`)는 shell.css 가 검정 알약으로 만든다
- 가로 스크롤러는 데스크톱에서 휠이 세로로만 가므로 `front.js` 가 양쪽 화살표와
  마우스 드래그를 붙인다
- 데이터: 카테고리는 이름 일부("특가" · "무니코틴" · "랭킹")로 찾는다 — "8월 특가 할인"
  처럼 달이 바뀌는 이름을 따라가기 위해서. 브랜드 분류(taxonomy)가 없어 상품명 앞
  `[브랜드]` 로 센다. 병당 가격은 이름의 "N병" 으로 나눈다. 평점·판매수는 실제 값이
  있을 때만 붙인다. 상품 조회는 10분 transient 캐시 (`dhr_*`), 상품 저장 시 비움
- 실제 상품 173종 (2026-09-02 기준): 카테고리 = 입호흡 액상 69 · 무니코틴 68 ·
  폐호흡 액상 22 · 8,800 적립금 상품 18 · 8월 특가 할인 15 · 액상 랭킹 14 ·
  기기/팟/코일 9 · 노보 액상 9. 리뷰·평점은 0 이다. 브랜드는 이름 앞 [..] 로
  얼려먹구싶오 24 · 액상덕후 18 · 제로닉 무무 13 · 맥스쿨 11 · 심쿵 11 …
- 상품 상세의 옵션은 PPOM `select` 4개 (구성 · 맛 · 팟/코일 추가 · 기기 추가)에
  `wd-option-builder` 가 덧씌워져 있다. **건드리지 않는다**
- **비로그인 방문자에겐 모든 상품 썸네일이 "19" 이미지로 바뀐다** (`img.wd-prelogin-thumb`,
  alt "상품 이미지 (로그인 후 확인 가능)"). 키플의 성인 인증 게이트다. 우회하지 않는다.
  스크린샷에서 19 만 보이면 로그인 상태가 아닌 것이다
- 홈 위에 겹치는 여름 세일 팝업(`#pop6`)과 `vf-redill-hero-js` 는 사장님 Code Snippets 다.
  플러그인이 숨기지 않는다 — 끄는 건 스니펫 쪽에서 한다
- 테마가 `wp_body_open` 으로 찍는 `.wd-mobile-header-custom`/드로어는 홈에서 CSS 로 숨긴다

### 스테이징 배포 확인법

GitHub Deployments **자동 배포가 켜져 있다** (2026-09-02 부터). push 하면 몇 분 안에
스테이징에 반영된다. 그 전에는 첫 연결 때 한 번만 돌아서 손으로 눌러야 했다. 최신인지 보는 법:
파일이 아니라 **렌더 결과**로 본다 — 홈 HTML 의 `<h1>` 이나 카드 수처럼 그 커밋에서
바뀐 것을 curl 로 확인한다 (`?nocache=<난수>` 를 붙여 캐시를 피한다). 새 파일이
생긴 커밋이면 `assets/*.css` 의 200/404 로도 된다.

로그인이 필요한 화면(주문취소 버튼 · `/membership-cancel/` 은 비로그인 시 302)은
스테이징 테스트 계정이 있어야 본다. 스크린샷 브리지(`live/shoot-b.mjs`)는 Node 쪽
쿠키 항아리를 두어 로그인 세션이 요청 사이에 살아남는다.

### 이 환경에서 사이트 보기

네트워크는 열려 있다 (curl 200). **크로미움만 `ERR_CONNECTION_RESET`** 이 난다 —
프록시 상태(`recentRelayFailures`)를 보면 CONNECT 뒤 TLS 단계에서 터널이 6초 만에
닫힌다. curl · Node(undici) 는 된다. 스크린샷은 Playwright `route` 에서 모든 요청을
undici(`EnvHttpProxyAgent` + CA 번들)로 대신 받아 `fulfill` 하는 방식으로 찍는다
(`scratchpad/live/probe-b.mjs` 패턴). 크로미움이 직접 소켓을 열게 두지 않는다.

## 사장님 피드백으로 정해진 것 (2026-09-03)

- **찜하기는 없앴다.** 브라우저에만 저장돼 기기를 바꾸면 사라지는 하트였다. 되살리려면
  테마의 위시리스트(장바구니 페이지 `.wd-cpg-wishlist`)에 붙여야 한다
- **판매 수량(N개 판매)은 카드에서 뺐다**
- **'공식' 이라는 말을 쓰지 않는다** (브랜드 섹션 · 배지 모두)
- 유틸 바 문구는 `19세 미만 판매 금지`
- 헤더 · 푸터에서 **이벤트 페이지 링크를 뺐다** (추후 다시 넣기로)
- 푸터 `배송 · 교환 · 환불` 은 `/tip/` 이 아니라 `/shipping/` 로 간다
- 카카오톡 문의 버튼은 오픈채팅방(`https://open.kakao.com/o/sZOm9HJh`)으로 간다.
  방을 옮기면 `add_filter( 'duckhoo_kakao_url', fn() => '…' );` 한 줄
- 관리자로 로그인하면 워드프레스 관리 바가 화면 위에 고정되므로 sticky 헤더에
  `body.admin-bar` 오프셋(783px↑ 32px · 601–782px 46px)을 준다. 없으면 헤더가 잘려 보인다

## 사는 쪽으로 미는 것들 (레퍼런스 cruntin.com 의 구조, 2026-09-03)

레퍼런스의 힘은 색이 아니라 **매 화면이 판다**는 것이다. 우리 데이터로 같은 자리를 채웠다.

- 헤더 위 `.promo` 띠: 비로그인 `가입 즉시 8,800원 적립` → `/register/`, 로그인은 무료배송 조건.
  숫자는 `Front\signup_points()` (필터 `duckhoo_signup_points`), 무료배송 기준은 필터
  `duckhoo_free_shipping_min` (30,000). 고정이 아니라 스크롤하면 같이 올라간다
- 모바일 헤더 아래 `.bnav` 칩 (880px↑ 숨김, 데스크톱은 `.dnav`) · 홈 `.qcats` 둥근 분류 타일.
  분류 이름은 `Front\short_cat()` 으로 줄인다 ("9월 특가 할인" → "이달 특가", "기기 / 팟 / 코일" → "기기·팟")
- 상품 상세: 사진 위 할인율 알약 `.dhp-gal__pill` · 몇 장째 `.dhp-gal__n` · 가격 아래 `Product\benefits()`
  (`ul.dhp-ben`, 필터 `duckhoo_product_benefits`) · **모바일 고정 구매 줄 `.dhp-bar`** — 진짜 버튼은 폼 안의
  `.single_add_to_cart_button` / `.wd-direct-checkout-btn` 이고 구매 줄은 잠김 상태 · 총액
  (`.dhx-sum__total`, 없으면 `data-price` × 수량)을 비추며 누르면 그 버튼을 대신 누른다.
  폼의 버튼이 화면에 보이는 동안은 내려가 있다 (`IntersectionObserver` → `.is-away`). 상세에서는 탭바를 숨긴다
- **장바구니 서랍 `.dhc`** (`Front\cart_drawer_html()`, 모든 껍데기 화면의 푸터 뒤): 헤더 · 탭바의
  장바구니 버튼과 담기 직후(`.woocommerce-message`)에 열린다. 내용은 Store API
  `/wp-json/wc/store/v1/cart` (쿠키 + `Nonce` 헤더, 응답 헤더의 Nonce 를 이어 쓴다). 지우기만 여기서
  (`remove-item`), 수량은 키플 장바구니 페이지에서. **비로그인에는 썸네일을 그리지 않는다** — Store API
  사진은 19 가림을 안 거친다. 불투명 · 데스크톱 오른쪽 420px · 모바일 아래 시트 · 포커스 가둠 · Esc
- 푸터 `.fbank`: `woocommerce_bacs_accounts` 의 계좌 + 입금자명 안내
- JS 설정은 `Front\js_config()` 한 곳 (`window.DHR`): 로그인 여부 · 논스 · 무료배송 기준 · 장바구니/상품 URL
- 사장님 스니펫 두 개가 화면을 덮는다: 비로그인 상품 페이지의 성인인증 안내 `#dh-agegate2`
  (`.dh-ag2-later` 로 닫힘) 와 홈 팝업 `#pop-dim`/`#pop6`. 플러그인은 건드리지 않고, **테스트 스크립트에서만**
  지운다 (`scratchpad/live/nudge-shot.mjs` 의 `later()`)

검증: `scratchpad/live/nudge-shot.mjs` — 비로그인/로그인 × 모바일/데스크톱에서 띠 · 칩 · 타일 · 구매 줄 · 서랍
(담기 → 서랍 열림 → 지우기 → 배지 0) · 푸터 계좌를 재고 찍는다. 담은 것은 스크립트가 도로 지운다.

## 사장님 피드백으로 정해진 것 (2026-09-04)

- **주력 브랜드는 노보 · 디오리퀴드 · 화이트아웃 · 펠릭스 · 액상덕후** 다. 무니코틴이 헤더 칩 ·
  분류 타일 · 푸터 브랜드에서 두 번째로 앞서 있던 것을 내렸다. `Front\featured_brands()` 가
  이 순서를 정하고 (필터 `duckhoo_featured_brands`), 푸터 브랜드 줄 · 홈 브랜드 카드 3장 ·
  티커 · `주력 브랜드` 카루셀이 모두 그것을 쓴다. 이름이 그대로 있으면 그것을 쓴다 —
  "노보" 를 물었는데 상품 수가 많다고 "노보 블랙" 을 내세우지 않는다
- **상품 목록에 분류 설명(term description)을 그리지 않는다.** 배송 · 가격 안내가 상품 위에
  길게 깔리면 사러 온 사람이 먼저 읽어야 할 것이 상품이 아니게 된다. 글은 관리자에 그대로 있다
- **카드의 `+` 대신 `구매하기` 알약**을 넣는다. 품절이면 같은 자리에 `품절`. 묶음은 옵션(구성 · 맛)이
  필수라 담기가 아니라 상품 상세의 구매 상자(`#dhp-buy`)로 보낸다
- 페이지 번호: `ul.page-numbers` 자신도 클래스가 `page-numbers` 라 알약 배경 규칙이 목록 전체에
  걸려 흰 막대가 화면 폭만큼 깔렸다. `li > .page-numbers` 로 좁혔다

## 선택창 · 구매 서랍 (2026-09-04)

- **선택창은 우리가 그린다** (`front.js` 의 `.dhsel`, 스타일은 `shell.css`). 브라우저가 그리는
  `select` 목록은 CSS 가 닿지 않는다. 원본 `select` 는 `.dhsel-src` 로 1px 접어 두고 값을 쥔 채
  남는다 — **name · value · change 경로는 그대로다.**
  - **`<button>` 으로 만들지 않는다.** 테마 `wd-option-builder.js` 가 `form.cart` 안의 버튼을
    전부 걷어 자기 구매 줄로 옮기고 라벨을 `.text()` 로 덮어쓴다. 한 번 그렇게 당해 목록이 비었다.
    `role` 만 준 `div` 로 짓는다
  - **`change` 는 한 번만 쏜다.** jQuery 위임 핸들러는 네이티브 이벤트로도 깨어나므로
    `jQuery(sel).trigger('change')` 를 더하면 테마가 같은 옵션을 두 번 담아 수량이 2가 된다
  - 테마는 고른 옵션을 아래 목록에 카드로 쌓고 **50ms 뒤 선택창을 placeholder 로 되돌린다**
    (`resetSelectToPlaceholder`). 값은 숨은 필드가 쥔다. 그래서 우리 선택창도 같이 되돌려야 한다
  - 묶음 상품은 옛 옵션 UI(`.dhx`)가 이미 같은 일을 하므로 걸리지 않는다 (`.dhx-src` 검사)
- **데스크톱 구매 서랍**: 구매 카드(`.dhp-card--form`)가 화면 밖으로 나가면 그 카드 **자체**가
  오른쪽 고정 서랍이 된다. 폼을 복제하지 않으므로 고른 옵션이 그대로다. 바깥 `.dhp-dockwrap` 이
  높이를 기억해 자리를 메우고, 닫으면 `구매하기` 알약만 남는다.
  높이 제한에는 `!important` 가 필요하다 — 위 `.dhp-card` 규칙이 `max-height: none !important` 다
- **입체감**: 그림자를 두 겹(바닥 + 주변)으로, 카드는 포인터를 따라 최대 3.2도 기운다.
  카루셀 상자는 가로만 자른다 (`overflow-x: clip; overflow-y: visible`) — `hidden` 이면 들린
  카드의 그림자가 잘린다. 반투명 · 블러는 여전히 쓰지 않는다
- 테마의 무료배송 게이지가 형광 분홍(`#FF0D74`)으로 강조한다 → `--dh-accent-warm` 으로 덮는다

## 사장님 피드백으로 정해진 것 (2026-09-04, 두 번째)

- **주력 브랜드에서 액상덕후를 뺐다** — 노보 · 디오리퀴드 · 화이트아웃 · 펠릭스 넷이다
  (`duckhoo_featured_brands`). 브랜드 카드 · 푸터 브랜드 줄이 이 넷을 쓴다. 티커는 전체
  브랜드 목록이라 뒤쪽에 액상덕후가 여전히 나온다 — 그것까지 빼려면 티커도 넷만 쓰면 된다
- **홈의 "병당 1만 원 아래" 를 "입호흡 액상 단품" 으로 바꿨다.** 입호흡 분류에서 묶음 · 세트 ·
  기획을 뺀 단품 16개를 슬라이드로, 아래에 분류 전체 보기 버튼
- **묶음 "다시 누르면 해지" 가 실제로 되게 했다.** 옛 옵션 UI(dh-option-ui)는 안내만 그렇게
  써 두고 동작하지 않았다. 그 플러그인 상태를 다시 짜지 않고, 같은 줄의 수량 `−` 버튼을
  0까지 눌러 준다 — 해지는 그쪽이 스스로 한다 (`front.js`, capture 단계에서 가로챈다)
- **마이페이지 주문 목록은 카드다.** 워드커머스 기본 표(`table.woocommerce-orders-table`)를
  주문 하나당 카드 한 장으로 편다. 상태 알약은 `tr.woocommerce-orders-table__row--status-*`
  로 색을 나눈다 (on-hold = 손님이 아직 할 일이 있는 상태라 주황). 왼쪽 메뉴는 데스크톱에서
  sticky 사이드바. 마크업은 그대로다 — 보기 · 취소 링크가 거기 걸려 있다
- **구매 버튼 호버는 우리 것으로 못 박았다.** 테마 쪽 색 그림자가 분홍으로 번지는데 규칙을
  하나씩 찾는 대신 `filter · text-shadow · outline · box-shadow` 를 통째로 덮는다.
  **그 줄(`.vf-drawer-actions`)은 버튼과 높이가 똑같은데 `overflow: hidden` 이었다** —
  호버로 1px 떠오르는 순간 버튼 윗변과 그림자가 잘려 "위가 가려진" 것처럼 보였다.
  `overflow: visible !important` 로 푼다 (2026-09-04)

## 장바구니 · 팝업 (2026-09-04, 세 번째)

- **장바구니 두 칸은 `form.wd-cpg-form` 자체를 격자로 만들어 나눈다.** `.wd-cpg` 를 격자로
  두고 폼을 `display:contents` 로 풀면 제목 · 안내가 칸을 가로질러 행 번호가 어긋나고,
  오른쪽 덩어리가 제목 위로 올라간다. 폼을 격자로 두면 그 셈이 없다
- 합계 · 주문 버튼 · 할인 적용 안내는 `front.js` 가 `.dhr-cartside` 로 묶어 오른쪽에 붙인다.
  **감싸는 상자는 반드시 form 안에** 만든다 — 주문 버튼이 `type=submit name=wd_checkout_selected` 다
- **금액대별 자동 할인 안내는 사장님 스니펫이 우리 뒤에 다시 그린다.** 한 번 쓰고 끝내면
  옛 문구로 되돌아가므로 MutationObserver 로 지켜보다 다시 쓴다 (8초 뒤 해제).
  문구의 값은 `window.DHR.discount` — 필터 `duckhoo_auto_discount` (현재 10만원↑ 1만원)
- 인라인 style 로 분홍 그라데이션이 박힌 곳: `#coupon-auto-notice` · `#coupon-applied-notice`
  (`!important` 필요), 형광 분홍 배지 `.wd-cpg__count`, 추천 썸네일의 6px 안쪽 여백
- **사장님 홈 팝업(`#pop6`)은 모양만 맞춘다.** 끄는 것은 스니펫 쪽 일이다. 스니펫이 인라인
  `<style>` 로 `#pop6 .bbtn` (1,0,1) 을 쓰므로 우리는 `body.dhr #pop6 .bbtn` (1,1,2) 로 이긴다.
  홈은 `body.dhr` 뿐이라 (dhr-wrap 이 없다) 팝업 규칙은 **front.css** 에 둔다

## 폰 푸터 · 탈퇴 확인 (2026-09-04, 네 번째)

- **푸터 링크 묶음은 폰에서 접힌다** (1,963px → 1,060px). `상품 · 브랜드 · 안내` 는 제목만
  보이고 눌러서 편다 (`.fcol__t[aria-expanded]`, front.js). **고객센터는 접지 않는다** —
  전화번호가 푸터까지 내려온 이유다. 데스크톱은 CSS 가 `.fcol__list` 를 늘 펼쳐 둔다
- 푸터 알약 링크에는 `max-width:100%` 가 필요하다. 긴 링크 하나가 화면 폭을 늘린다
- **회원탈퇴는 막지 않는다.** 진행 중 주문 · 남은 적립금은 `Front\...\cautions()` 가 안내로
  보여 주고, 마지막에 확인 창(`[data-leave-confirm]`)이 실제 숫자로 한 번 더 묻는다.
  예전처럼 막으려면 `duckhoo_membership_cancel_require_clear` 를 true 로
- `assets/membership-cancel.css` 는 오래 **enqueue 되지 않고 있었다.** 탈퇴 화면의 버튼·입력은
  front.css 에서 `body.dhr` 로 한 번 더 못 박는다 (테마 규칙이 더 세다)
- 마이페이지 메뉴에서 `다운로드` 를 뺐다 (`duckhoo_account_menu_hide`)
- **`documentElement.scrollWidth` 는 잘린 콘텐츠도 센다.** 카루셀이 있는 화면에서 390px 인데
  564px 로 보고된다. 진짜 가로 스크롤인지는 `scrollTo(600,0)` 뒤 `scrollX` 로 확인한다

## 계정 · 회원가입 (2026-09-04, 다섯 번째)

`includes/account.php` + `assets/shell.css` 의 `.dhr-ah` 부분.

- **테마는 페이지마다 왼쪽 위에 `<h1 class="wd-page__title">` 을 하나 찍는다.** 마이페이지에서
  그것이 "내 계정" 이라는 낱말 하나로, 가운데 좁은 폼과 아무 상관 없이 떠 있었다.
  지우는 대신 **제목이 설 자리를 만들었다** — 어두운 머리판 `.dhr-ah` 하나가 로그인 ·
  가입 3단계 · 마이페이지를 다 묶는다. 테마 h1 은 `:has(.dhr-ah)` 로 **머리판이 실제로
  그려진 화면에서만** 뺀다 (안 그러면 어쩌다 머리판이 안 나왔을 때 제목 없는 화면이 된다)
- **유리(반투명 + 블러)를 쓰는 곳은 여기뿐이다.** 머리판 위 알약(`.dhr-ah__chips`)과
  단계 표시(`.dhr-ah__steps`)처럼 **뒤 배경을 우리가 정한** 자리에만 얹는다. 글을 읽는
  면(폼 카드)과 상품 목록은 여전히 불투명 — 카드가 수십 장 깔리는 화면에 블러를 넣으면
  스크롤이 끊긴다
- **회원가입은 키플이 만든 페이지 세 장이다**: `/register/`(방식 선택) → `/agree/`(약관) →
  `/join-form/`(본인확인 · 정보입력). 세 장 다 자기 헤더 · 카테고리 줄 · 푸터를 또 그리고
  링크가 duck-hoo.com(프로덕션)과 옛 분류를 가리킨다 → shell.css 가 걷어낸다.
  **폼 · 필드 이름 · 본인확인 버튼은 그대로다**
- **이 세 장은 `the_content` 가 돌지 않는다** — 템플릿이 통째로 그린다. 그래서 머리판은
  `wp_body_open`(우선순위 6, 우리 헤더 바로 뒤), 돌아가는 길은 `wp_footer`(4)에 붙인다.
  단계 구성은 필터 `duckhoo_signup_steps`
- **`가입하기` 버튼이 맨 글자로 나왔다.** 페이지 자체 `<style>` 은 살아 있는데(높이 62px ·
  글자 22px 이 걸린다) `background` 와 `border-radius` 만 바깥 규칙에 졌다. 검은 알약으로
  못 박았다 (`.wd-join-submit` · `.wd-agree-submit` · `.wd-register-btn`)
- **본인확인 버튼이 두 개였다** (폼 밖 하나 + 폼 안 하나). 폼 안의 것만 남긴다 — 그쪽에
  상태 문구(`#wdJoinVerifyStatus`)와 토큰 필드가 붙어 있고, 눌러 보면 `window.open` 이
  실제로 불린다 (확인함). **단계를 건너뛴 것이 아니라 같은 버튼이 두 개였던 것**이다
- **로그인 화면에 회원가입으로 가는 길이 아예 없었다.** 폼 아래 `.dhr-join` 카드를 붙였다
  (`woocommerce_after_customer_login_form`). 가입 세 장에는 로그인으로 돌아가는 줄을 붙였다
- 영문 제목 `Sign In`(가입 화면인데) · `Terms & Policy` 는 머리판 제목이 대신한다
- 마이페이지 머리판은 `woocommerce_account_navigation`(우선순위 5)에 붙어 메뉴 · 내용보다
  먼저 나온다. 데스크톱 격자에서 알림 1행 · 머리판 2행 · 메뉴/내용 3행이다.
  첫 화면 템플릿의 인사말은 뺐다 — 머리판이 이름을 그린다
- 약관 화면의 필수 표시가 형광 분홍(`#ff5c73`)이었다 → `--dh-accent-deep`

검증: `scratchpad/live/auth-check.mjs` — 390/1440px × 네 화면에서 머리판 · 단계 · 버튼 ·
가로 넘침 · 영문 제목 · 본인확인 버튼 수를 잰다. **로그인이 필요한 화면(마이페이지 본문)은
스테이징 계정이 있어야 찍는다.**

## 구매 게이트를 우리가 막고 있었다 (2026-09-04, 여섯 번째)

**증상**: 묶음 상품에서 구성 · 맛을 다 골라도 `구매하기` 가 잠긴 채
`이벤트/기획/기기 옵션을 모두 선택해주세요` 만 떴다. 열 방법이 없었다.

**원인은 우리 선택창(`.dhsel`)이다.** 사장님 스니펫(`vf` 구매 게이트)은 각 `select` 가
무슨 칸인지를 **조상 텍스트**로 판단한다 — 위로 올라가다 한글 2자 이상이 나오는 조상에서
멈추고, 거기서 `<select>` 만 지운 나머지 글자를 칸 이름으로 쓴다. 우리가 select 를
`.dhsel` 안으로 옮기고 옵션 글자를 그 옆에 늘어놓자 칸 이름이 통째로 바뀌었다:

| 칸 | 원래 이름 | 우리 선택창이 켜졌을 때 | 결과 |
|---|---|---|---|
| 기기 추가 | `기기 함께 구매하기 (할인 특가 · 선택사항)` | `…조바 기기+40,000원…` | 「함께 구매」가 사라져 **필수로 잡힘** |

`REQUIRED_PACKAGE_PATTERN` 은 `기기` 를 필수로, `OPTIONAL_ADDON_PATTERN`(`함께 구매`)을
예외로 본다. 이름이 바뀌면서 예외가 풀려 필수가 1개 → 2개가 됐고, 그 칸은 기기를 사지
않으면 고를 수가 없으니 게이트가 **영원히 안 열렸다.**
(계측: 우리 front.js 켬 `requiredCount 2` · 끔 `1`)

**고친 방법 두 가지.**

1. **선택창은 폼 안에 글자를 남기지 않는다.** select 는 원래 부모에 그대로 두고(옮기지 않는다),
   고른 값은 텍스트 노드가 아니라 `data-l` + CSS `content: attr(data-l)` 로 그리며,
   목록은 열 때만 만들어 `document.body` 로 띄우고 닫으면 지운다. 읽어 주는 것은
   트리거의 `aria-label` 이 맡는다. 목록은 `position:fixed` 라 화면 밖으로 나가지 않게 가둔다
   (위 `.dhsel__list` 규칙을 이기려면 클래스를 하나 더 붙여야 한다 — `.dhsel__list.dhsel__list--pop`)
2. **옛 옵션 UI(`.dhx`)가 있는 화면에서는 아예 만들지 않는다.** 묶음 · 이벤트 상품이 그렇다.
   거기서는 `.dhx` 가 보이는 UI 를 그리고 우리 선택창은 34px 로 찌그러져 **보이지도 않으면서**
   판정만 어긋나게 했다. `.dhx` 는 그쪽 스크립트가 DOMContentLoaded 에서 만드는데 우리는
   푸터에서 그보다 먼저 도므로, **`load` 까지 기다렸다가** 본다 (처음엔 이걸 놓쳐 검사가 헛돌았다)

**교훈**: 이 사이트의 구매 게이트는 마크업의 **글자**를 읽는다. 폼 안에 무엇을 더하든
칸 이름이 바뀔 수 있다. 상품 폼에 요소를 넣을 때는 `requiredCount` 를 반드시 재 본다.

검증: `scratchpad/live/opt8.mjs`(10병 채워 버튼이 열리는지) · `survey.mjs`(묶음 0개 · 단품 1개) ·
`pick2.mjs`(우리 선택창과 네이티브 select 가 같은 값을 만드는지 — `ppom[ppom_option_price]` 비교).

## 카드의 `구매하기` 는 상품 페이지 맨 위로 연다

`#dhp-buy` 조각을 붙였더니 브라우저가 그 자리로 뛰어내려 폰 940px · 데스크톱 611px 지점부터
열렸다. 사진도 가격도 안 보이는 자리다. 조각을 뺐다. 폰은 맨 위에서도 아래 고정 구매 줄이
이미 떠 있다.

## 성인인증은 살아 있다 (확인함, 2026-09-04)

| 시험 | 결과 |
|---|---|
| 본인확인 없이 가입 폼 제출 | 거부 (`본인인증을 완료해주세요.`) · 계정 안 생김 |
| 본인확인 토큰 위조 | 거부 (`본인인증 정보가 일치하지 않습니다.`) · 계정 안 생김 |
| 비로그인 `/checkout/` | 302 → `/register/` |

가입 폼의 토큰은 서버가 HMAC 으로 서명해 발급하고 이름 · 연락처 · 생년월일도 인증 응답에서
서버가 채운다. **다만 비로그인도 장바구니에는 담긴다** — 막히는 곳은 결제다. 법적 의무는
지켜지지만 손님은 다 담고 나서 벽을 만난다. 앞단 안내를 붙일지는 사장님 판단.

## 팝업 · 푸터 계좌 · 나이 문구 (2026-09-04, 일곱 번째)

- **성인인증 안내 팝업(`#dh-agegate2`)은 사장님 Code Snippets 다.** 그 파일은 건드리지 않는다.
  분홍(`#e91e63`)이 배지 · 선물 줄 · 주버튼 세 곳에 쓰였다 → shell.css 가 우리 색으로 덮는다
  (스니펫 인라인 `<style>` 이 `.dh-ag2-primary` (0,1,0) 이라 우리 (0,4,2)가 이긴다).
- **`나중에 볼게요` 는 닫기만 하고 다음 화면에서 또 떴다.** front.js 가 문구를
  `오늘 하루 보지 않기` 로 바꾸고 누른 날짜를 `localStorage['dhr-ag2']` 에 적는다.
  **노드를 지우면 안 된다** — 스니펫이 열 때 html/body 에 `dh-ag2-lock` 을 붙여 스크롤을
  잠그므로, 팝업만 사라지고 화면은 잠긴 채 남는다. 노드는 두고 (1) `body.dhr-ag2-off` 로
  화면에서만 빼고 (2) MutationObserver 로 열리는 순간 잠금을 푼다.
  **팝업 마크업은 우리 스크립트보다 뒤에 찍힌다** — DOMContentLoaded 와 load 에서 다시 찾는다
  (바로 찾으면 늘 null 이라 한 번 헛돌았다).
  막는 것은 이 안내뿐이다. 가입 때의 휴대폰 본인확인과 결제 단계의 회원 확인은 서버 쪽이다.
- **푸터 계좌는 상자를 다시 지었다.** 계좌이체만 받는 가게라 이 열한 자리가 푸터에서 가장
  잘 보여야 한다. 번호를 22px 흰 글자로 세우고 은행 · 예금주는 그 아래 캡션으로,
  `복사` 버튼(`.fbank__copy`, clipboard API + execCommand 대체)을 붙였다. 입금자명 안내는
  따로 강조 줄로 뺐다
- **약관 동의의 `만 14세 이상입니다` 를 `만 19세` 로 바꿨다** (front.js, `.wd-agree-age` 안만).
  **약관 본문(스크롤 상자)의 14세는 그대로 둔다** — 그것은 개인정보 수집 규정이라 맞는 말이다
- **회원가입 1단계(`/register/`)**: 윗줄과 제목이 둘 다 "회원가입" 이라 한 번만 남기고
  (윗줄은 `액상덕후 회원`), `이메일로 가입합니다…` 한 줄은 뺐다. 버튼 하나를 흰 상자에 넣으니
  비어 보여 상자를 걷고 60px 알약 + 화살표만 세웠다. **로그인 줄이 250px 아래 떨어져 있던 것은
  테마가 `#content` 에 `min-height:380px` 을 주기 때문**이다 — `body.dhr-signup` 에서 푼다

검증: `scratchpad/live/check4.mjs` — 복사(클립보드 값까지) · 약관 나이 문구 · 팝업 색 · 라벨 ·
닫은 뒤 저장 · 다시 방문 시 안 뜨는지 · 스크롤 잠김이 풀리는지.

## 적립금 상품 분류 · 푸터 색 (2026-09-04, 여덟 번째)

- **가입하면 8,800원을 주면서 그 돈을 쓸 곳으로 가는 길이 어디에도 없었다.**
  `8,800 적립금 상품` 분류를 헤더 칩 · 홈 분류 타일 · 푸터 상품 목록에 넣었다.
  이름은 `short_cat()` 이 `적립금 상품` 으로 줄인다 (금액은 띠 · 팝업 · 가입 화면에 이미 있다).
  아이콘은 `coin` — 특가와 같은 가격표를 쓰면 두 타일이 같은 것으로 보인다
- 타일이 일곱 개가 되면서 폰에서는 **4열 두 줄**로 바꿨다. 한 줄에 일곱 개를 밀어 넣으면
  타일(59px)이 아이콘(54px)보다 좁아진다. 640px 위는 그대로 한 줄
- **푸터 계좌 번호가 테마 화면에서만 검정으로 덮였다.** front.css 의 `.dhr .fbank .n` 은
  (0,2,0) 이라 홈(`body.dhr`)에서는 이기지만 테마 화면(`dhr-wrap`)에서는 진다 —
  검은 상자 위 검은 글씨라 아예 안 보였다. shell.css 에 `html body.dhr.dhr-wrap.dhr-wrap`
  으로 다시 못 박았다. **푸터에 새 색을 줄 때는 홈과 테마 화면 양쪽에서 재 봐야 한다**

## 모바일 구매 시트 (2026-09-04, 아홉 번째)

데스크톱은 구매 카드가 오른쪽 서랍이 되는데, 모바일에는 아래 구매 줄만 있었다.
옵션을 고르려면 상세를 한참 거슬러 올라가야 했다 (`hit()` 이 `scrollIntoView` 를 했다).

- **같은 카드를 아래에서 올린다** (`.dhp-card--form.is-sheet`). 데스크톱 서랍과 똑같이
  **폼을 복제하지 않는다** — 진짜 폼이 그대로 올라오므로 고른 옵션이 유지되고,
  구매 게이트가 읽는 칸 이름도 폼 안쪽이라 바뀌지 않는다 (확인함: 시트 안에서 묶음 +
  10병을 채우면 `담기` 가 열린다)
- 여는 곳: 구매 줄의 **총액**(`[data-bar-open]`), 그리고 아직 다 고르지 않았을 때의
  `결제하기`. 닫는 곳: 막 · Esc · × · 담기/결제하기를 누른 뒤
- 바깥 `.dhp-dockwrap` 이 높이를 기억해 카드가 fixed 로 바뀌어도 페이지가 접히지 않는다
- 시트가 올라오면 아래 구매 줄은 감춘다 (`body.dhp-sheet-on`) — 같은 버튼이 두 번 보이면 안 된다
- 무료배송 게이지는 시트에서 뺀다 (데스크톱 서랍과 같다). 화면의 5분의 1을 안내가 차지하면
  정작 옵션이 밀린다. 상세 본문에는 그대로 있다
- 높이 제한에는 `!important` 가 필요하다 — 위 `.dhp-card` 규칙이 `max-height: none !important` 다
- **구매 줄은 두 줄이다.** 화살표 하나로는 눌러야 하는 줄인지 알 수 없어 손님이 전화로 묻는다 —
  윗줄을 통째로 버튼(`.dhp-bar__open`)으로 만들고 `총 금액 · 금액 · 옵션 열기 ⌃` 라고 글자로 적는다.
  아랫줄이 담기 · 결제하기. 줄 높이 74px → 124px
- **구매 줄은 구매 상자를 지나쳐 내려갔을 때만 올린다.** 예전에는 화면을 열자마자 떠 있어서,
  아직 보지도 않은 버튼이 화면을 가리고 상자가 아래에 있다는 사실도 감췄다. 관찰자만으로는
  경계를 넘을 때만 깨어나므로 스크롤에서 `bottom < 0` 을 한 번 더 본다
- **`.dhp-buy` 격자 칸에 폭을 못 박아야 한다.** `auto` 는 max-content 라 안쪽에 줄바꿈이
  안 되는 요소 하나가 칸을 밀어낸다 — 320px 자리에 393px 이 잡혀 카드가 화면 밖으로 잘렸다
  (360 · 390 · 412px 전부). `grid-template-columns: minmax(0,1fr)` + 자식 `min-width:0`
- **머리판과 1번 상자 사이가 73px 이나 비어 있었다.** form 위 여백 16px + 비어 있는 PPOM
  가격·오류 상자 + **묶음에서 1px 로 접힌 원본 선택칸 줄 40px**. 마지막 것은 `.dhx` 가 있는
  화면에서만 접는다 (`:has(.dhx)`) — 단품에서는 그 줄이 보이는 본체다. 73px → 26px

검증: `scratchpad/live/sheet.mjs`(단품) · `sheet3.mjs`(묶음 — 2,600px 내려간 뒤 시트만으로
10병을 채워 버튼이 열리는지).

## 상품 추천 카드 (2026-09-04, 열 번째)

상세 아래 `이런 상품은 어떠세요?` 는 Product Recommendations 플러그인이 `<ul class="products">`
로 그리고, 그 안의 카드만 우리 `content-product.php` 다.

- **카드 뒤에 회색 사각형이 하나 더 보였다.** 테마가 `li.product` 마다 각진 1px 테두리
  (`#EBEBEB`)를 그리는데 안의 카드는 둥글어서 네 귀퉁이로 삐져나온 것이다.
  `li` 는 자리만 잡게 두고 테두리 · 여백 · 배경 · `::before/::after` 를 모두 없앤다
- **카드 크기가 제각각이었다.** 줄마다 높이가 따로 정해져서 이름이 긴 줄과 짧은 줄이
  다르게 보였다 → `grid-auto-rows: 1fr`. 카드는 칸을 꽉 채우고(`height:100%`),
  `구매하기` 는 `margin-top:auto` 로 언제나 맨 아래에 붙는다 (확인: 네 장 모두 401px,
  버튼이 카드 바닥에서 14px)
- 목록 · 홈 · 장바구니는 우리 격자라 `li.product` 자체가 없다 — 이 문제는 상세 추천에만 있었다

## 1:1 문의가 워드프레스 로그인으로 튀었다 (2026-09-04, 열한 번째)

`/inquiries/` 는 kboard 게시판이고, **비로그인 방문자에게 `alert("로그인 하셔야 사용할 수
있습니다.")` 를 띄운 뒤 `top.window.location` 으로 `wp-login.php` 로 보내 버린다.**
워드프레스 관리자 로그인 화면이라 손님 눈에는 사이트가 통째로 다른 곳으로 튄 것처럼 보이고,
브랜드도 없고 회원가입으로 가는 길도 없다.

- **게시판 플러그인은 건드리지 않는다.** 대신 `Front\inquiry_url()` 이 로그인 여부를 보고
  주소를 정한다 — 로그인했으면 게시판, 아니면 우리 로그인 화면 + `?redirect_to=게시판`.
  상세의 `1:1 문의` · 헤더 유틸 · 푸터 · 카카오 문의 버튼이 모두 이것을 쓴다
  (게시판 주소는 필터 `duckhoo_inquiry_page`)
- 로그인 폼에 숨은 `redirect` 필드를 넣어(`woocommerce_login_form`) 로그인이 끝나면 게시판으로
  돌아간다. 주소는 `wp_validate_redirect()` 로 같은 사이트인지 확인한다 — 바깥으로 튀는 주소는 버린다
- 로그인 머리판이 왜 여기에 왔는지 한 줄로 알려 준다 (`문의를 남기려면 로그인이 필요합니다`)
- **구매 줄의 `결제하기` 는 남은 자리를 다 쓴다.** content 폭으로 두면 오른쪽이 비어
  두 버튼이 왼쪽으로 몰린다
- **카카오톡 오픈채팅이 들어왔다** (2026-09-04): `https://open.kakao.com/o/sZOm9HJh`.
  `Front\kakao_url()` 의 기본값이고 필터 `duckhoo_kakao_url` 로 바꾼다.
  **로그인 없이 물어볼 수 있는 유일한 창구**라 상품 상세의 링크 줄에서 `1:1 문의`(회원 전용)
  보다 앞에 둔다. 밖으로 나가는 주소는 `Front\is_external()` 이 보고 새 창으로 연다

## 프로덕션 배포 (2026-09-04)

duck-hoo.com 에 GitHub Deployments 를 연결하고 첫 배포 · 활성화까지 마쳤다.

- 저장소 `officialvafriend/new-website-build` · 브랜치 `claude/ui-ux-design-refresh-p5czcx` ·
  대상 `/wp-content/plugins/new-website-build` · 자동 배포 켬
- **연결만으로는 배포되지 않는다.** 자동 배포는 다음 push 부터다 — 첫 배포는 `배포 실행` 을 손으로 누른다
- 파일이 올라가도 화면은 안 바뀐다. **플러그인 활성화가 따로 있다** (플러그인 52개 → 53개)
- 되돌리기: 플러그인 비활성화 한 번. 주문 · 회원 · 적립금은 어느 단계에서도 안 건드린다
- 활성화 때 `/shipping/` `/terms/` `/privacy/` 세 페이지가 실제로 생성됐다 (프로덕션에 없었다).
  비활성화해도 페이지는 남는다
- 프로덕션에만 있는 것: 사장님 공지 띠(`dh-ann`)가 우리 띠 위에 하나 더 있어 **검은 줄이 두 겹**이다.
  끄려면 우리 `.promo` 를 빼거나 스니펫 쪽 띠를 끈다
- 프로덕션 첫 화면은 `home blog` (정적 페이지가 아니라 블로그). `is_front_page()` 라 그대로 잡힌다

## 옛 옵션 UI 의 수량 스테퍼가 잘렸다 (아이폰)

`.dhx-qty` 는 `margin-left: auto` 로 줄 오른쪽 끝에 붙는데, 바깥 `.dhx-card__inner` 가
`overflow: hidden` 이라 줄이 1~2px 만 넓어져도 `+` 버튼이 잘려 나갔다. **크로미움에서는
재현되지 않는다** (390 · 360px 에서 13px 여유). 아이폰 사파리의 글자 폭 차이로 보인다.

- `.dhx-card__inner{ overflow: visible }` — 자르는 것 자체를 없앤다
- 줄에 `flex-wrap: wrap` — 그래도 안 들어가면 잘리는 대신 아랫줄로 내려간다
- 이름(`.dhx-bundle__pick` 등)에 `min-width: 0` — 줄어들 자리를 준다

## 안내 페이지

`includes/pages.php` 가 없는 페이지를 만든다 — `/terms/`(이용약관) · `/privacy/`(개인정보처리방침) ·
`/shipping/`(배송 · 교환 · 환불). `admin_init` 과 활성화 훅에서 한 번만 돌고
(`duckhoo_pages_version` 옵션), 같은 슬러그가 이미 있으면 손대지 않는다. 만들어진 뒤에는
워드프레스 관리자에서 여느 페이지처럼 고치면 된다. 가게 정보는 `duckhoo_shop_info` 필터.

**이 글은 초안이다.** 법률 검토를 받아야 하고, 개인정보처리방침의 수탁사(본인확인기관 ·
호스팅) 이름은 실제 계약처로 채워야 한다.

## 옛 옵션 UI 의 수량 스테퍼 (2026-09-07)

- **`.dhx-card__inner` 의 `overflow: hidden` 을 풀면 안 된다.** 그 카드는
  `.dhx-card__body { grid-template-rows: 0fr → 1fr }` 로 접히는 아코디언이고,
  접힌 상태를 실제로 감추는 것이 안쪽의 `overflow:hidden` 이다. 잘림을 막겠다고
  `visible` 로 바꿨더니 닫힌 카드의 행들이 밖으로 흘러나와 서로 겹쳤다
- **flexbox 에 `flex-wrap: wrap` 을 주면 줄이기 전에 먼저 넘긴다.** 이름이 긴 줄
  하나가 첫 줄을 다 먹고 가격 · 스테퍼가 둘째 줄로 떨어져 줄 높이가 61px → 89px 로
  뛰고, 그 둘째 줄이 카드 밖에서 잘렸다. 한 줄로 못 박고(`nowrap`) 이름 쪽이
  줄어들게 해야 한다
- **폰에서는 한 줄에 다 들어가지 않는다.** 카드 안쪽 폭이 252px 뿐이다 —
  `main.dhp-main` 17.6 + `.wrap` 17.6 + 구매 카드 19 + `.dhx-card__inner` 14,
  여백이 네 겹이라 한쪽에서만 68px 이 사라진다. 스테퍼 114px · 가격 70px 을 빼면
  이름 자리가 60px 이라 `브이메이트...` 로 잘렸다. 899px 아래에서는 줄을 격자로
  바꿔 **이름 한 줄 · 그 아래 가격(왼쪽) · 스테퍼(오른쪽)** 로 나눈다. 우연히 접히는
  것이 아니라 칸을 정해 두므로 잘릴 자리가 없다 (줄 높이 91px)
- 스테퍼는 폰에서 36+32+36 = 104px 로 줄이고, 버튼 44px · 숫자칸 36px 이던 높이
  어긋남도 44px 로 맞춘다. 데스크톱은 그대로 (114px · 한 줄 · 줄 높이 52px)

검증: `scratchpad/live/qty11.mjs`(폰) · `qty13.mjs`(데스크톱 · 모바일 구매 시트).
`--fix` 없이 돌리면 현재 배포본과 비교할 수 있다.

## 공짜 옵션을 몇 개든 담을 수 있었다 (2026-09-07)

`젤로 크리스탈 기기 + 액상 5병 증정` 상품의 3번 카드는 기기 **색**을 고르는 칸인데
(실버 · 블랙, 둘 다 +0원) 수량을 3까지 올릴 수 있었고 **총액은 69,000원 그대로**였다.
주문에는 `실버 × 3` 이 그대로 실린다 — 기기 두 대를 공짜로 담을 수 있었다.

- 사장님 게이트 스니펫(`IS_BUNDLE_PRODUCT` · `BUNDLE_RATIO`)은 **맛(addon_1)이
  세트당 5개인지만** 본다. 그 뒤 칸(`addon_2` 기기 색)에는 상한이 없다.
  수량은 테마의 `wd-option-builder` 가 쥐고 `input[name=wd_option_builder_json]` 으로 실린다
- 옛 옵션 UI 는 자기 상태를 만들지 않는다 — 테마의 `.wd-option-plus` 를 대신 눌러 줄 뿐이라
  그쪽에 상한이 없으면 그대로 올라간다
- **테마 · 스니펫은 건드리지 않고 `front.js` 가 `+` 를 capture 단계에서 가로챈다.**
  값(`+N원`)이 붙은 줄이 하나라도 있는 카드는 **돈을 더 내고 사는 추가 구매**이므로 그대로 둔다
  (팟 · 코일 · `기기 함께 구매하기`). 값이 하나도 없는 카드 = 고르는 칸이라 **세트당 1개**.
  세트를 2개 담으면 2개까지 (`sets()` 가 json 의 `type:required` 수량을 센다).
  필터 `duckhoo_free_choice_per_set` — 0 이면 제한을 끈다
- 이미 하나를 고른 채 다른 색을 누르면 **바꿔 준다** — 그쪽을 − 로 0까지 내리고 이쪽을 올린다
- 담기 · 결제하기에도 마지막 빗장을 둔다. **담는 데이터에는 손대지 않고** 무엇이 몇 개인지만
  말하고 멈춘다
- 안내 글자는 `data-l` + CSS `content` 로 그린다. 폼 안에 텍스트 노드를 더하면
  구매 게이트가 읽는 칸 이름이 바뀔 수 있다

**이건 앞단 방어일 뿐이다.** 제대로 막으려면 상품 옵션 설정(PPOM/테마)에서 그 칸을
수량이 아닌 단일 선택으로 두거나 서버에서 잘라야 한다. 사장님 판단이 필요하다.

검증: `scratchpad/live/dev5.mjs`(상한 · 바꾸기 · 정상 담기) · `dev6.mjs`(유료 팟은
3개까지 그대로) · `dev7.mjs`(세트 2개 → 2개까지).

## 적립금을 쓴 주문을 취소하면 적립금이 사라진다 (2026-09-07)

사장님 확인: **적립금을 써서 주문한 뒤 취소하면 그 적립금이 돌아오지 않는다.**

- 적립금을 빼는 쪽은 테마다. 결제 화면의 입력칸이 `#wd_point_discount` 이고
  (`assets/js/wd-checkout-custom.js`), 주문 합계에는 `적립금 할인` 줄로 붙는다.
  **되돌리는 코드는 확인할 수 없다** — 테마 · keyple-customer 의 PHP 는 이 저장소에 없고
  스테이징 관리자 계정도 없다
- **우리가 만든 문제는 아니다.** 다만 `woocommerce_valid_order_statuses_for_cancel` 로
  입금전(on-hold)에 취소 버튼을 연 것이 우리라, 손님이 스스로 눌러 적립금을 잃는
  길을 만든 것도 우리다
- **적립금을 여기서 직접 돌려주지 않는다.** keyple-customer 는 잔액만이 아니라 적립금
  **내역(원장)**을 쌓는다. 회원 메타에 숫자만 더하면 내역에 없는 돈이 생겨 정산이
  어긋난다. 저장 키도 모른다

`includes/points.php` 가 하는 일은 세 가지뿐이다.

1. `used( $order )` — 그 주문이 적립금을 얼마나 썼는지 **읽는다**. 지정 메타
   (`_wd_point_discount` 등) → 수수료 줄(음수) → 쿠폰 줄 → 이름이 `적립…사용/할인` 인
   메타 순. **적립(earn) 기록은 세지 않는다** — 잘못 세면 취소를 괜히 막는다
2. 적립금을 쓴 주문은 손님이 혼자 취소하지 못하게 하고 (`우리가 연 상태만` 닫는다 —
   워드커머스 기본 pending·failed 는 그대로) 그 자리에 `취소 문의` 버튼을 세운다
   (`woocommerce_my_account_my_orders_actions`)
3. 그래도 취소·환불로 넘어가면 **주문 메모**를 한 번 남긴다 — 얼마를 돌려줘야 하는지

필터: `duckhoo_order_points_used`(사용액 못 박기) · `duckhoo_order_points_meta_keys` ·
`duckhoo_block_cancel_with_points`(false 면 도로 열린다).

**아직 못 한 것 — 자동 반환.** 하려면 둘 중 하나가 필요하다: (가) 적립금을 더하는
keyple/테마 함수 이름, (나) 스테이징 관리자 계정. 그때 `woocommerce_order_status_cancelled`
에서 그 함수를 부르고 `_duckhoo_points_note` 자리에 반환 표시를 남기면 된다
(두 번 돌려주지 않게).

검증: `php design/php-tests/run.php` — 34개 항목 (적립금 관련 12개).

## 여름 세일 팝업은 스테이징에만 남아 있었다 (2026-09-07)

사장님이 `메인 팝업` 에서 다 지웠는데도 `8/3~8/31 여름 혜택에 풍덩` 팝업이 계속 떴다.
홈 HTML 의 `const slides = […]` 를 두 사이트에서 꺼내 보니 답이 나온다.

| | 탭 |
|---|---|
| duck-hoo.com | `고객 안내` 하나 (검은 `판매 제품 안내` 이미지) — **이미 깨끗하다** |
| 스테이징 | `8월 이벤트`(여름혜택에-풍덩) · `고객 안내` · `2+3 이벤트` · `젤로 크리스탈` · `무니코틴 할인` · `공식 오픈채팅방` **6개** |

**팝업 목록은 사이트 DB 에 있다.** GitHub Deployments 는 플러그인 폴더만 옮기므로
한쪽에서 지워도 다른 쪽은 그대로다. 프로덕션 관리자에서 지우고 스테이징 화면을
보면 그대로 뜬다. 확인법:

```bash
curl -sS "https://duck-hoo.com/?nocache=$RANDOM" | grep -o 'const slides = \[.*\]'
```

우리 front.js 는 `window.DHR.popupTab`(기본 `고객 안내`) 하나만 남기고 나머지 칩을
감춘다 — 그래서 탭이 여섯이어도 버튼 줄이 비어 보이고 첫 장(여름 세일)이 그려진다.

## 적립금이 어디에 사는지 (2026-09-07, 진단 결과)

사장님이 `도구 → 적립금 진단` 을 돌려 준 결과로 확인된 것.

| | |
|---|---|
| 잔액 | 회원 메타 **`_keyple_points`** (가입 적립분은 `_wd_signup_point_balance`) |
| 원장 | 표 **`wp_keyple_points_log`** |
| 주문의 사용액 | 주문 메타 **`_wd_point_discount`** (+ 켜짐 표시 `_wd_point_discount_applied = 1`) |
| 주문 합계 | 수수료 줄 `적립금 할인 = -8800` |

관련 함수는 전부 테마의 `wd_*` 다:
`wd_apply_point_discount_fee`(결제에서 할인 줄을 붙인다) · **`wd_log_keyple_points_change`**(원장에 기록) ·
`wd_get_keyple_points_log_table` · `wd_get_keyple_points_log_columns` · `wd_get_myaccount_user_points` ·
`wd_award_purchase_points_on_delivery`(배송완료 때 적립) · `wd_get_user_point_history` ·
`wd_migrate_user_points_to_mycred_once`. keyple 쪽은 `Keyple_CRM_Ajax::handle_points_adjust` ·
`handle_delete_point_log` (관리자 화면에서 손으로 조정하는 자리).

**`used()` 는 이미 맞게 읽는다** — 진단이 최근 주문 4건의 사용액(8800 · 5000 · 8800 · 8800)을
정확히 집어냈다. `_wd_point_discount_applied`(값 1) 를 금액으로 읽지 않도록 `_applied` 로
끝나는 칸은 뺀다.

**취소 훅에는 적립금을 되돌리는 것이 하나도 없었다** (2차 진단으로 확인).
`woocommerce_order_status_cancelled` 에 걸린 것은 쿠폰 사용횟수 · 재고 · 기프트카드뿐이고,
`_changed` 의 `wd_award_purchase_points_on_delivery` 는 **배송완료 때 1% 적립**하는 쪽이다.
반환은 애초에 아무도 안 한다.

**그래서 `Points\return_points()` 가 그 자리를 채운다.** 테마가 결제 때 빼는 방식
(`functions.php:3235-3237`)을 그대로 뒤집는다:

```php
$before = (int) get_user_meta( $uid, '_keyple_points', true );
update_user_meta( $uid, '_keyple_points', $before + $amount );
wd_log_keyple_points_change( $uid, $amount, '주문 #N 취소 적립금 반환' );
```

- **잔액과 원장을 둘 다** 건드린다. 하나만 하면 정산이 어긋난다
- 가입 적립금 몫(`_wd_signup_point_balance`)은 주문에 기록이 없다 → **주머니에서 비어 있는
  만큼만** 되돌린다 (`min(사용액, 지급액 − 현재)`). 실제 두 경우로 맞는 것을 확인했다:
  가입 적립금 8,800 을 쓴 주문은 8,800 이 돌아오고, 일반 적립금 5,000 을 쓴 주문은 0
- 한 번만 (`_duckhoo_points_returned`). `_wd_point_discount_applied` 가 없는 주문
  (잔액에서 빠진 적이 없다)은 건너뛴다. 비회원 주문도 건너뛴다
- **취소 차단은 스스로 풀린다** — `blocks_cancel()` 기본값이 `! can_return()` 이라,
  테마 함수가 있으면 손님이 다시 스스로 취소할 수 있고 적립금은 돌아온다.
  테마가 바뀌어 함수가 사라지면 다시 막고 메모만 남긴다

## 적립금 자동 반환 — 확인 완료 (2026-09-07)

사장님이 실제 주문으로 취소 → 반환까지 확인했다. **잔액 · 원장 · 주문 메모 셋 다 맞았다.**

진단 화면(`includes/points-doctor.php` · 도구 → 적립금 진단)은 저장 위치를 찾는 데 썼고
**지웠다.** 다시 필요하면 git 이력에 있다 (커밋 `0de37ba` · `d883cd9`). 그 화면이 한 일:
취소 훅에 걸린 콜백 목록 · 이름에 적립금이 든 함수 · 적립금 표 · 적립금을 쓴 최근 주문과
회원 잔액 · 원장 최근 줄 · 테마 적립금 함수의 원본 코드.

## 노보 물량 이벤트 (2026-09-08)

노보 물량이 정해져 있는데 한 사람이 하루에 다 쓸어 가면 다음 손님이 살 것이 없다.
`includes/novo.php` + `includes/novo-admin.php`.

- **하루 구매 한도 10병.** 담을 때(`woocommerce_add_to_cart_validation`) ·
  장바구니에서(`woocommerce_check_cart_items`) · 결제 직전(`woocommerce_checkout_process`)
  세 곳에서 본다. **워드커머스가 내준 검증 훅만 쓴다** — 폼 필드도, 담기는 데이터도
  건드리지 않는다. 오늘 주문한 병 수 + 장바구니 병 수를 합쳐 세고, 취소 · 환불 · 실패한
  주문은 빼 준다 (`duckhoo_novo_free_statuses`)
- **세는 것은 값을 치르는 병뿐이다** (`paid()`). `10+1` 은 11병을 받고 **10병으로 센다**.
  사은품까지 세면 낱병 10병을 산 손님과 한 세트를 산 손님의 하루치가 달라진다.
  그래서 낱병 10병 = 10+1 한 세트 = 하루치 하나로 맞아떨어진다.
  화면에 적는 병 수(총 11병)는 `bottles()` 가 그대로 쓴다 — **두 값은 다르다**
- **병 수는 상품 이름에서 읽는다**. 받는 병은 `Front\per_bottle()` ("10+1" → 11),
  세는 병은 `paid()` ("10+1" → 앞의 10). 라인은 이름 앞 `[노보 블랙]` · `[노보]` 로
  가른다 — **블랙을 먼저 봐야 한다**, 순서가 바뀌면 블랙이 일반으로 잡힌다
- **비로그인은 세지 않는다** (주문 이력이 없다). 손님은 장바구니에 더 담을 수 있지만
  결제 단계에서 막힌다 — 이 가게는 비로그인 결제가 애초에 안 되므로 구멍은 아니다
- 라인별로 따로 세려면 `duckhoo_novo_event` 의 `scope` 를 `'line'` 로. 한도는
  `limit`(값을 치르는 병 수), 값은 `lines`
- **남은 수량은 워드커머스 재고 관리가 켜진 상품만** 숫자가 있다. 173개 중 8개만
  켜져 있다 — 노보 상품은 관리자에서 재고 관리를 켜고 수량을 넣어야 보인다.
  숫자를 우리가 만들지 않는다
- **이벤트 기준 가격** (일반 1병 13,000 · 10+1 120,000 / 블랙 1병 13,500 · 10+1 130,000)은
  `config()` 에 적어 두지만 **화면의 가격을 덮어쓰지 않는다.** 판매가는 언제나
  워드커머스가 쥔 값이다 — 주문 · 정산이 보는 값과 손님이 보는 값이 갈리면 안 된다.
  `도구 → 노보 이벤트` 가 실제 판매가 · 재고와 나란히 놓고 어긋난 줄만 표시하며,
  누르면 `set_regular_price()` 로 맞춘다 (`_duckhoo_novo_price_before` 에 이전 값을
  남겨 한 번에 되돌린다). 대상은 단품(1병)과 10+1(11병)뿐 — 지금 있는 `노보 10병`
  묶음(#146)은 병 수가 달라 대상이 아니다
- **안내는 `form.cart` 바깥에만 그린다.** 구매 게이트가 마크업의 글자를 읽기 때문이다.
  카드에는 필터 `duckhoo_card_extra`, 상세에는 가격 바로 아래 액션
  `duckhoo_product_after_price` 를 새로 뒀다 (확인함: 칸 이름 `팟, 코일 *` 그대로)

### 담기만 막아서는 샌다 (2026-09-08)

사장님 신고: 11병인데 결제가 됐다. 프로덕션에서 재현했다.

```
POST /wc/store/v1/cart/add-item     수량 11  → 400 (막힘)
POST /wc/store/v1/cart/update-item  10 → 11  → 200 (통과)   ← 여기로 샜다
```

이 사이트의 장바구니(`옵션/수량 변경`)는 수량을 **Store API `update-item`** 으로 고치는데
그 길에는 `woocommerce_add_to_cart_validation` 이 걸리지 않는다. 빗장을 세 개 더 건다.

- `woocommerce_store_api_product_quantity_maximum` — 상한을 남은 만큼으로 낮춘다.
  **고치는 중인 줄은 빼고 세야 한다** (`max_units( $p, $cart_item_key )`) — 자기 자신과
  겨루면 상한이 0 이 되어 이미 담긴 장바구니가 아예 열리지 않는다. 어느 줄인지 모르면
  손대지 않는다
- `woocommerce_store_api_validate_cart_items` — Store API 가 장바구니를 검사할 때.
  `RouteException` 이 있으면 그것으로, 없으면 평범한 예외로 던진다
- `woocommerce_checkout_create_order` — **마지막 빗장.** 어느 길로 왔든 주문은 여기를
  지난다. 던진 예외를 워드커머스가 잡아 결제 화면의 오류로 보여 준다

**교훈: 이 사이트에서 수량이 바뀌는 길은 담기 말고도 있다.** 새 제한을 만들 때는
`add-item` 과 `update-item` 을 둘 다 눌러 봐야 한다.

### 거절 안내는 합계가 아니라 **할 일**을 말한다 (2026-09-09)

사장님이 10+1 한 세트를 주문한 뒤 또 담아 보니 이렇게 떴다:

> 노보 액상이 하루 한도(10병)를 넘었습니다. 지금 20병입니다. 수량을 줄여 주세요.

막는 것은 맞는데 **손님이 무엇을 해야 하는지가 없다.** 10+1 을 산 사람은 병을
22개 받았으므로 20이라는 숫자부터 낯설고, 이미 주문한 몫은 줄일 수가 없다.

- `counts()` 가 **오늘 주문한 몫과 장바구니에 있는 몫을 나눠** 센다. 할 일이 다르다 —
  앞의 것은 내일까지 기다려야 하고, 뒤의 것은 지금 빼면 된다
- `over_text( $ordered, $in_cart, $adding )` 이 그 셋으로 말을 고른다.
  묶음만 세는 지금 설정(`singles => false`)에서는 병이 아니라 **세트로 말한다** —
  손님이 고른 단위가 세트다. `nword()` 가 1~5 를 「한 세트」로 쓴다
- **담는 중에 「담겨 있다」고 말하지 않는다.** 장바구니에 한 세트가 있는데 또 담으면
  「두 세트가 담겨 있어요」가 아니라 「장바구니에 이미 한 세트가 있어서 더 담을 수 없어요」
- 끝은 언제나 할 일이다: `장바구니에서 노보 묶음을 빼시면 나머지 상품은 그대로 주문하실
  수 있고, 노보는 내일 다시 주문해 주세요. (낱병은 제한 없습니다.)`
- **HTML 을 넣지 않는다** — `guard_order()` 가 `esc_html()` 로 예외 메시지를 만든다

### 자동 할인은 테마에 **두 번** 적혀 있다 (2026-09-09, 원인 확정)

`도구 → 자동 할인 진단` 으로 자리를 찾았다. 스니펫이 아니라 **테마**다.

**(가) 돈을 깎는 쪽** — `functions.php:10668` 익명 함수, `woocommerce_cart_calculate_fees` 20:

```php
$total = Σ( $item['data']->get_price() × quantity ) − $cart->get_discount_total();
if ( $total >= 100000 ) $discount = 10000;   // 8만↑ 5,000 · 5만↑ 3,000
$cart->add_fee( '🎁 금액 자동 할인', -$discount, false );
```

**(나) 화면에 그리는 쪽** — 두 곳이 서로 다르게 구한다:

| 화면 | 방법 |
|---|---|
| 장바구니 `functions.php:1940` · `page-cart.php:54` | **fee 를 합산** (`amount < 0` 인 fee) |
| 결제 `woocommerce/checkout/form-checkout.php:145` | **fee 를 무시하고 스스로 다시 계산** |

```php
// form-checkout.php:145  「표시 안정화: fee 목록 대신 직접 구간 계산」
$wd_tier_base = Σ( price × qty ) − get_discount_total();
if ( $wd_tier_base >= 100000 ) { $wd_auto_fee_discount = 10000; }
```

**그래서 fee 만 떼면 안 된다.** 장바구니는 맞게 따라오지만 결제 화면은 10,000 을 계속
빼서 실제 주문 금액과 어긋나고, 그 어긋남에서 총액이 0원이 된다. 실측(프로덕션,
fee 를 훅에서 뗀 상태):

| 장바구니 | 결과 |
|---|---|
| 펠릭스 2개 · 조바 팟 · 장바구니 화면 전부 | 정상 |
| **노보 낱병 10병** (정가↔판매가 차이가 없는 유일한 경우) | **0원** |

**테마 파일은 못 고친다** (사장님: 전에 고쳤다가 사이트가 멈춘 적이 있다). 그래서
`includes/discount.php` 가 **테마를 읽기만 하고 사본을 건넨다.**

워드커머스는 템플릿을 열 때마다 `wc_get_template` 필터로 「어느 파일을 쓸까」를 묻는다.
그때 테마 파일 대신 **`$wd_auto_fee_discount = 10000;` 의 숫자만 0 으로 바꾼 사본**의
경로를 돌려준다. 사본은 `uploads/duckhoo-tpl/fc-<md5>.php` 에 두고, 이름에 원본의
mtime · 크기가 들어가 테마가 바뀌면 다시 만든다.

- **바꾸는 것이 정수 리터럴 하나뿐이라 문법이 깨질 수 없다.** 다른 줄은 한 글자도 안 건드린다
- 숫자를 한 군데도 못 찾으면 사본을 만들지 않고 **원본을 그대로 돌려주며 할인도 끄지
  않는다** (모를 때는 건드리지 않는 쪽) — 그때는 관리자에게 알림이 뜬다
- fee 를 떼는 쪽(`kill()`)과 사본을 건네는 쪽이 **같은 `killing()` 을 본다.** 한쪽만
  걸리는 일이 없어야 화면과 금액이 갈리지 않는다
- 모드 `duckhoo_auto_discount_mode`: `off`(그대로) · `preview`(쿠키 `dhr_disc_off` 를
  가진 사람에게만 — 라이브에서 확인할 때) · `on`(모두, 현재). 되돌리기는 `off` 한 줄,
  또는 플러그인 비활성화
- 사본 폴더에 `index.html` · `.htaccess` 를 두고 사본 첫 줄에 `ABSPATH` 검사를 세운다

**프로덕션 실측** (2026-09-09, 껐을 때):

| 장바구니 | 상품 | 총 주문금액 |
|---|---|---|
| 노보 낱병 10병 (전에 0원이 나던 그것) | 135,000 | **135,000** ✅ |
| 펠릭스 2개 | 178,000 (정가차 −22,000 은 그대로) | **178,000** ✅ |
| 노보 10병 + 펠릭스 1개 | 224,000 | **224,000** ✅ |
| 낱병 2병 (기준 미달) | 27,000 | 29,500 (배송비 2,500) ✅ |

장바구니의 「10만원 이상 1만원 할인」 안내도 같이 사라진다 (`duckhoo_auto_discount` 를
비우면 front.js 가 `#coupon-auto-notice` · `#coupon-applied-notice` 를 지운다).

검증: `php design/php-tests/run.php` (자동 할인 23개 — 사본이 PHP 로 읽히는지 ·
테마 파일이 그대로인지 · 다른 템플릿은 건드리지 않는지) ·
`scratchpad/live/disc3.mjs`(결제 화면 네 장바구니) · `disc4.mjs`(장바구니 화면 · 안내 문구).

### 홈 맨 위 검은 공지 띠는 스니펫이 그린다 — 글자만 우리가 정한다 (2026-09-09)

자동 할인을 껐는데 띠는 **「9월 특가 진행 중 — 10만원 이상 구매 시 10,000원 자동 할인!」**
이라고 그대로 말하고 있었다. 없는 혜택을 믿고 담은 손님은 결제 화면에서 배신당한다.

- 그 띠(`#wd-top-announce`)는 사장님 Code Snippets **두 개**가 만든다. **파일은 건드리지
  않는다** — `#pop6` · `#dh-agegate2` 와 같은 방식으로 **글자만** `front.js` 가 갈아 끼운다
  - **A** `injectAnnounceBar` — 띠를 만든다 (DOMContentLoaded, 아직 8월 문구가 들어 있다)
  - **B** `dhfPatchTopBar` — 그 띠를 9월 문구로 **덮어쓴다.** `MutationObserver(subtree:true)`
    라 body 가 바뀔 때마다 다시 돈다. 이미 고친 것은 `data-dhf="1"` 로 가른다
  - 그래서 **한 번 써 놓고 끝내면 안 된다.** 우리가 먼저 `data-dhf="1"` 을 찍어 B 가
    지나가게 하고, 그래도 덮이면 띠 자신을 8초간 지켜보다 되돌린다 (처음엔 이걸 놓쳐
    배포하고도 화면은 그대로였다)
- 문구는 `Front\announce()` (필터 `duckhoo_announce`). 지금은
  `10만원 이상 1만원 자동 할인 이벤트가 9월 9일부로 조기 종료되었습니다.`
  **빈 문자열이면 띠 자체를 없앤다.** 스니펫 글자를 그대로 두려면 `duckhoo_take_announce` 를 false 로
- **문구를 고른 기준.** ①손님이 **본 이름 그대로** 쓴다 — 「금액 자동 할인」은 우리끼리
  쓰는 말이고 띠에 떠 있던 것은 「10만원 이상 구매 시 10,000원 자동 할인」이었다.
  ②**언제부터인지** 적는다 — 어제 그 할인을 받은 손님이 자기가 잘못 본 줄 알면 안 된다.
  ③**「그동안 이용해 주셔서 감사합니다」를 쓰지 않는다** — 쇼핑몰 맨 위 검은 띠에 그 말이
  있으면 **가게가 문을 닫는 것처럼** 읽힌다. ④사과 · 인사는 공지사항에 둔다 — 폰에서
  세 줄이 되면 화면을 먹는다
- **띠는 홈에서만 보인다.** 테마 화면(`dhr-wrap`)에서는 shell.css 가 이미 숨긴다.
  그래서 결제 화면 스크린샷에는 안 나온다 — 없어진 것이 아니다
- 옛 문구가 한 번 번쩍이지 않게 `body.dhr-ann #wd-top-announce:not([data-dhr-ann])` 를
  `visibility:hidden` 으로 두고, 글자를 넣은 뒤에 보인다. 띠는 우리 스크립트보다 뒤에
  만들어질 수 있어 **지금 · DOMContentLoaded · load** 세 번 보고, 그래도 놓치면
  body 를 8초간 지켜본다

### 상품 후기가 아예 없었다 — 우리 템플릿이 그 자리를 빼먹고 있었다 (2026-09-09)

사장님: 리뷰 작성 기능이 없다. 관리자 `상품 → 상품평` 메뉴가 있으니 **워드커머스
리뷰는 켜져 있는데**, 상세를 우리 템플릿으로 바꾸면서 리뷰 영역이 통째로 안 그려지고
있었다 (상품 173개 전부 리뷰 0 · 평점 0인 이유).

- 안쪽은 **워드커머스가 그린 그대로**다 — `Product\reviews()` 가 자리와 제목만 주고
  `comments_template()` 을 부른다. 폼 · 필드 이름 · 논스 · 구매자 확인이 그쪽 것이라야
  관리자 `상품평` 화면과 별점 집계가 맞는다. 자리는 배송·교환·환불 다음, 함께 볼 상품 앞
- **가져온 상품은 `comment_status` 가 `closed` 로 남는다.** 전체 설정을 켜도 그런 상품엔
  리뷰가 안 나오는데 173개를 하나씩 열 수는 없다 → `comments_open` 필터로 **상품에만,
  전체 설정이 켜져 있을 때만** 연다 (`duckhoo_force_product_reviews` 를 false 로 하면 끈다)
- **테마가 `#reviews` 를 `display:none !important` 로 접는다** (리뷰를 탭 안에 넣어 둔
  잔재). 우리 상세에는 탭이 없어 열어 줄 사람이 없다 — shell.css 도 `!important` 로 편다.
  처음엔 이걸 놓쳐 영역 높이가 43px, 제출 버튼이 0px 였다
- 별점 `select` 는 `form.cart` **바깥**이라 우리 선택창(`.dhsel`)에 걸리지 않는다
  (`.dhsel` 은 `form.cart select.ppom-input` 만 본다)
- 쓸 자리가 바로 아래 있으면 「아직 상품평이 없습니다」 상자는 폼 제목과 같은 말이라
  `#reviews:has(#commentform)` 일 때만 뺀다 — 비로그인처럼 쓸 자리가 없을 때는 그대로 둔다
- **`innerText` 로 「숨겼는지」를 재면 안 된다.** 렌더되지 않는 요소의 `innerText` 는
  `textContent` 를 그대로 돌려준다 — 숨겼는데도 글자가 잡혀 한 번 헛짚었다. 화면으로 본다
- 카드에는 이미 평점 자리가 있어(`평점·판매수는 실제 값이 있을 때만`) 후기가 쌓이면
  목록 · 홈에도 별이 같이 뜬다

**구매한 고객만 쓴다** (사장님 결정 2026-09-09). 관리자 설정을 기다리지 않고
플러그인이 그 값을 정한다 — `option_woocommerce_review_rating_verification_required`
필터가 `yes` 를 돌려주므로 관리자 화면의 체크박스도 켜진 것으로 보인다.

- **폼을 감추는 것만으로는 모자란다.** 워드커머스는 안 산 사람에게 폼을 안 그릴 뿐이라
  주소만 알면 그대로 보낼 수 있다 — `preprocess_comment` 에서 **보내는 것도 막는다**
  (`guard_review()`). 상품 후기일 때만 보고, 관리자는 지나간다 (답글 · 정리)
- **워드커머스는 이 가게의 주문을 「샀다」고 보지 않는다.** `wc_customer_bought_product()`
  는 `wc_get_is_paid_statuses()` = `processing` · `completed` 만 센다. 그런데 이 가게는
  입금전(on-hold) → 입금확인(payment-confirmed) → 배송준비중(ready-to-ship) →
  배송완료(delivered) 로 흐르고 **그 중 어느 것도 그 목록에 없다.** 그대로 두면
  「구매한 고객만」을 켜는 순간 **산 사람에게도 폼이 안 나온다** (실제로 그랬다 —
  구매 이력이 있는 계정으로 확인).
  `woocommerce_pre_customer_bought_product` 로 **후기 판정 하나만** 우리가 답한다 —
  `woocommerce_order_is_paid_statuses` 를 넓히면 매출 · 정산 · 재고까지 따라 움직인다.
  세는 상태는 `배송완료 · 완료 · processing` (필터 `duckhoo_review_bought_statuses`) —
  **받은 사람만** 센다. 사진 후기를 쓰려면 물건이 손에 있어야 한다
- 끄려면 `add_filter( 'duckhoo_reviews_verified_only', '__return_false' );`
- **아직 안 켠 것**: `리뷰 승인 후 노출` (워드프레스 → 설정 → 토론). 사장님 판단

**리뷰 적립금**: 사이트 어디에도 정해진 것이 없었다 (2026-09-09 확인 — 저장소 ·
공지사항 · 상품 상세 · 안내 페이지). 사장님이 **사진 후기만 1,000원**으로 정했다 →
아래 「사진 후기 적립금」 참고.

검증: `php design/php-tests/run.php` (후기 6개) · `scratchpad/live/rev.mjs`(로그인 ·
폰/데스크톱) · `rev3.mjs`(비로그인 · 폼 필드 이름 · action).

### 사진 후기 적립금 1,000원 (2026-09-09)

사장님 결정: **사진 후기만** 준다. 글만 쓴 후기에는 주지 않는다. `includes/review-photos.php`.

워드커머스 리뷰에는 사진을 올리는 자리가 없어서 우리가 만든다 — 폼의 파일칸
(`comment_form_field_comment` 뒤) · 미디어 저장(`comment_post`) · 썸네일
(`comment_text`) · 적립금.

- **적립금은 `Points\grant()` 로만 준다** (취소 반환이 쓰던 두 줄을 밖으로 뺐다) —
  잔액 `_keyple_points` 와 원장 `wd_log_keyple_points_change()` 를 **같이** 건드려야
  정산이 어긋나지 않는다. 잔액은 0 아래로 내려가지 않는다
- **주는 조건**: 사진 1장 이상 · **승인된 뒤** · 로그인 회원 · **상품 하나당 한 번** ·
  그 후기에 준 적 없을 것(`_dhr_points`)
- **사진이 붙은 후기는 검토 대기로 잡는다** (`pre_comment_approved`). 안 그러면 후기를
  써 본 적 있는 회원의 글이 **자동 승인되면서 아무도 사진을 보지 않은 채 돈이 나간다** —
  빈 화면을 찍어 올려도 1,000원이 된다. 사진 없는 후기는 그대로 지나간다 (돈이 안 걸린다)
- **내리거나 지우면 도로 가져간다** (`transition_comment_status` · `delete_comment`).
  없으면 「쓰고 받고 지우기」가 그대로 된다
- **폼의 `enctype` 은 서버에서 채운다.** `comment_form()` 은 `<form>` 을 직접 찍고
  그 칸을 넣을 필터를 주지 않는다. JS 로 붙이면 스크립트가 늦거나 죽었을 때 **사진이
  조용히 사라지고** 손님은 올렸다고 믿는다 — `Product\with_uploads()` 가 우리가 그린
  글자에서 그 한 칸만 채운다 (이미 있으면 그대로)
- 확장자를 믿지 않는다 — `getimagesize()` 로 실제 내용을 본다. 3장 · 8MB 까지
- 관리자 `상품평` 화면도 `comment_text` 를 거치므로 **사진이 거기 그대로 보인다** —
  승인 전에 사장님이 보고 판단할 수 있다
- 필터: `duckhoo_photo_review_points`(1000) · `_max`(3) · `_bytes`(8MB) ·
  `_hold`(검토 대기) · `_on`(전체 끄기)

**아직 라이브에서 못 본 것 — 「진짜 구매자에게 폼이 보이는가」.** 스테이징 테스트 계정
(`kkuromi1004`)의 주문 6건이 **전부 취소됨**이라(우리가 시험하며 취소한 것들) 배송완료
주문이 하나도 없다. 그래서 폼이 안 나오는 것이 맞는 동작이고, **나오는 쪽**은 단위
테스트와 코드로만 확인했다. 사장님이 **배송완료 주문에 들어 있는 상품** 페이지를 로그인
상태로 한 번 열어 보시면 끝난다.

검증: `php design/php-tests/run.php` (사진 후기 25개 + 구매 판정 7개 — 지급 조건 ·
회수 · 대기 · 폼 · 주문 상태) · `scratchpad/live/rev4.mjs`(폼 · enctype · 파일칸) ·
`ord.mjs`(그 계정의 주문 상태).

### 후기 — 나중에 할 것 (사장님 2026-09-09)

후기가 쌓이면 그때 만든다. 지금은 **상품 상세에서만** 보인다.

- **후기 모아 보는 페이지** 한 장 (`/reviews/` — 사진 있는 것 먼저, 상품별 거르기)
- **홈에도 후기 줄** — 사진 후기 카루셀이나 별점 요약
- 둘 다 `get_comments( type: 'review' )` 로 읽으면 되고, 사진은 `_dhr_photos` 에 있다.
  카드에 이미 평점 자리가 있어 목록 · 홈은 값이 들어오는 순간 별이 뜬다

**후기는 저절로 쌓이지 않는다.** 지금 후기를 쓸 수 있는 길은 상품 상세 한 곳뿐이라,
배송완료 안내나 주문내역에서 「후기 쓰고 1,000원 받기」로 한 번 불러 주는 것이
페이지를 만드는 것보다 먼저다.

## 매출 대시보드 (2026-09-09)

`includes/sales.php` · 관리자 메뉴 **매출**. 주문 · 회원을 **읽기만** 한다.

**왜 따로 만드는가.** 워드커머스 기본 `분석` 은 매출을 `wc_get_is_paid_statuses()` =
`processing` · `completed` 로만 센다. 이 가게 주문은 입금전(on-hold) → 입금확인
(payment-confirmed) → 배송준비중(ready-to-ship) → 배송완료(delivered) 로 흐르고
**그 중 어느 것도 그 목록에 없다** — 배송완료 1,083건이 쌓여 있어도 기본 분석은
매출을 0 에 가깝게 본다. 후기의 「구매한 고객」 판정이 한 명도 안 잡혔던 것과 같은
원인이다. `woocommerce_order_is_paid_statuses` 를 넓히면 매출 · 정산 · 재고까지 따라
움직이므로 **거기는 건드리지 않고** 이 화면이 자기 목록으로 센다.

- **매출을 한 숫자로 합치지 않는다.** 무통장입금 가게라 `입금전` 은 아직 안 들어온 돈이다.
  `확정 매출`(입금확인 이후 · 필터 `duckhoo_sales_confirmed_statuses`) 과
  `입금 대기`(`duckhoo_sales_pending_statuses`) 를 나누고, 취소 · 환불 · 실패는 어디에도 안 센다
- **「지금 묶여 있는 돈」은 날짜로 자르지 않는다.** 열려 있는 주문은 언제 들어왔든 지금 문제다.
  입금전으로 `duckhoo_sales_stale_days`(5일)를 넘긴 것은 따로 경고로 세운다 — 입금이 안 됐거나
  **입금자명이 달라 자동확인이 못 걸린** 주문이라 사람이 봐야 한다
- **상품 줄(`get_items`)을 열지 않는다.** 주문 하나당 질의가 한 번 더 나간다. 적립금은 확인된
  메타(`_wd_point_discount`)에서 읽고, 수수료 줄(자동 할인 · 적립금 할인)은 `fee_map()` 이
  **주문 항목 표를 한 번의 질의로** 읽어 채운다 (이 표는 HPOS 를 켜도 그대로다).
  적립금은 **메타가 먼저** — 수수료 줄로 덮지 않는다
- **처음에 400일치를 한 번에 읽다가 화면이 죽었다** (제목만 찍히고 아래가 통째로 빔).
  PHP 오류가 화면에 안 나오는 설정이라 사장님 쪽에서는 「아무것도 안 보인다」로만 보였다. 그래서:
  - 기본 기간을 **90일**로 내리고 각주에서 `90 · 200 · 400일` 을 고를 수 있게 했다
    (`?dhr_days=`, `duckhoo_sales_scan_days`). **70일 아래로는 못 내려간다** —
    「지난달 같은 기간」이 최대 62일 뒤를 본다
  - **시간을 재다가 넘으면 읽던 만큼으로 그린다** (`duckhoo_sales_budget`, 15초).
    아무것도 없는 화면보다 「일부만」이 낫다 — 그때는 맨 위에 그렇게 적는다.
    **열려 있는 주문을 먼저 읽는다** (건수가 적고 제일 급한 숫자다) — 시간이 모자라면 긴 쪽이 잘려야 한다
  - `watch_render()` 가 `register_shutdown_function` 으로 **죽은 자리를 화면에 적는다.**
    안 그러면 다음에도 「아무것도 안 보인다」로만 돌아온다
- **재구매는 읽어 둔 범위 안에서만 센다.** 그 전에 한 번 사고 만 손님은 새 손님으로
  잡힌다 — 라벨과 각주에 그 범위를 적어 둔다
- 10분 transient 캐시. `지금 다시 읽기` 는 논스를 낀 링크다 (고른 기간을 이어 간다). 상한
  `duckhoo_sales_max_orders`(8,000건)에 걸리면 각주가 알려 준다. 각주에 **읽는 데 걸린 시간**도 적는다
- **지난달 같은 기간은 `-1 month` 로 구하면 안 된다** — 3월 31일이 3월 3일이 된다.
  `last_month_same()` 이 달 끝을 넘지 않게 자른다
- 각주의 상태 이름은 `names()` 가 **이 가게에 실제로 등록된 상태만** 남긴다.
  없는 슬러그를 그대로 적으면 사장님이 모르는 낱말이 화면에 뜬다
- **확정 매출은 적립금 · 쿠폰 · 자동 할인이 이미 빠진 금액이다** — `get_total()` 이 음수 수수료
  줄(`적립금 할인 −8,800`)과 쿠폰까지 반영한 값이라, **손님이 실제로 입금한 돈**이다.
  그래서 「나가는 돈」 표를 매출에서 또 빼면 두 번 빼는 것이 된다. 처음엔 그 표가
  `매출의 1.0%` 라고 적어 오해를 불렀다 — 매출에 애초에 없던 돈인데 매출을 기준으로
  말한 것이다. 지금은 **뺄셈 장부**로 세운다: `할인 전 주문 금액 − 적립금 − 쿠폰 − 자동할인
  = 확정 매출`, 비율도 `할인 전의 %` 다
- **배송비는 아직 안 뺀다** (사장님: 계산이 어려워 추후). 값은 `ship` 으로 읽어 두므로
  화면에 붙이는 것은 한 줄이다. 상품별 · 분류별 순위도 아직 없다 (사장님이 안 고르심)
- 폰에서도 연다 — 카드는 1 → 2 → 4열(600 · 1100px), 숫자 칸은 `white-space:nowrap`,
  표는 `.dhr-sl-scroll` 안에서만 옆으로 민다. 반투명 · 블러는 여기서도 안 쓴다

검증: `php design/php-tests/run.php` (매출 25개 — 상태 가르기 · 달 경계 · 수수료 줄 ·
적립금 우선순위) · `scratchpad/sales-render.php`(화면을 통째로 그려 390 · 1280px 스크린샷).

### 세트 구매 제한을 풀었다 (사장님 2026-09-10)

**하루 한 세트 한도를 껐다.** 이벤트 자체(배너 · 기준 가격 · 남은 재고 표시)와
한도는 스위치가 따로다 — `config()` 에 `limit_on => false` 를 두고 `limiting()` 이
`on() && limit_on` 을 본다.

- **막는 쪽과 말하는 쪽이 같은 스위치를 본다.** 한쪽만 꺼지면 「하루 한 세트」라고 써
  놓고 안 막거나, 안 써 놓고 막는다. 그래서 `limiting()` 하나가 담기 검증 ·
  장바구니 · 결제 · `store_max` · `guard_order` 와 배너 · 카드 칩 · 상세 안내 ·
  선택창 상한(`js_config`)을 **함께** 여닫는다. `limited( $p )` 도 맨 앞에서 본다
- **「제한 없음」이라고 적지 않는다.** 한도를 푼 뒤 상세 안내는 제한이라는 낱말을
  아예 안 꺼낸다 — 없던 제한을 손님 머리에 심는 말이다. 남은 재고만 말하고,
  재고 숫자가 없으면 상자 자체를 안 그린다
- **배너는 통째로 내려간다.** 그 배너는 처음부터 「노보 10+1 구매 제한 안내」였다.
  사장님이 올린 이미지 배너(`duckhoo_novo_banner_img`)도 같이 내려간다 — 그 이미지에도
  「1인 1일 최대 1세트」가 적혀 있다
- 다시 걸려면 한 줄: `add_filter( 'duckhoo_novo_event', fn( $c ) => array( 'limit_on' => true ) + $c );`
- 세는 코드(`counts()` · `over_text()` · `store_max()` …)는 **지우지 않았다.** 물량이
  다시 빠듯해지면 그대로 켜면 된다. `도구 → 노보 이벤트` 가 지금 켜졌는지 꺼졌는지 적는다

검증: `php design/php-tests/run.php` — 한도를 재는 항목들은 `$capOn()` 으로 켜 놓고 보고,
**꺼진 기본값**은 44번 항목이 따로 본다 (아홉 세트가 담기는지 · 안내가 안 뜨는지 ·
카드 · 상세 · 선택창에 「하루」가 없는지).

## 구매 여정 설계 (2026-09-10)

리디자인(껍데기)은 끝났다고 보고, **손님이 처음 들어와 돈을 부치기까지**의 길을 그렸다.
아티팩트 「액상덕후 구매 여정」(https://claude.ai/code/artifact/17a318ce-8fc6-4fe0-973a-1721c4503e54).
기억이 아니라 **프로덕션을 로그인 없이 그대로 받아 센 것**으로 그렸다.

**측정 (2026-09-10, duck-hoo.com, 비로그인 curl)**: 홈 카드 58장 — 이름 · 가격 · 할인율 ·
병당 가격은 다 보이고, **사진은 78곳 전부 「19」 가림**. `/checkout/` → 302 → `/register/`.
가입 3장 · 후기 0개. 우리 카드 가격 셀렉터는 `.pr .n` 이다 — `woocommerce-Price-amount` 로
세면 홈에 가격이 0개로 잘못 나온다 (한 번 그렇게 헛짚었다).

**좁아지는 목 셋**: ①살지 말지를 정할 수 없다(사진이 말없는 회색 사각형) ②가입 벽이 맨 끝에
서 있다(다 담고 나서 302) ③입금 — 여정에서 유일하게 사이트 밖에서 일어난다.
**우리에게 유입 · 이탈 숫자는 없다** (Jetpack 꺼짐 · GA 못 읽음). 순서는 화면 측정과 주문
데이터로 세웠고, 효과는 가입 수 · 주문 수 변화로 뒤늦게 잰다.

### 목 1 — 「19」 가림을 벽이 아니라 문으로 (2026-09-10)

가리는 것은 키플의 성인 인증 게이트라 **지킨다.** 바꾼 것은 그 자리에 **말을 얹은 것**뿐이다.

- `Front\gated()` (= 비로그인, 필터 `duckhoo_photos_gated`) · `join_url( $back )` ·
  `login_url( $back )` — 둘 다 `redirect_to` 로 돌아온다 (결제가 튕길 때 쓰는 것과 같다)
- **카드**: `.fig` 안 `<span class="lock">가입하면 사진 공개</span>` — 「무엇」만.
  `<a>` 안이라 span 이어야 한다. 불투명 검은 띠, 사진 밑변. **`aria-hidden`** — 이미지 alt 가
  이미 「로그인 후 확인 가능」이라 두 번 읽지 않게. 폰 2열에서 사진 폭이 **134px** 뿐이라 「가입하면 사진이 열려요」(11자)는 `…` 로 잘렸다 —
  `scrollWidth` 는 overflow:hidden 안에서는 잘림을 안 알려 준다. 8자로 줄이고 줄바꿈을 허용했다
- **목록 · 홈 한 줄** `gate_note()` (`.dhr-gate`): 「왜」는 여기서 **한 번만** — 성인인증
  회원에게만 · 가입 즉시 8,800원 · 본인확인 1분 → `/register/`. 목록은
  `templates/archive-product.php` 의 칩 아래 · 격자 위, 홈은 분류 타일 아래 · 첫 상품 위.
  **`Shell\archive_title()` 에 넣으면 안 돈다** — `duckhoo_take_archive` 가 true 라 그 함수는
  일찍 return 한다 (한 번 그렇게 넣고 배포까지 하고서야 알았다)
- 카드 HTML 은 10분 transient 에 들어 있어 **배포 뒤 10분은 옛 카드와 새 카드가 섞여 보인다**
  (홈 lock 5 → 61). 미니파이된 HTML 은 한 줄이라 `grep -c` 로 세면 1 이 나온다 — `grep -o | wc -l`
- **상세**: `Product\gate()` — `.dhp-gal__main`(position:relative) 안, 「19」 슬라이드 뒤에
  불투명 흰 판. 가입하고 사진 보기(48px 검은 알약) · 이미 회원이면 로그인. **`form.cart`
  바깥**이라 구매 게이트가 읽는 칸 이름과 무관하다 (확인: 폼을 만들지 않는다)
- 로그인하면 셋 다 아무것도 안 그린다. 갤러리 · 19 가림 로직에는 손대지 않았다

검증: `php design/php-tests/run.php` (가림 문 10개) · `scratchpad/live/gate-shot.mjs`
(배포를 `.dhr-gate` 렌더로 기다린 뒤 홈 · 목록 · 상세를 390/1280 에서 찍고 띠가 잘리는지 잰다).

### 깔때기는 플러그인이 센다 · 가입 뒤 되돌림 (2026-09-10)

사장님: **처음 들어왔을 때부터 구매까지의 전환이 목표, 기획도 만드는 것도 내 몫.**
그래서 숫자를 남에게 부탁하지 않고 플러그인이 센다 — `includes/funnel.php`.

- **화면 단계는 브라우저 신호로 센다.** 비로그인 화면은 페이지 캐시가 내주므로 서버 훅으로는
  안 보인다 (`?nocache=` 없이 받으면 PHP 가 안 돈다). front.js 가 `window.DHR.stage` 를
  `POST /wp-json/duckhoo/v1/f` 로 보낸다. **하루에 단계마다 한 사람 한 번** (localStorage
  `dhr-f`). 봇은 JS 를 안 돌려 저절로 빠지고, UA 로 한 번 더 거른다
- **사건(담기 · 가입 완료 · 주문 완료)은 서버 훅** — `woocommerce_add_to_cart` ·
  `user_register` · `woocommerce_checkout_order_processed`(+Store API). 캐시와 무관
- 표 `{prefix}dhr_funnel(day, stage, who, n)`, `INSERT … ON DUPLICATE KEY UPDATE n=n+VALUES(n)`
  한 질의 — 동시에 와도 안 샌다. dbDelta 로 한 번 만든다 (`duckhoo_funnel_db`).
  개인정보 없음. 매출 화면에 최근 7일 · 30일 깔때기 + 앞 단계 대비 %
- 읽는 법은 화면 각주에 적었다: 첫 화면→상세 낮으면 고르기, 상세→담기 낮으면 사진·가격·신뢰,
  담기→가입 완료 낮으면 가입 벽, 결제→주문 낮으면 주문서. **일주일 쌓인 뒤에 다음 손을 정한다**
- 끄기: `add_filter( 'duckhoo_funnel_on', '__return_false' );`

**가입 뒤 되돌림** `includes/back.php`: 키플 가입 흐름이 `/register/?redirect_to=` 를
두 번째 장(`/agree/`)으로 가는 링크에서 **버린다** (확인함 — 맨 주소다). 상품 사진을 열려고
가입해도 끝나면 아무 데나 떨어졌다. 첫 장에서 돌아올 곳을 쿠키 `dhr_back`(1시간, httponly,
Lax)에 적고, `user_register` 가 돈 요청의 `wp_redirect` 필터에서 그리로 바꾼다. 로그인은
`woocommerce_login_redirect` 가 기본(내 계정)일 때만. `wp_validate_redirect` 로 같은 사이트만.
**아직 라이브에서 못 봤다** — 본인확인 없이는 가입을 끝낼 수 없다. 사장님이 한 번 가입해
보시거나, 다음 신규 가입자의 도착 화면으로 확인한다.

**진단을 두 군데 고쳤다 (사장님 질문에 다시 재 봄)**: ①「19」 이미지는 빨간 원의
청소년 이용불가 표시라 성인이면 뜻을 안다 — 「사진이 깨진 가게」는 과장이었고, 없는 것은
설명이 아니라 **버튼**이다. ②「가입 뒤 돌아온다」는 확인 없이 한 말이었고 틀렸다 → 위 back.php.
GTM(`GTM-P4DG6S3M`) · 메타 픽셀이 붙어 있어 GA4 가 있을 가능성이 높지만 이 자리에서는 못 읽는다.

### 노보 배너

노보 분류 목록 맨 위(`duckhoo_archive_before_grid` → `Novo\banner()`). `도구 → 노보
이벤트` 에서 이미지를 고르면 그 이미지를(옵션 `duckhoo_novo_banner_img`, 폰용은
`_m`), 비워 두면 플러그인이 그린 글자판을 쓴다. 글자판 문구는 필터
`duckhoo_novo_banner`. 색은 상품의 브랜드 색(감청 `#12328F` · 노랑 `#FFE02E`)이다 —
우리 하늘색을 얹으면 노보 병 옆에서 두 브랜드가 싸운다.

### 거절 안내를 삼키는 것은 **사장님 스니펫 #21** 이다 (2026-09-09 정정)

처음에는 테마가 하는 일로 적어 두었는데, 사장님이 스니펫 원문을 보내 주셔서 확인했다 —
`스니펫 #21` (`wp_footer`, 우선순위 20)이 `#wd-coupon-toast` 를 만들고 MutationObserver 로
`.woocommerce-message` · `.woocommerce-error` · `.woocommerce-info` 를 가로챈다.
텍스트에 `제거|삭제|취소` 가 있으면 `removed`, 아니면 **전부 `success`** 로 칠하고
(거절도 성공 색이 된다) 80자에서 자른 뒤 3.5초 만에 지운다. 원본에는 인라인
`display:none` 과 `data-wd-toasted="1"` 을 박는다. 「담았습니다」 한 줄에는 맞지만
왜 안 됐는지를 설명하는 두 줄은 거기서 잘리고 몇 초 뒤 사라진다. 게다가 거절도
`success` 로 칠한다.

거절(`.woocommerce-error`)일 때만 원본을 도로 세우고(`display:block !important` —
인라인 style 을 이겨야 한다) `:has()` 로 토스트를 끈다. 워드커머스가 안내에 포커스를
옮기므로 포커스 링은 남기되 색만 상자의 것으로 바꾼다.

검증: `php design/php-tests/run.php` (노보 46개) · `scratchpad/live/novo1.mjs`(안내 ·
카드 · 칸 이름) · `novo6.mjs`(12병 거절 → 안내가 읽히는지).

### 상품 하나를 바꾸면 **다른 상품 85곳**이 같이 바뀌어야 한다 (2026-09-16)

사장님: 「브이메이트V4팟 0.7옴(2EA) 8,000원」이 「브이메이트V5팟 0.7옴(3EA) 12,000원」으로
바뀌었다 — **바꿀 게 많더라고.** 프로덕션 상품 178개의 상세를 전부 받아 세어 봤다.

| 어디 | 몇 |
|---|---|
| 상품 자체 | **1개** — `#164 [부푸] 브이메이트 V4 0.7옴팟` · 8,000원 |
| 다른 상품의 「팟, 코일」 선택칸 | **85곳** (83곳 `+8,000원` · 2곳 `+7,000원`) |
| 실제로 고쳐야 하는 PPOM 그룹 | **15군데** |

- **선택칸은 상품마다 따로 적혀 있지 않다.** PPOM 그룹(`ppom[fields][id]`) 하나를 여러
  상품이 함께 쓴다 — **그룹 49 하나가 72개 상품**을 쥔다. 나머지 14개는 묶음 · 이벤트
  상품마다 그룹이 따로라 한 그룹에 상품 하나다. 그래서 「85곳」이 「15군데」가 된다
- 값이 다른 두 그룹이 있다: `그룹 56`(펠릭스 5병 묶음) · `그룹 57`(맥스쿨)이 `+7,000원`.
  **일부러 싸게 준 자리일 수 있어 한꺼번에 덮지 않는다** — 미리 보기에서 갈라 보이고
  사장님이 고른다
- 찾는 법 (로그인 없이 된다): 상품 목록을 Store API 로 받아 상세 HTML 을 전부 내려받고
  `<option …>` 의 글자와 `data-price` 를 센다. `ppom[fields][id]` 가 그룹 번호다

**`도구 → 옵션 이름 바꾸기`** (`includes/opt-admin.php`)가 이 일을 한 번에 한다.

- **읽고 보여 준 뒤에만 쓴다.** 미리 보기가 **바뀌기 전후를 글자 그대로** 보여 주고
  「바꾸기」를 눌렀을 때만 저장한다. 못 알아본 자리는 손대지 않는다
- **주문은 건너뛴다** (`off_limits()`) — 옛 주문은 그때 산 이름 그대로 남아야 맞다.
  주문의 옵션 이름은 주문 항목 표에 따로 있어 여기 손이 닿지도 않는다
- **묶인 값(serialize)은 글자만 바꾸면 깨진다** — `s:24:"…"` 의 길이 숫자가 안 맞는다.
  풀어서 바꾸고 다시 묶는다. JSON · 맨 글자는 **이름 뒤 가까운 값 칸만** 본다
- **값을 못 찾으면 이름만 바꾸고 값은 그대로 둔다** — 모를 때는 건드리지 않는 쪽
- 되돌리기는 바꾸기 전 원본을 통째로 들고 있다가 되돌린다 (`duckhoo_opt_rename_backup`)
- **폼 필드 이름(`name` · `id`)은 건드리지 않는다.** 선택칸 이름은 `f_____` 그대로이고
  `data-optionid` · `ppom-option-…` 클래스는 PPOM 이 이름에서 다시 만든다
- 상품 자체(이름 · 값)도 같은 화면에서 바꾼다. **주소(slug)는 그대로 둔다** — 바꾸면
  검색엔진에 올라간 주소가 끊긴다. 메타 설명은 템플릿이라 새 이름을 저절로 따라간다
- **값이 바뀌면 이미 담겨 있던 장바구니가 걸린다.** 테마 `dh-price-guard` 가 옵션 행의
  합계와 `line_subtotal` 을 견주므로 옛 값으로 담은 줄은 결제에서 막힌다 — 손님은
  장바구니를 비우고 다시 담으면 된다. 화면 아래에 그렇게 적어 두었다

검증: `php design/php-tests/run.php` (옵션 이름 바꾸기 45개 — 다시 풀리는지 · 다른 옵션이
안 바뀌는지 · 값을 못 찾을 때 · 주문을 건너뛰는지 · ID 로 바꾸기).

### 옵션은 **이름이 아니라 ID 로 찾는다** (2026-09-16, 이 건의 결론)

이름(한글)으로 DB 를 뒤져 **여섯 번 연속 0건**이 나왔다. 글 메타 → 설정 → 글 본문 →
모든 표까지 넓히고, escape 된 꼴 · 빈칸이 다른 꼴까지 다 해 봐도 안 나왔다.
그동안 사장님께 미리 보기를 여섯 번 누르게 했다. **답은 처음부터 상품 페이지 HTML
안에 있었다** — 그 화면의 `ppom_input_vars` 가 저장된 모습을 그대로 보여 준다:

```json
{"option":"브이메이트V4팟 0.7옴(2EA)","price":"8000","weight":"","stock":"",
 "id":"_____v4__0_7__2ea_"}
```

**옵션마다 영문 · 숫자로만 된 고유 ID 가 같이 저장된다.** 한글이 escape 돼 있든
빈칸이 다른 종류이든 ID 는 그대로다. 이것으로 찾으면 한 번에 걸린다 (실제로
61군데를 찾아 43군데를 바꿨다).

- `scan_key()` · `swap_key()` — ID 로 찾아 **그 옵션 덩어리 하나만** 손댄다.
  묶인 값(serialize)은 풀어서, JSON 은 그 객체만 글자 수술. **저장된 모습을 따라간다**
  (escape 돼 있었으면 escape 해서 넣는다). 옆 옵션은 한 글자도 안 바뀐다
- **ID 자체는 절대 바꾸지 않는다** — 장바구니 · 주문이 그 ID 로 옵션을 알아본다
- 화면의 「옵션 ID」 칸이 차 있으면 이름 대신 그것으로 찾는다. 비우면 이름으로

**교훈: 이 사이트에서 무엇을 찾을 때는 한글부터 대지 않는다.** 화면 · HTML 에
영문 열쇠(ID · slug · data 속성)가 같이 있으면 그것이 언제나 확실하다. 그리고
**DB 를 못 읽는 상황에서 추측을 한 번에 하나씩 시험하지 않는다** — 한 화면에
진단을 다 넣어 한 번에 답이 나오게 만든다 (지금 화면이 그렇게 돼 있다).

**결과 확인 (2026-09-16, 프로덕션 178개 전부 다시 받아 셈)**: 선택칸 85곳 전부
`브이메이트V5팟 0.7옴(3EA) [+12,000원]` · 옛 이름 0곳 · 옆 옵션(소울V2 9,000 ·
젤로맥스 13,500 · 젤로 부스트팟 12,000) 그대로 · 상품 #164 는
`[부푸] 브이메이트 V5 0.7옴팟` 12,000원 (JSON-LD · 메타 설명도 따라감).
그룹은 49번 하나가 72개, 나머지 13개가 묶음 · 이벤트 상품 하나씩이었다.

### 목록 정렬 — 막고 있던 것은 **젯팩 검색**이었다 (2026-09-16)

사장님: 검색 결과에 가격 순 정렬이 있으면 좋겠다. `includes/sort.php`.

화면에 정렬 줄(`추천순 · 낮은 가격 · 높은 가격 · 신상품`)을 **링크로** 그린다 —
`?orderby=` 로 가므로 JS 없이 되고 주소를 복사해 보내면 같은 화면이 열린다.
페이지 번호는 떼고 옮긴다. 자리는 `.dha-bar` (건수 옆), 폰에서는 건수를 위로 올린다.

**누가 정렬하는가 — 화면마다 다르다.**

| 화면 | 정렬하는 쪽 |
|---|---|
| 분류 · 전체 상품(`/shop/`) | **워드커머스** (`WC_Query::product_query`) — 손대지 않는다 |
| 검색 결과 | **우리** (`is_search()` 라 워드커머스가 일찍 물러난다) |
| 브랜드 `/brand/<slug>/` | **우리** (`pre_get_posts` 로 바꾼 화면이라 `is_post_type_archive()` 가 아니다) |

**여기까지 오는 데 네 겹을 벗겼다. 순서대로 적어 둔다 — 다음엔 이 순서로 본다.**

1. **`woocommerce_catalog_ordering()` 은 화면에 한 번도 나온 적이 없었다.**
   `wc_setup_loop()` 없이는 `wc_get_loop_prop('is_paginated')` 가 비어 아무것도
   안 그린다. 템플릿에 함수가 **적혀 있는 것**과 **그려지는 것**은 다르다
2. **`$q->set('orderby', …)` 는 부탁이다.** 뒤에 도는 누군가가 덮으면 조용히 진다 →
   `posts_orderby` 로 직접 썼다
3. **`posts_clauses` 가 `posts_orderby` 보다 늦다.** 우선순위 999 로 써도 조각을
   통째로 갈아끼우면 진다 → 거기에도 같이 쓴다
4. **그래도 안 바뀌었다.** 진단을 보니 `posts_clauses` 의 **원래** 값이 이미
   `wc_product_meta_lookup.max_price DESC` 였다 — **워드커머스가 건 정렬도 안 먹고
   있었다.** SQL 다툼이 아니라 SQL 자체가 버려지고 있다는 뜻이다.
   `posts_pre_query` 에 걸린 이름을 찍으니 답이 나왔다:
   **`Automattic\Jetpack\Search\Classic_Search::filter__posts_pre_query`**

**젯팩 검색은 `posts_pre_query` 로 SQL 을 통째로 건너뛰고 결과를 자기가 내준다.**
`jetpack_search_should_handle_query` 로 물러나 달라는 것은 이 버전에서 안 먹어,
`drop_engine()` 이 **클래스 이름으로 찾아 그 훅을 이 요청에서만 뗀다**
(필터 `duckhoo_sort_drop_engine`). **손님이 가격순을 고른 그 요청에서만** 뗀다 —
기본 검색은 젯팩이 하던 대로 두어 검색 품질을 안 건드린다.

- **정렬에 `ID` 꼬리를 붙인다.** 낮은 가격순은 되는데 높은 가격순만 안 되던 마지막
  한 겹이 워드프레스닷컴 쿼리 캐시(`Advanced_Post_Cache`)였다 — 열쇠가 달라져
  새로 계산된다. 덤으로 **같은 값끼리 순서가 고정**돼 쪽을 넘겨도 같은 상품이
  두 번 안 나온다 (실측: 1쪽 ↔ 2쪽 겹침 0)
- 값은 **곁붙임 질의**로 읽는다 — `meta_key` 를 걸면 JOIN 과 WHERE 가 따라붙어
  그 칸이 없는 상품이 결과에서 빠질 수 있다. 정렬 때문에 상품이 사라지면 안 된다
- 모르는 `orderby` 값은 무시한다 (주소를 손으로 바꿔도 안전)
- **`?dhr_sort=1`** — 고른 것 · 검색/메인 여부 · post_type · 우리가 쓴 ORDER BY ·
  실제로 돈 질의의 ORDER BY · 떼어낸 것 · `posts_pre_query`/`the_posts`/`posts_results`
  에 걸린 콜백 이름을 화면 아래 주석으로 적는다. **사람에게 캡처를 부탁하지 않고
  주소 한 번으로 가른다** — 이 건은 그 줄들이 없었으면 못 찾았다.
  진단 주석은 여러 줄이라 `grep` 으로는 안 잡힌다 (SQL 에 줄바꿈이 있다)

**교훈: 정렬 줄이 보이는데 순서가 안 바뀌면 「보이는 것」과 「하는 것」이 갈린 것이다.**
이 사이트에서 목록을 손볼 때는 **화면을 그리는 쪽과 결과를 내주는 쪽을 따로** 확인한다.

검증: `php design/php-tests/run.php` (정렬 20개) · 프로덕션 실측 — 검색 · 전체 · 분류 ·
브랜드 네 화면에서 낮은/높은 양방향 · 정렬 줄 · 쪽 넘김 겹침 0.

## 저장소 구조

```
duckhoo-redesign.php     플러그인 본체 — 배포됨
includes/                front.php(홈·회원탈퇴 페이지) · shell.php(나머지 화면 껍데기) · membership-cancel.php — 배포됨
templates/               home.php · page-membership-cancel.php · content-product.php(목록 카드) — 배포됨
assets/                  tokens · front.css/js(공통) · shell.css(테마 껍데기 걷기) · duckhoo-theme.css(폼·계정) — 배포됨
.deployignore            아래 것들을 배포에서 제외
design/                  시안·기반 문서 (참고)
reference/duckhoo-front/ 기존 프론트 플러그인 소스 (참고)
.claude/skills/          Claude Code 스킬 98개 (13MB)
```

`.deployignore` 덕분에 실제로 사이트에 올라가는 것은 플러그인 본체와 토큰
CSS 뿐이다. 새 파일을 저장소 루트에 추가할 때는 그것이 배포 대상인지 확인한다.

## 라이브 버그 두 개

### 주문취소 — 원인 확인, 고쳐서 넣어 뒀다

**실제 상태 목록 (2026-09-02 스테이징 관리자 주문 화면, 1,652건):**

| 슬러그 | 이름표 | 건수 | 뜻 |
|---|---|---|---|
| `on-hold` | 결제 확인 중 | 293 | **입금 전** (무통장입금 주문이 여기로 들어온다) |
| `payment-confirmed` | 입금확인 | 8 | 입금 매칭 완료 |
| `ready-to-ship` | 배송준비중 | 112 | |
| `delivered` | 배송완료 | 1,083 | |
| `completed` · `cancelled` · `refunded` · `checkout-draft` | 완료됨 · 취소됨 · 환불됨 · 임시글 | | WooCommerce 기본 |

플러그인 설명에 있는 "입금전"·"확인필요" 상태는 **실제로는 없다.** 입금 전 = `on-hold`.

WooCommerce 는 기본적으로 `pending`·`failed` 에만 취소 링크를 그리므로 `on-hold` 주문에
버튼이 없었다. `duckhoo-redesign.php` 의 `woocommerce_valid_order_statuses_for_cancel`
필터로 **`on-hold`** 를 열었다 (혹시 나중에 "입금전" 이름표가 생기면 그것도 잡는다).
`payment-confirmed` 이후는 열지 않는다 — 환불이 남아 사람이 봐야 한다.

주의: `on-hold` 는 "입금 문자가 왔는데 이름이 안 맞는" 경우도 포함할 수 있다
(확인필요 상태가 따로 없으므로). 그때 고객이 취소하면 환불이 남는다. 이건 운영 판단이다.

### 회원탈퇴 — 버튼만 있고 뒤가 없었다. 만들어 넣었다

`/membership-cancel/` (page 521) 은 내용이 `/` 뿐이지만, 테마의 마이페이지 템플릿이
자기 "회원탈퇴 신청" 상자를 그린다 (로그인 시 · 비로그인은 302). 그 버튼은
`admin-ajax.php?action=wd_membership_cancel` 을 부르는데 **서버에 그 액션이 없다** —
로그인 상태로 틀린 논스를 보내면 없는 액션과 똑같이 `400 "0"` 이 온다. 그래서 눌러도
"오류가 발생했습니다" 만 떴다.

`includes/front.php` 가 이 페이지도 `template_include` 로 우리 껍데기에 그리고
(`templates/page-membership-cancel.php`), `includes/membership-cancel.php` 가 폼과
처리를 맡는다. 테마의 상자와 JS 는 그 템플릿에 있어서 아예 안 나온다.

- **계정을 지우지 않는다.** 로그인을 끊고(비밀번호 무효화 + `authenticate` 차단)
  이름·연락처·주소만 지운다. 주문은 그대로 남는다 — 전자상거래법상 거래기록 5년 보존
- 확인 절차: 비밀번호 재입력 + 동의 체크 + 논스. 셋 다 서버에서 본다
- 막는 조건: **끝나지 않은 주문이 있을 때**, **적립금이 남아 있을 때**
- 화면은 숏코드 `[duckhoo_membership_cancel]`. 페이지에 직접 넣으면 그쪽이 우선이고,
  슬러그가 `membership-cancel` 이면서 내용이 사실상 비어 있으면 자동으로 대신 그린다

**적립금 읽는 키를 아직 모른다.** keyple-customer 가 어디에 저장하는지 공개돼 있지
않아, 회원 메타에서 이름(`point`/`mileage`/`reward`/`적립`)으로 찾는다. 못 찾으면
적립금으로는 막지 않는다. 정확한 키를 알게 되면 한 줄이면 된다.

```php
add_filter( 'duckhoo_member_points', fn( $v, $id ) => (float) get_user_meta( $id, '<키>', true ), 10, 2 );
```

검증: `php design/php-tests/run.php` — 워드프레스 없이 스텁으로 22개 항목을 본다.

## 알려진 문제

- **`[dh_coverflow]` 숏코드가 동작하지 않는다.** `duckhoo-front` 가
  `dist/dh-front.js` 를 찾는데 그 폴더가 아예 없다. 숏코드를 쓰면 빈 div 만
  출력된다. 되살리려면 바닐라 JS(GSAP Draggable/Observer)로 다시 짜야 한다.
- `duckhoo-front` 의 CSS 에 `!important` 가 270개 있다. 테마(WoodMart 계열,
  옵션 폼이 `wd-option-builder`)와 싸우는 중이라 테마 업데이트에 취약하다.
- `duckhoo-front` 는 설명과 달리 React/Tailwind 가 아니다. 순수 PHP + CSS +
  jQuery 다. 빌드 단계가 없으므로 직접 수정하면 된다.
- `--dhx-ink-3: #94A3AE` 가 11.5px 상태 텍스트에 쓰이는데 흰 배경 2.59:1 로
  AA 에 크게 미달한다.
- **주문서에서 테마 스크립트가 죽는다.** `assets/js/wd-checkout-custom.js:185` 가 래퍼 밖에서
  `$` 를 쓰는데 워드프레스는 `jQuery` 만 준다 (`$ is not a function`). 그 줄부터 안 걸리므로
  **쿠폰 카드 체크박스가 아무 일도 하지 않는다.** 테마 파일이라 우리가 고치지 않았다.
  플러그인에서 고치려면 테마 스크립트보다 먼저 `window.$ = window.jQuery` 를 넣으면 된다
  (이 사이트에서 `$` 를 쓰는 다른 스크립트는 없다). 사장님 판단이 필요하다

## 데모 사이트

`design/demo.html` — 12개 화면이 전부 눌리는 한 파일짜리 데모.
`python3 design/demo-src/build.py` 로 다시 만든다 (소스는 `design/demo-src/`).
배포 대상이 아니다 (`.deployignore` 에 `design/` 이 들어 있다).

실제 운영 규칙을 데모에도 그대로 넣어 뒀다. 디자인을 볼 때 이것들이 같이 보여야 한다.

- 주문서에서 **입금자명 ≠ 주문자명** 이면 `확인필요` 로 접수된다 (일치하면 `입금전`)
- **주문취소는 `입금전` 하나뿐이다.** 확인필요부터는 입금 문자가 이미 들어온 상태라
  버튼이 잠기고 안내 문구가 뜬다 (플러그인 쪽 필터도 같은 규칙이다)
- **회원탈퇴는 끝나지 않은 주문이나 남은 적립금이 있으면 막힌다.** 막힌 이유를
  모달에 그대로 적어 준다 (플러그인 쪽도 같은 규칙이다)
- 회원가입은 **본인확인이 1단계**, 정보입력이 2단계다. 건너뛸 수 없다

검증: `design/demo-src/demo-test.mjs` (Playwright). 390 / 768 / 1280px 에서
14개 라우트의 가로 넘침·JS 오류를 보고, 검색 · 로그인 → 담기 → 주문 → 취소,
가입 → 탈퇴 흐름을 끝까지 눌러 본다.

테스트는 데모 파일에 **아티팩트와 같은 뼈대(charset + viewport 메타 + 리셋)를
씌워서** 880px 미만은 `isMobile` 로 본다. viewport 메타가 없으면 모바일
브라우저가 레이아웃을 980px 로 잡아 버려 실제와 전혀 다른 화면을 검사하게 된다.

### 문서는 스크롤하지 않는다 — 앱 껍데기

`body` 는 `height:100dvh; overflow:hidden` 세로 플렉스이고, 스크롤은 `#view`
안에서만 한다. 헤더와 아래 탭바는 흐름 안의 플렉스 아이템이다 — fixed 도
sticky 도 아니다.

이유: 문서가 스크롤하면 모바일 브라우저 주소창이 접혔다 펴지며 뷰포트 높이가
바뀌고, 그때마다 fixed/sticky 탭바가 같이 움직인다 (홈 → 주문내역에서 그랬다).
안에서만 스크롤하면 주소창이 건드려지지 않는다. 그래서:

- 라우트 전환 시 `#view.scrollTo(0,0)` (window 가 아니다)
- ScrollTrigger 는 `scroller:'#view'`
- 테스트가 라우트마다 문서가 스크롤되지 않는지(`docH <= cliH`)와 탭바 위치,
  그리고 홈에서 900px 스크롤한 뒤와 주문내역으로 옮긴 뒤 탭바 `top` 이
  같은지를 본다

### 홈 구성 (레퍼런스: FootWear 스니커즈 몰 랜딩)

사용자가 준 데스크톱 레퍼런스를 그대로 따른다. ui-ux-pro-max 의 landing 도메인
"Marketplace / Directory" 패턴과 같다 — **검색이 첫 CTA**, 그 다음 분류·특가·목록·신뢰·CTA.

1. 데스크톱 상단 유틸 바 (공지 · FAQ · 카톡 문의 · 배송조회 · 19세 안내)
2. 헤더: 로고 · **넓은 검색창**(데스크톱) · 장바구니 알약(병 수) · 아바타. 모바일은 검색 아이콘
3. 히어로 2장: 왼쪽 흰 카드는 **묶음 상품 슬라이드**(최대 5장, 이전/다음 · 점 · 멈춤 버튼,
   손가락 넘김, 5초 자동 넘김 — 마우스·포커스가 오면 서고 `prefers-reduced-motion` 이면 안 돈다),
   오른쪽 하늘색 그라데이션 "9월 특가"
4. **오늘의 특가** 회색 패널 — 마감 카운트다운(9/30 23:59 KST 까지 1초 단위) + 가로 카드
5. **지금 고르세요** — 가운데 큰 제목 + 4열 그리드 12개 + 더 보기 (필터 알약 3개는 걷어냈다)
6. **브랜드로 둘러보기** — 종수 · 2×2 썸네일 카드 3장. **'공식' 이라는 말을 쓰지 않는다**
7. 이번 달 볼 것 — 커버플로우 (사용자가 요청한 21st.dev 컴포넌트)
8. 검은 배너
9. 검은 푸터(소개 · 카카오톡 채널 · 링크 4열 · 법적 고지) — **모든 화면에** 붙는다.
   `route()` 가 어느 화면이든 `html + footer()` 로 그린다. 화면 함수 안에 넣지 않는다

상품 카드는 레퍼런스대로: 아이브로(신상·9월 특가·재구매 1위·품절) · 찜 하트(`S.wish`,
`aria-pressed`) · 이름 · ★평점·판매수 · 정가 취소선 + 가격. 평점·판매수는 **예시값**
(id 로 정해진다). id 6 은 품절 예시 — 구매 버튼이 잠긴다.

큰 제목이 헤더 뒤로 들어가면 헤더 가운데에 작은 제목이 켜진다 (`.gnb.small .ttl`).

### 글자·아이콘

- 폰트는 **Pretendard Variable 을 파일 안에 심는다** (`design/demo-src/pretendard.css`,
  data URI). 아티팩트 CSP 가 외부 폰트를 fonts.gstatic.com 으로만 막아 Pretendard 를
  링크로는 못 싣고, Noto Sans KR 은 사용자가 싫다고 했다. KS X 1001 2,350자 + 데모에서
  쓰는 글자, wght 400–800 만 남긴 서브셋이라 310KB 다. 안드로이드 기본 한글 폰트는
  중간 굵기가 없어 600 이 400 으로 그려지므로 웹폰트 없이는 안 된다
- 다시 만들기: `npm i pretendard`, `pip install fonttools brotli` 한 뒤
  `pyftsubset PretendardVariable.woff2 --text-file=<2350자+사용글자> --flavor=woff2 …`,
  `fonttools varLib.instancer … wght=400:800`, base64 로 `pretendard.css` 에 넣는다.
  데모에 새 한자·특수문자를 쓰면 서브셋에 없을 수 있다 — 그 글자만 시스템 폰트로 빠진다
- 숫자에 모노 폰트를 쓰지 않는다. 가격에 IBM Plex Mono 를 썼더니 개발자 화면처럼
  보였다. 같은 글꼴의 `tabular-nums` 로 맞춘다
- 캡션 최소 13px, 본문 15–16px, 큰 제목 34px/900. 11–12px 은 쓰지 않는다
- 아이콘은 유니코드 글리프가 아니라 `design/demo-src/icons.json` 의 인라인 SVG
  (24px, 2px 스트로크). 빌드가 JS 상수 `I` 와 껍데기 HTML 양쪽에 넣는다

## 검색 노출 (SEO) — 바닥 다지기 (2026-09-10)

사장님 목표: **베이프몬스터 · 노보마트를 이기고 싶다.** 2026-09-10 실측(네이버 DataLab · 프로덕션 비로그인):

| | 베이프몬스터 | 노보마트 | 액상덕후 |
|---|---|---|---|
| 가게 이름 검색량 (상대값) | ≈65 | ≈7.4 | ≈2.4 |
| 상품 수 | 817 | — | 173 |
| 후기 | 상품당 수백 개 | — | 0 |
| 펠릭스 더블라임 | 14,500 | — | 20,000 |
| 상품 설명 | JS 렌더 | **글** (네이버가 설명으로 보여 준다) | **이미지뿐** (글자 0) |

이름 검색량 27배 · 가격 38% 차이는 SEO 로 못 메운다. 이길 수 있는 자리는 **상품 · 브랜드 · 분류
페이지에 검색엔진이 읽을 글이 있느냐**다 — 노보마트가 이기는 이유가 그것이고, 우리는 0 이었다.

`includes/seo.php`. **테마 · AIOSEO · 키플 파일은 건드리지 않는다.**

- **네이버 서치어드바이저 인증 메타.** `도구 → 검색 노출` 에 코드만 붙인다 (`duckhoo_naver_verify`).
  사이트에 `naver-site-verification` 이 없었다 — 네이버는 인증 전에는 사이트맵을 안 받는다
- **상품 한 줄 설명(글)** `_dhr_text` — 상품 편집 화면의 상자. 상세(가격 아래 `.dhp-about`,
  `duckhoo_product_after_price` 20) · 메타 설명(`aioseo_description`) · JSON-LD `description`
  (`woocommerce_structured_data_product` 11) 세 곳에 같은 글. **안 쓴 상품은 이름 · 가격 · 분류로
  사실만 엮어**(`auto_text()`) 메타 · JSON-LD 만 채우고 화면에는 안 그린다 — 보이는 값의 되풀이다.
  행사 분류(특가 · 적립 · 랭킹)는 설명에 안 넣는다 — 달이 바뀌면 거짓이 된다
- **분류 메타 설명**: 관리자 설명이 있으면 그것(160자), 없으면 `cat_text()` (입호흡(MTL) · 폐호흡(DL) ·
  무니코틴 · 기기). 폐호흡 분류가 비어 있었다
- **브랜드 페이지 `/brand/novo/`** (`brand_slugs()` — 영문 없는 브랜드는 한글 그대로). 이름 앞
  `[노보]` · `[노보 블랙]` 을 `posts_where` LIKE 로 고른다. 목록 템플릿이 그리고 (`duckhoo_is_archive_page`),
  제목 h1 `노보 액상` 아래 `.dhr-brandintro` 한 줄(개수 · 실제 분류)이 **글자로** 있다. 제목 · 메타 ·
  canonical 은 `aioseo_title` · `aioseo_description` · `aioseo_canonical_url` 로. **`is_home` 을 꺼야 한다** —
  프로덕션 첫 화면이 블로그라 그대로 두면 `is_front_page()` 가 참이 되어 홈 템플릿이 잡힌다.
  `Front\brand_url()` 이 필터 `duckhoo_brand_url` 로 이 주소를 쓰므로 푸터 · 홈 브랜드 링크가 전부 바뀐다
  (전에는 `?s=노보` 검색 결과 — 검색엔진이 색인하지 않는 주소였다). 주소 규칙은 옵션 버전이 바뀔 때 한 번 flush
- **쓰지 않는 말**: 건강 · 금연 · 순하다 · 해롭지 않다 (담배사업법 광고 제한). 테스트가 글에 그 낱말이 없는지 본다
- **브랜드 페이지는 AIOSEO 사이트맵에 안 실린다** (워드프레스 글이 아니다) → 플러그인이
  `/brands.xml` 을 따로 낸다 (`…-sitemap.xml` 은 AIOSEO 가 가로채 404 — 그 이름을 피한다) (`brand_sitemap_xml()`, 상품이 있는 브랜드만). 네이버 · 구글에
  메인 사이트맵과 별도로 한 번 더 제출한다. 2026-09-10 사장님이 `sitemap.xml` 제출 완료
- **네이버 인증 코드는 태그를 통째로 붙여도 받는다** (`clean_code()`). 처음엔 영숫자만 남기다
  `metanamenaversiteverificationcontent04c1…` 로 이어 붙어 「메타태그를 찾을 수 없습니다」가 났다
- **사장님이 승인한 상품 글 4개**는 `includes/seo-texts.php` 에 slug 로 실었다 (펠릭스 더블라임
  입 · 폐, 네스티 하이민트 더블 슬로우블로우 입 · 폐). 상자(`_dhr_text`)가 비어 있을 때만 쓰고,
  상자에 쓰면 그쪽이 먼저다. 프로덕션에서 상세 `.dhp-about` · JSON-LD 에 들어간 것을 확인했다.
  펠릭스는 AIOSEO 메타 설명을 사장님이 이미 써 두어 그것이 그대로 남는다 (비어 있을 때만 채운다)
- 잽쥬스는 안 판다 (사이트맵 174개에 없다) — 입고 뒤에 한다

검증: `php design/php-tests/run.php` (검색 노출 51개). 라이브는 `/brand/novo/` 의 h1 · 소개 줄 · `<title>` ·
meta description, 상품의 JSON-LD `description`, 폐호흡 분류의 meta description 을 curl 로 본다.

## 결제 금액 검증은 테마에 있다 — `dh-price-guard.php` (2026-09-11)

사장님 신고: 결제 화면에 「"[리퀴드랩] 핑크 그레이프" 상품의 주문 금액이 올바르게 계산되지
않았습니다. (정상 금액 19,800원)」. **우리 플러그인에는 그 글자가 없다** — 테마다.

- **어디**: `키플_액상덕후/inc/dh-price-guard.php` (장바구니 · 결제 차단) ·
  `inc/dh-price-audit.php` (주문 생성 후 검산 + 관리자 화면). 3단계로 막는다:
  ①결제 단계에서 `WC()->cart->calculate_totals()` 강제 재계산 → ②`wc_add_notice(...,'error')`
  로 결제 차단(우선순위 99) → ③주문이 생기면 `확인필요`(`wc-need-check`)로 돌리고 주문 메모
- **무엇을 견주는가**: 옵션 빌더가 있는 줄만 본다.
  `expected = wd_calculate_builder_total( $cart_item['wd_option_builder'], $base_price )` 대
  `actual = $cart_item['line_subtotal']`, 1원 미만은 반올림으로 넘긴다.
  즉 **옵션 행(JSON)이 말하는 금액**과 **워드커머스가 실제로 물린 금액**이 갈리면 막는다
- 로그: `error_log('[DH 금액오류] 결제 차단 product_id=%d 정상=%s 청구=%s user=%d')`.
  정상↔청구의 **차액**이 원인을 말해 준다 (옵션값 하나만큼 차이 나면 그 옵션 행이 빠진 것)
- **옵션 수량 규칙은 또 다른 곳이다** (헷갈리지 말 것): 묶음의 `맛 선택을 총 N개` 는
  `functions.php:9095`(JS) · `10776`(PHP `woocommerce_check_cart_items`) ·
  `wd-option-builder.js` · `wd-single-product-checkout-popup.js` 네 군데가 각자 검사한다
- **이번 건은 사장님이 Codex 로 직접 고쳤다** (2026-09-11). 우리 플러그인은 손대지 않았다.
  다시 나면 위 로그의 차액부터 본다

### 도구 → 코드 찾기 (`includes/finder.php`)

화면 문구가 어느 코드에서 나오는지 찾는 관리자 화면. 테마(부모 · 자식)의 PHP · JS 와
Code Snippets 표(`{prefix}snippets`)에서 낱말을 찾아 앞뒤 12줄을 보여 준다. **읽기만 한다.**
테마 · 스니펫 파일이 이 저장소에 없어 진단 때마다 사장님께 캡처를 부탁하던 것을 대신한다.

### 금액 오류는 다시 나지 않는다 — 검수 결과 (2026-09-11)

사장님이 Codex 로 고친 뒤, 터졌던 조합을 프로덕션에서 비로그인으로 그대로 재현해 봤다.
`dh-price-guard` 가 견주는 두 값(**옵션 행 합계** vs **`line_subtotal`**)을 직접 재서 본다.

| 시험 | 옵션 행 합계 | 실제 줄 금액 | |
|---|---|---|---|
| 디오리퀴드 로젤하트 · 옵션 2개 (터졌던 것) | 29,800 | 29,800 | ✅ |
| 리퀴드랩 핑크 그레이프 · 옵션 2개 (터졌던 것) | 19,800 | 19,800 | ✅ |
| 핑크 그레이프 · 옵션 2개 + 유료 팟(+8,000) | 27,800 | 27,800 | ✅ |
| 로젤하트 옵션 2개를 **두 번** 담기 | 59,600 | 59,600 | ✅ |
| 펠릭스 더블라임 옵션 2개 + 화이트아웃 1개 (두 상품) | 54,900 | 54,900 | ✅ |

장바구니 화면에 오류 안내도 없다. **아직 못 본 것**: 결제 화면은 비로그인이 못 들어가
(302 → `/register/`) 로그인 계정이 있어야 본다. 다만 게이트가 보는 값이 위 두 개뿐이라
그것이 맞으면 게이트는 통과한다. **고치기 전에 담아 둔 장바구니는 세션에 그대로 남아
계속 걸린다** — 안내대로 지우고 다시 담으면 된다.

검증: `scratchpad/live/guard4.mjs` (네 경우) · `guard3.mjs` (두 상품 + 장바구니 · 결제 화면).

### 상품 글이 검색 결과에 나간 적이 한 번도 없었다 (2026-09-11)

프로덕션 상품 **175개의 메타 설명을 전부 받아 보고** 알았다. **137개가 꼬리까지
똑같았다** — 「… 상품입니다. 가입 시 적립금 8,800원 증정 + 3만원 이상 무료배송으로
빠르게 만나보세요.」 이름과 분류만 바뀌는 AIOSEO 템플릿이라 맛 · 용량 · 니코틴이
한 글자도 없다. 우리 `description()` 은 「AIOSEO 값이 비어 있을 때만」 채우는데
템플릿이 **늘** 값을 채우므로, 상품 글은 화면(`.dhp-about`)과 JSON-LD 에만 살고
**검색 결과에는 닿은 적이 없었다.**

- `templated()` 가 그 꼬리를 가려낸다 (필터 `duckhoo_meta_desc_template_marks`).
  상품 상세에서 **그때만** 우리 글로 바꾼다. 사장님이 손으로 쓴 38개는 그대로 둔다 —
  펠릭스 넷이 그렇다
- 손으로 쓴 글에는 누를 이유를 꼬리로 한 줄 붙인다 (`duckhoo_meta_desc_tail`).
  사실로 엮는 `auto_text()` 는 같은 꼬리를 이미 달고 있어 **두 번 붙이지 않는다**
- `auto_text()` 는 이름이 브랜드로 시작하면 앞의 브랜드를 떼고 쓴다 —
  `[맥스쿨] 맥스쿨 소다…` 가 「맥스쿨 맥스쿨 소다」로 나갔다
- **메타 설명은 화면만 보고 판단할 수 없다.** 우리 글이 상세에 보여도 검색 결과는
  다른 글일 수 있다. 새 글을 붙였으면 `curl | grep 'name="description"'` 로 본다

**사장님이 손으로 고쳐야 하는 것 두 개** (AIOSEO 상자의 글이라 플러그인이 손대지 않는다):

| 상품 | 지금 메타 설명 | 왜 틀렸나 |
|---|---|---|
| `[노보] 데저트` | 「노보 **블랙** 데저트 액상 30ml…」 | 일반 라인 상품에 블랙 설명이 붙었다 |
| `[노보 블랙 리퀴드] 10+1` (130,000원) | 「…10병을 **병당 7,000원(총 70,000원)**에」 | 실제 값과 다르다. 주소(slug)에는 또 「병당 8000원 · 금액 80000원」이 적혀 있다 |

### 노보 10+1 두 개가 브랜드 페이지에서 빠져 있었다 (2026-09-11)

이름이 `[노보 리퀴드] 10+1` · `[노보 블랙 리퀴드] 10+1` 이라 `[노보]` · `[노보 블랙]`
접두에 안 걸렸다. `/brand/novo/` 에 10병이 하나도 없었고, 브랜드 목록에는 상품
하나짜리 「노보 리퀴드」 · 「노보 블랙 리퀴드」가 따로 서서 `brands.xml` 에도 그
주소가 실렸다. `brand_aliases()` 에 둘을 더하면 브랜드 페이지 · 목록 · 티커 ·
사이트맵이 한꺼번에 따라온다 (그 네 곳이 모두 이 함수를 본다). 고친 뒤 12종.

**상품 175개의 이름 앞 대괄호를 전부 뽑아 대조**했고 같은 식으로 쪼개진 브랜드는
이 둘뿐이다 (`과일먹구싶오` 는 `얼려먹구싶오` 와 다른 라인이라 그대로 둔다).

### 얼려먹구싶오 24종 (2026-09-11)

모드 10종 · 단품 13종 · 5병 묶음. **분류로 확인된 것만 쓴다** — 모드는 「무니코틴 ·
폐호흡 액상」이라 폐호흡(DL)이라 적고, **단품 13종은 분류가 「무니코틴」뿐이라
입호흡이라고 쓰지 않았다** (사장님께 분류를 넣을지 물어볼 것). **용량(ml)은 이름 ·
상품 화면 · Store API 어디에도 없어 적지 않았다.** 5병 묶음의 맛 목록이 곧 단품
13종이라는 것은 상품 화면에서 확인했다. 주소 `…-자-무니코틴-액상` 은 **자몽**이다 —
주소와 이름이 다른 여섯 번째 상품.

### 단품에서 색을 두 개 고를 수 있었다 — 젤로 팟 (2026-09-14)

사장님 신고: `젤로 크리스탈 0.6옴 팟 [블랙/클리어]` 는 1팩 10,000원인데 **클리어와
블랙을 둘 다 골라도 담기가 되고 값은 10,000원 그대로**다. 프로덕션에서 재현했다 —
주문 데이터에 두 줄이 그대로 실린다:

```
[{group_key:"addon_pod_0", label:"…클리어팟", qty:1, unit_price:0, type:"addon"},
 {group_key:"addon_pod_0", label:"…블랙팟",   qty:1, unit_price:0, type:"addon"},
 {group_key:"required_main", …, qty:1, unit_price:10000}]        총 상품금액 10,000원
```

**뿌리는 색이 `addon`(수량을 올릴 수 있는 추가 구매 항목)으로 등록돼 있다는 것**이다.
색은 「둘 중 어느 것」이지 「몇 개 더」가 아니다. 제대로 고치는 자리는 관리자다 —
아래 「사장님 쪽에서 할 것」.

**2026-09-07 의 `.dhx` 규칙과 같은 생각, 다른 화면이다.** 저쪽은 옛 옵션 UI(묶음 ·
이벤트)의 `.dhx-card` 를 보고, 이쪽은 **테마 옵션 빌더가 직접 그리는 단품 화면**을
`wd_option_builder_json` 으로 본다 (`.wd-option-item` · `button.wd-option-remove[data-group][data-label]` ·
`.wd-option-minus` · `.wd-option-plus`).

- **`.dhx` 가 있으면 물러난다.** 묶음의 맛 칸(`addon_1`)도 값이 0원이라 그대로 두면
  같이 잡힌다 — 거기는 세트당 5개가 정상이다. 확인: 디오 5+5 는 `.dhx-card` 4개 ·
  이 팟은 0개. 라이브에서 맛 3개를 골라 그대로 남는 것을 봤다
- **상한은 세트 수(`type:required` 의 수량) × `freeChoicePerSet`.** 그래서 막는 규칙이
  아니라 **「고른 개수 = 사는 개수」** 규칙이다 — 2팩을 사면 색도 두 개 고를 수 있다
- 마지막에 고른 것을 남기고 먼저 고른 줄의 `×` 를 대신 눌러 **바꿔 준다**.
  **넘친 만큼만 내린다** — 먼저 고른 줄의 수량이 넘친 양보다 크면 지우지 않고 `−` 를
  한 번 누른다 (처음엔 통째로 지워 2팩짜리가 1개로 줄었다)
- 안내 글자는 `data-l` + CSS `content` 로 그린다. 폼 안에 텍스트 노드를 더하면
  구매 게이트가 읽는 칸 이름이 바뀐다. `.dhr-onenote` 규칙을 `.wd-option-builder` ·
  `form.cart` 안까지 넓혔다
- **안내는 방금 고른 칸 바로 아래에 붙인다** (사장님 2026-09-14). 고른 것이 쌓이는 목록은
  선택칸보다 **위**에 있어서(측정: 목록 1063px · 선택칸 1400px) 거기 붙이면 손이 있는
  자리에서 멀다. `.dhsel` 다음에 넣는다 — 원본 select 는 1px 로 접혀 있어 **보이는 칸은
  `.dhsel` 쪽**이다 (네이티브 select 를 기준으로 위아래를 재면 틀린 답이 나온다).
  확인: 폰 · 데스크톱 둘 다 선택창 아래 8px · 화면 안
- **한 번만 보면 놓친다.** `change` 는 테마가 숨은 필드를 고치기 **전에** 오고, 테마가
  옵션 상자를 다시 그리면 거기 걸어 둔 관찰자가 떨어져 나간다 — 그래서 안 줄어드는 일이
  섞여 나왔다. 몸통(body)을 보고 고른 뒤 **250 · 800 · 1600ms 에 다시 본다**
- 테마가 다시 그리는 데 시간이 걸려 **고른 직후 1.8초 뒤에 재면 아직 안 줄어 있을 수 있다** —
  시험할 때는 4초쯤 기다리고 본다

**상품 175개를 훑어** 값이 붙지 않은(=고르는) 선택칸이 있는 상품은 16개인데 15개가
묶음(5병 · 10병 골라 담기)이고 **묶음이 아닌 것은 이 팟 하나**다.

**사장님 쪽에서 할 것 (지금 것은 앞단 방어다)**

1. **상품을 색상별로 둘로 나눈다** — 「…0.6옴 클리어팟」 · 「…0.6옴 블랙팟」 각 10,000원.
   옵션이 사라지므로 헷갈릴 것이 없고 재고도 색깔별로 세며 검색에도 각각 잡힌다.
   맥스쿨은 이미 맛별 상품으로 나뉘어 있다 — 이 가게의 원래 방식이다 (추천)
2. 하나로 두려면 색을 `addon` 이 아니라 **단일 선택**으로 등록한다 (워드커머스 변형상품 등).
   이 사이트의 구매 흐름(PPOM + 테마 빌더)과 섞이는 자리라 시험이 필요하다
3. **이름도 손보면 좋다** — 지금 이름이 `[블랙/클리어]` 라 **한 팩에 두 색이 다 온다**고
   읽힌다. 「(2입) · 색상 선택」처럼 적으면 옵션을 열기 전에 안다

검증: `scratchpad/live/pod5.mjs`(1팩 바꾸기 · 묶음은 그대로) · `pod6.mjs`(2팩 두 색) ·
`pod7.mjs`(세 번째는 넘친 만큼만 · 버튼 클래스 확인).

## 쿠폰 — 발급은 되는데 보이지도 먹히지도 않았다 (2026-09-14)

사장님: **자동쿠폰만 해 놨고, 실제로 쿠폰을 발급하고 적용하는 시스템이 필요하다.
키플이 만든 쿠폰 시스템이 있는데 정확하게 잘 되지 않는다.** 코드 배포(오픈채팅 · 문자)로
가기로 했고, **적용 버그부터** 잡는 순서로 정했다.

### 키플 쿠폰 시스템의 생김새 (관리자 설정 화면 · 프론트 스크립트에서 확인)

| | |
|---|---|
| 플러그인 | `keyple-coupon-manager` (`assets/frontend.js` · `frontend.css`) |
| 발급 | `keyple_receive_coupon`(받기 버튼) · `keyple_redeem_serial`(코드 입력) — 둘 다 admin-ajax |
| 상품 상세 박스 | **켜짐** · 「다운로드 가능한 쿠폰」 / 버튼 「쿠폰 받기」 · 위치 **판매가 하단(권장)** |
| 정책 | **1회만 발급** · **로그인 필수** |
| 쿠폰함 | **워드커머스 마이페이지** (별도 페이지면 숏코드 `[keyple_my_coupons]`) |
| 적용 | 결제 화면의 쿠폰 카드 체크 → **워드커머스 쿠폰칸에 코드를 넣고 적용 버튼을 누른다** |

**바탕은 워드커머스 쿠폰이다.** 키플 것은 그 위의 「쿠폰함 + 발급」 껍데기다 — 그래서
쿠폰 계산 로직을 우리가 새로 짤 일이 없다.

### (가) 상세의 쿠폰 박스가 아예 안 그려졌다 — 우리 탓

사장님이 쿠폰을 만들었는데 상품 화면에 아무것도 안 보였다. **우리 상세 템플릿은
`woocommerce_single_product_summary` 를 쏘지 않는다** (쏘면 테마의 제목 · 가격 · 구매
버튼이 한 벌 더 그려진다). 그래서 **그 훅에 붙는 것은 무엇이든 안 그려진다** —
상품 후기가 통째로 없던 것(2026-09-09)과 **같은 원인, 세 번째 사례**다.

- **진단 스위치 `?dhr_raw=1`** 을 만들었다 (`Front\raw_off`, 필터 `duckhoo_raw_switch`).
  주소에 붙이면 상세 · 목록에서 **우리 템플릿이 손을 뗀다** — 「안 보이는 것이 우리 탓인가,
  원래 없는 것인가」를 사장님 손 안 빌리고 가른다. 실측: 테마 쪽 `keyple-coupon-box` 4개 ·
  「쿠폰 받기」 2개 vs 우리 템플릿 0개
- **`Product\summary_extras()`** 가 그 훅에 붙은 것 중 **파일 경로가 정해 둔 폴더 안인
  콜백만** 골라 부른다 (`duckhoo_summary_extra_dirs`, 기본 `keyple-coupon-manager`).
  자리는 `duckhoo_product_after_price` 30 — 플러그인이 권장하는 「판매가 하단」과 같은 자리이고
  **`form.cart` 바깥**이라 구매 게이트가 읽는 칸 이름과 무관하다
- **보는 훅은 우리가 절대 쏘지 않는 것 하나뿐이다.** 구매 폼 안쪽 훅까지 넓히면 폼이
  그릴 것을 우리가 먼저 그려 **두 번** 나온다
- 확인: 가격 → 쿠폰 박스 → 혜택 → 구매 카드 순, 폰 282px · 데스크톱 485px, 폼 밖.
  묶음 맛 고르기 · 팟 색상 규칙은 그대로 (회귀 확인)

### (나) 결제 화면의 쿠폰 카드가 **코드 한 줄 때문에** 죽어 있었다 — 테마 탓

`wd-checkout-custom.js` 는 `jQuery(function ($) { … })` 로 열리는데 **마지막 28줄(쿠폰 카드
체크박스 핸들러)이 그 래퍼 밖**이다 — 183줄에서 닫히고 185줄부터 다시 `$` 를 쓴다.
워드프레스는 전역에 `$` 를 주지 않으니 `$ is not a function` 이 나고 **핸들러가 등록조차
되지 않는다.** 쿠폰함에서 받은 쿠폰을 결제에서 체크해도 **아무 일도 일어나지 않았다.**

테마 파일은 건드리지 않고 **같은 핸들러를 front.js 가 등록**한다. 테마 쪽이 살아나면
물러나고(`$._data(document,'events')` 에 같은 선택자가 있는지 본다), 그래도 둘 다 걸릴 수
있어 **같은 코드를 1.5초 안에 두 번 적용하지 않는다.** `window.$ = jQuery` 로 전역을 만드는
길도 있지만 전역을 하나 더 만드는 대신 이쪽을 골랐다.

### 아직 안 본 것 · 남은 것

- **장바구니 화면에 쿠폰 입력칸(`coupon_code`)이 0개**다. 코드를 배포해도 손님이 넣을
  자리가 장바구니엔 없다 — 결제 화면에 있는지는 로그인해야 본다
- **결제 화면에 들어갈 때마다 「쿠폰함 …」 알림이 뜬다**(사장님). 그 글자가 테마 · 스니펫 ·
  클라이언트 스크립트 어디에도 없다 → **플러그인 PHP** 다. 그래서 `도구 → 코드 찾기` 가
  **플러그인 PHP 도 뒤지게** 넓혔다 (기본 `keyple-*` · `duckhoo-*`, 체크박스로 전부,
  12초 예산 `duckhoo_finder_budget`)
- 장바구니 서랍이 **주문서에서는 안 열게** 했다 — 담기 알림이 남아 있으면 결제하러 온
  손님 앞에 장바구니가 다시 펴졌다
- 확인 필요: **쿠폰 사용 활성화**(상점설정 → 설정 → 일반) · **로그인 계정**(결제 화면)

검증: `php design/php-tests/run.php` (상세 다시 그리기 3개) · `scratchpad/live/coupon-shot.mjs`
(박스가 폼 밖에 · 화면 안에) · `pod5.mjs`(묶음 · 팟 회귀).

### 주소(slug)와 상품 이름이 다른 상품이 다섯 개 있다

상품을 주소로 찾아 글을 붙일 때 **반드시 `<h1>` 과 대조한다.** 안 하면 엉뚱한 상품에
남의 맛 설명이 붙는다 — 실제로 두 개가 그렇게 나갔다 (2026-09-11 대조에서 잡아 뺐다).

| 주소 | 실제 상품 |
|---|---|
| `화이트아웃-파인애플-9-8mg-30ml복사` | 화이트아웃 **체리** |
| `디오리퀴드-블루워터-9-8mg-30ml` | **화이트아웃** 블루레몬에이드 |
| `파이-아이스멘솔9-8mg-30ml` | **디오리퀴드** 레드에너지 |
| `디오리퀴드-멜론-9-8mg-30ml` | 디오리퀴드 **로젤하트** |
| `디오리퀴드-샤인머스켓-9-8mg-30ml` | 디오리퀴드 **알로에 그레이프** |

앞의 셋은 글이 맞아 그대로 두고(주소를 고쳐도 따라가게 새 주소도 같이 실었다),
**뒤의 둘은 맛이 달라 글을 뺐다.** 사장님 확인 뒤에 넣는다.
대조법: `grep -o "^\s*'[^']*'\s*=>" includes/seo-texts.php` 로 주소를 뽑아
`curl`로 각 상품의 `<h1>` 을 받아 나란히 본다.

## Codex SEO 1차 작업을 이어받았다 (2026-09-11)

사장님이 Codex 로 **홈 제목 · 설명(AIOSEO 글로벌)** 과 **홈 · 브랜드 구조화 데이터 보정
스니펫 ID 34** 를 운영에 적용했다. 우리 `includes/seo.php` 와 겹치는 자리가 브랜드 페이지라
**중복부터 확인했다 — 겹침 없다.**

| 화면 | meta description | og:title / og:description | canonical | h1 |
|---|---|---|---|---|
| 홈 | 1 | 1 / 1 | 1 | 1 |
| `/brand/novo/` | 1 | 1 / 1 | 1 | 1 |
| 상품 상세 | 1 | 1 / 1 | 1 | 1 |

브랜드 페이지 JSON-LD 는 이제 `BreadcrumbList[홈 > 상품 > 노보 액상]` + `CollectionPage`
(이름 · 설명 있음)다. 이름 · 설명은 **우리 `title()` · `brand_intro()` 가 만든 값**을
스니펫이 읽어 쓴다 — 두 쪽이 같은 값을 본다.

**되돌리기**: 구조화 데이터만 되돌리려면 스니펫 **ID 34 만** 비활성화한다
(가격 보정 스니펫 **ID 33** 은 별개다). 홈 제목 · 설명은 AIOSEO 같은 화면에서 복원한다.

### 이어서 고친 것

- **홈에 대표 제목이 없었다.** h1 다섯 개가 전부 히어로 슬라이드(묶음 상품) 제목이라
  이 화면이 무엇을 파는 곳인지 말하는 제목이 하나도 없었다. 슬라이드는 `h3.hcard-t` 로
  내리고, `<main>` 맨 위에 **화면에서만 감춘** h1 한 줄을 세운다 (`.dhr-h1`, 필터
  `duckhoo_home_h1`). **디자인은 한 픽셀도 안 바뀐다** — CSS 규칙은 선택자만 옮겼다.
  확인: 홈 h1 1개 · `.hcard-t` 5개
- **입호흡 분류 설명이 230자였다.** 검색 결과는 한글 80자 안팎에서 잘리는데 검색엔진이
  자르면 **문장 한가운데**서 끊긴다. `Seo\cap()` 이 **문장 끝**에서 자른다 (앞 문장이
  최대 길이의 절반을 넘을 때만 — 아니면 낱말 경계 + 말줄임표). **원문은 안 건드린다** —
  AIOSEO 관리 화면의 글은 그대로고 검색 결과로 나가는 값만 줄어든다.
  끄기: `duckhoo_meta_desc_max` 를 0 으로. `도구 → 검색 노출` 이 160자 넘는 분류를 적는다

검증: `php design/php-tests/run.php` (길이 자르기 7개) · 프로덕션 curl 로 홈 h1 · 분류 메타 ·
홈 · 브랜드 중복 없음 확인.

### 쿠폰 한 번에 만들기 (2026-09-15)

사장님이 손님마다 다른 금액의 쿠폰 코드를 문자로 뿌린다. 관리자에서 하나씩 만들면
열 장만 넘어도 일이 된다 → `includes/coupon-admin.php`, **마케팅 → 쿠폰 한 번에 만들기**
(마케팅 메뉴가 없으면 도구로 간다).

- **만드는 것은 워드커머스 쿠폰이다** (`WC_Coupon`) — 키플 쿠폰함이 아니라 장바구니 ·
  결제가 실제로 계산에 쓰는 그것. 만드는 길도 관리자에서 손으로 만드는 것과 같다
- 한 줄에 한 장. **처음 나오는 숫자가 금액**이고 나머지가 메모, `@` 가 든 낱말은
  이메일로 보아 그 계정만 쓰게 한다. 끝의 `x5` 는 장수다. `#` 로 시작하는 줄은 건너뛴다
- 모두 **한 번만 · 한 사람당 한 번만** (`usage_limit` · `usage_limit_per_user` = 1).
  코드에 헷갈리는 글자(`0 O 1 I L`)를 쓰지 않는다
- **만료일은 하루를 더해 저장한다** — 워드커머스는 만료일 00:00 에 끊으므로 9월 30일을
  그대로 넣으면 **29일까지만** 쓴다. 화면에는 사장님이 고른 날짜를 그대로 적는다
- 「미리 보기」로 무엇이 몇 장 만들어지는지 먼저 보고 **「만들기」를 눌렀을 때만** 만든다.
  한 번에 `duckhoo_coupon_batch_max`(300)장까지
- 만든 뒤 **문자에 붙여 넣을 목록**(`{코드}` `{금액}` `{메모}` `{만료}` `{주소}`)과
  기록용 목록(코드,금액,메모,이메일)을 뽑는다. 날짜는 `kdate()` 가 `9월 30일` 로 적는다
- 되돌리기는 **「전부 만료」** — 지우지 않는다. 이미 쓴 쿠폰은 건드리지 않는다
  (주문에 기록이 남아야 한다). 묶음은 쿠폰 메타 `_dhr_batch` 로 묶인다
- `쿠폰 사용 활성화`(상점설정 → 설정 → 일반)가 꺼져 있으면 화면 맨 위에서 알려 준다
- **줄을 `\R` 로 나누면 안 된다.** UTF 모드가 아니면 바이트 `0x85`(NEL)도 줄바꿈으로
  보는데 **한글 안에 그 바이트가 들어 있다** — 「안녕하세요」가 글자 가운데서 쪼개져
  깨진 글자로 오류 메시지가 나갔다. `/\r\n|\r|\n/` 로 나눈다. **`trim()` 의 글자 목록도
  바이트 단위**라 한글이 섞이면 글자를 반으로 자른다 — 정규식으로 뗀다
- 같은 폼에서 숨은 칸과 버튼이 이름을 나눠 쓰면 **숨은 칸이 먼저 와야 한다**.
  같은 이름이 두 번 오면 나중 값이 이기므로, 뒤에 두면 「만들기」를 눌러도 미리 보기가 된다

검증: `php design/php-tests/run.php` (쿠폰 17개 — 줄 읽기 · 장수 · 코드 · 만료일 · 문자 문구)

### 결제 화면 `쿠폰 적용하기` 가 아무 일도 하지 않았다 (2026-09-15)

사장님 영상: 코드를 넣고 눌렀는데 **안내도 오류도 없이** `쿠폰할인 − 0원` 그대로.
"적용했는데 안 먹었다"가 아니라 **버튼이 아무 데도 안 닿고 있었다.**

- 워드커머스는 그 버튼의 **클릭**이 아니라 `form.checkout_coupon` 의 **submit** 에 걸려 있다
  (`checkout.js`). 칸·버튼이 그 폼 밖이거나 버튼이 submit 이 아니면 눌러도 아무 함수도
  안 불린다 — 오류조차 안 난다. 쿠폰 카드 체크박스가 죽어 있던 것과 **다른 자리, 같은 증상**
- **결제 화면은 로그인이 있어야 열려 DOM 을 직접 못 본다.** 그래서 원인 하나에 걸지 않는다:
  ①닿을 수 없는 것이 확실하면(폼 밖 · submit 아님 · 위임 핸들러 없음) 그 자리에서 우리가 넣고
  ②닿을 수 있어 보이면 **워드커머스에게 먼저 맡기고 1.8초 지켜보다** 안 들어갔으면 그때 넣는다.
  넣기 전에 장바구니를 읽어 **이미 들어간 쿠폰이면 아무 것도 하지 않는다** (두 번 넣으면
  「이미 적용됨」 오류가 손님에게 뜬다)
- 넣는 길은 장바구니 쿠폰칸과 같은 Store API `apply-coupon`. 끝나면 `update_checkout` 으로
  **합계만** 다시 그린다 — `location.reload()` 하면 손님이 적어 둔 배송 정보가 날아간다
- 위임(capture)으로 한 번만 건다. 결제 화면은 합계를 다시 그릴 때 칸이 바뀔 수 있어
  요소마다 걸면 핸들러가 겹친다
- **결제 화면 주문 요약은 이미 `쿠폰할인` 줄이 따로 있다** (상품가격 · 배송비 · 할인 ·
  쿠폰할인 · 적립금할인 · 총 주문금액). 뭉쳐 있던 것은 장바구니뿐이다
- 「사용 가능한 쿠폰이 없습니다…」 한 줄은 뺐다 (사장님 2026-09-15) — 키플 쿠폰함
  이야기라 문자로 코드를 받은 손님에게는 틀린 말이고, 바로 아래가 그 코드를 넣는 칸이다.
  글자를 가진 **가장 안쪽 요소 하나만** `.dhr-hide-note` 로 화면에서 뺀다

**이름에 걸었더니 두 번 헛돌았다 (2026-09-15, 정정).** 사장님 캡처를 보니 화면에는 코드
넣는 자리가 **둘**이다 — 왼쪽 `쿠폰` 상자와 오른쪽 `상품권이 있나요?` 상자. 이름이
`coupon_code` · `apply_coupon` 이 아니면 우리 핸들러는 **아예 안 깨어난다.** 그래서
이름이 아니라 **화면에 보이는 것**으로 잡는다.

- **버튼에서 시작해 칸을 찾는다.** 칸에서 위로 올라가면 배송 주소칸도 「쿠폰」이 적힌 큰
  조상에 걸려 같은 버튼과 짝지어진다 — 가짜 화면 시험에서 주소칸이 먼저라 코드 대신
  빈 값을 읽었다. 버튼 → 글자칸이 **하나뿐인** 가장 가까운 줄 → 그 위의 「쿠폰」 상자
- **「상품권」 상자는 건드리지 않는다** (다른 시스템이다). 조상을 훑을 때 **더 가까운
  이름이 이긴다** — 계속 올라가면 결국 둘 다 든 조상(body)에 닿아 멀쩡한 쿠폰칸까지 물러난다
- **만든 안내 · 목록은 칸(input)에 매달아 둔다.** 안내를 상자 **밖**(줄 다음)에 붙여 놓고
  `spot.box.querySelector` 로 다시 찾으면 못 찾아 부를 때마다 새로 만든다 — 12개까지 늘었다
- 걸린 쿠폰을 `빼기` 와 함께 보여 준다 (Store API `remove-coupon`)
- **「쿠폰이 있으세요? 코드를 입력하려면 여기를 클릭하세요」 알림을 없앤다.** 접어 둔 폼을
  여는 워드커머스 안내인데, 코드 넣는 자리가 이미 펼쳐져 있어 할 일이 없고 **스니펫 #21**
  이 그것을 토스트로 바꿔 결제 화면에 들어갈 때마다 띄운다. 스니펫은 건드리지 않고
  `data-wd-toasted="1"` 을 먼저 찍어 지나가게 한 뒤 `.dhr-hide-note` 로 화면에서만 뺀다
- **안 될 때는 `?dhr_cpn=1`** — 찾은 칸 · 버튼 · `form.checkout_coupon` 안쪽 여부를
  화면 아래에 적는다. 캡처 한 장으로 끝난다

**로그인 화면을 못 보면 가짜로 만들어 시험한다.** `scratchpad/cpntest/` 에 결제 화면을
흉내 낸 HTML 두 장(테마식 · 워드커머스식)과 `fetch` 를 가로챈 Store API 대역을 두고,
front.js 에서 그 블록만 떼어 붙여 돌린다. 적용 · 빼기 · 없는 코드 · 빈 칸 · 상품권 칸을
안 건드리는지 · 안내가 하나인지를 본다 — **두 번의 헛발질을 여기서 잡았다.**

### 장바구니 합계의 할인이 한 줄에 뭉쳐 있었다 (2026-09-15)

`할인금액 4,000원` = 정가↔판매가 3,000원 + 쿠폰 1,000원. 문자로 코드를 받아 넣은 손님이
자기 쿠폰이 먹었는지 이 숫자로는 알 수 없었다. 키플 마크업은 그대로 두고 `front.js` 가
그 줄을 둘로 나눈다 (`상품 할인` · `쿠폰 할인`).

- 쿠폰 몫은 워드커머스가 준 `totals.total_discount` 그대로다 — 금액을 우리가 만들지 않는다
- 남는 몫은 `상품금액 − total_items` 와 맞을 때만 「상품 할인」이라 부른다 (아니면 「기타 할인」)
- 숫자를 못 읽거나 쿠폰 몫이 더 크면 손대지 않는다
- 합계 상자는 **한 줄에 한 칸인 격자**라 줄이 하나 늘어도 폭이 안 밀린다 (확인: 390 · 1280px 넘침 0)

### 사장님이 올린 동영상을 보는 법 (2026-09-15)

`.mp4` 는 H.264 라 **플레이라이트가 들고 있는 ffmpeg 로는 못 푼다** (`disable-everything` 빌드).
크로미움도 못 연다 (독점 코덱 없음 — `readyState 0`). `pip install imageio-ffmpeg` 로 받은
정적 ffmpeg 면 된다.

```bash
FF=/usr/local/lib/python3.11/dist-packages/imageio_ffmpeg/binaries/ffmpeg-linux-x86_64-v7.0.2
$FF -ss 14 -t 8 -i v.mp4 -vf "fps=3,crop=620:520:640:520,scale=1150:-1" -q:v 3 s%02d.jpg
$FF -ss 8 -t 14 -i v.mp4 -vf "fps=2,scale=640:-1,tile=5x6" -q:v 3 sheet.jpg   # 한 장으로 훑기
```

`tile` 로 묶어 한 번에 보고, 자리를 찾은 뒤 `crop` 으로 그 자리만 크게 다시 뽑는다.
1920px 원본을 그대로 줄이면 한글이 안 읽힌다.

### 상품권 칸을 뺐다 · 주문 요약은 AJAX 로 안 바뀐다 (2026-09-15, 결말)

**상품권.** 결제 화면에 코드 넣는 자리가 둘이었다 — `쿠폰` 과 `상품권이 있나요?`.
뒤엣것은 `woocommerce-gift-cards` 플러그인이 그리는데 **이 가게는 상품권을 팔지 않는다**
(확인: 상품권 · 기프트 · gift 검색 0건). 플러그인은 건드리지 않고 화면에서만 뺀다
(`Front\js_config()` 의 `hideGift` · 필터 `duckhoo_hide_giftcard`). 플러그인 클래스
(`wc_gc_*`)를 먼저 보고 못 찾으면 「상품권」 이라고 적힌 상자를 찾되 **「쿠폰」 이 같이
적힌 상자는 건너뛴다.** 칸 하나만 숨기면 제목 · 버튼이 남으므로 상자까지 올라간다.

**주문 요약이 안 따라왔다 — 그리고 `update_checkout` 으로는 영원히 안 바뀐다.**
쿠폰은 들어갔는데(칸 아래 `DHTEST 1,000원 할인`) 요약은 `쿠폰할인 − 0원` 그대로였다.
테마 `wd-checkout-custom.js` 의 `refreshDiscountSummary()`:

```js
couponTotal = Math.abs( parsePrice( $("#wd-summary-coupon-discount").text() ) );  // 자기에게서 읽고
$("#wd-summary-coupon-discount").text( "- " + formatPrice( couponTotal ) );        // 자기에게 쓴다
```

**서버(`form-checkout.php`)가 그린 값을 되쓰는 구조**라 AJAX 로는 절대 안 바뀐다
(테마 주석: 「#order_review 테이블이 이 체크아웃 화면에는 없어서 항상 0으로 계산되던 버그」).

- 그래서 쿠폰을 넣거나 뺀 뒤 **그 두 숫자를 우리가 써 넣는다.** 값은 워드커머스가 준 것
  그대로 — 할인액 `totals.total_discount`, 총액 `totals.total_price`(주문에 실제로 잡히는 금액).
  **우리가 계산하지 않는다** — 화면과 결제 금액이 갈리면 안 된다
- **새로고침하지 않는다** — 손님이 적어 둔 배송 정보가 날아간다
- 손댄 뒤에는 테마가 요약을 다시 그릴 때마다(`updated_checkout`) 장바구니를 다시 읽어
  맞춘다. 안 그러면 주소를 고치는 순간 쿠폰 줄이 도로 0 이 된다

**교훈: 이 결제 화면의 요약은 서버가 그린 정적 HTML 이다.** 거기 숫자를 바꾸려면
`update_checkout` 이 아니라 그 요소를 직접 써야 한다.

### 도구 → 분류 묶기 (2026-09-15)

사장님이 표로 준 상품들을 **「타격감」** 분류로 묶었다. 27개를 관리자에서 하나씩 열어
체크하는 대신 `includes/cat-admin.php` 가 한 번에 한다.

- **원래 분류는 그대로 둔다** — 새 분류를 *더한다* (`wp_set_object_terms( …, true )`).
  입호흡 · 무니코틴처럼 이미 걸려 있는 것이 빠지면 목록 · 검색이 무너진다
- **눌렀을 때만 쓴다.** 그 전에 「미리 보기」가 어느 줄이 어느 상품인지 보여 준다
- 여럿에 걸리거나 하나도 안 걸리는 줄은 **건너뛴다** — 엉뚱한 상품이 들어가는 것보다
  빠지는 편이 낫다. 정확히 집으려면 `id:254` 처럼 번호로 적는다
- 못 찾은 줄에는 **비슷한 이름을 하나 귀띔**한다 (자동으로 넣지는 않는다 —
  사장님이 적은 「파이낫푸르」와 가게의 「파이낫**푸루**」처럼 한 글자가 다를 수 있다)
- 되돌리기는 「이 분류에서 전부 빼기」 (분류 자체는 남는다)

**붙여 놓은 그대로 이어진 자리를 먼저 본다.** 낱말이 다 들어 있기만 하면 되는 규칙으로는
`[노보] 블랙멘솔` 과 `[노보 블랙] 블랙멘솔` 이 **안 갈린다** — 앞엣것도 「노보」 · 「블랙」 ·
「블랙멘솔」 을 다 가지고 있다. **테스트가 이걸 잡았다.** 괄호 안(9.8mg / 30ml)은 남긴다 —
같은 맛의 30ml 와 60ml 를 가르는 유일한 단서다.

**타격감 첫 목록 24개** (2026-09-15 사장님 확인): 흑염룡 9 · 쨩 3 · 아이스빌런 2 ·
노보 블랙 7(타박멘솔 #4617 · 엠에스블랜드 #4620 은 사장님이 새로 추가) ·
화이트아웃 멘솔시가 #298(표의 「화이트멘솔」) · 디오리퀴드 알로에 그레이프 #283(표의 「디오알포」) ·
펠릭스 더블라임 #254(`id:` 로 못 박는다 — 모드 60ml 과 안 갈린다).
**말라이트는 뺐고**, 블루워터 · 라임알로에는 사장님이 상품을 추가한 뒤 `#` 만 지우면 된다.

검증: `php design/php-tests/run.php` (분류 묶기 8개) · 실제 카탈로그 175개로
`seed()` 를 돌려 24줄 전부 1:1 인지 확인.

## 송장번호가 손님 화면에 없었다 (2026-09-17)

사장님: **관리자 주문 목록에는 「우체국 + 13자리」가 보이는데 손님에게는 「배송중」이라는
낱말 하나뿐**이다. 게다가 우리가 만든 `/shipping/` 안내가 「송장번호가 등록되면 주문내역에서
조회하실 수 있습니다」라고 약속하고 있었다 — 그 약속이 지켜지지 않고 있었다.

`includes/tracking.php` 는 **읽고 그리기만 한다.** 송장을 넣는 쪽(`keyple-order-excel-tracking` ·
`woocommerce-epost-shipping`)은 건드리지 않고, 주문 데이터에 한 글자도 쓰지 않는다.

- **저장 위치를 모르는 채로 짓는다.** 플러그인 소스가 이 저장소에 없고 관리자 계정도 없다 →
  ①필터 `duckhoo_order_tracking` ②못 박은 키(옵션 `duckhoo_tracking_meta_key`) ③이름이 알려진 키
  ④**주문 메타를 훑어** 이름이 송장을 가리키고 값이 9~14자리 숫자인 칸. 적립금(`Points\used()`) ·
  옵션 이름 바꾸기(`Opt\Admin\scan()`)와 같은 방법이다. **프로덕션에서 ④가 실제로 통했다** —
  사장님이 배송조회 버튼을 눌러 우체국 화면이 열리는 것을 확인
- **숫자만으로는 송장이라고 하지 않는다.** 키 이름이 송장을 가리켜야 한다 — 전화번호 ·
  금액 · 주문번호가 걸리면 손님이 없는 송장을 조회하게 된다
- 그리는 곳: 주문 상세 맨 위(`woocommerce_view_order` 5 · `woocommerce_order_details_before_order_table` 5,
  **한 주문에 한 번만**) · 주문 목록의 `배송조회` 버튼(`woocommerce_my_account_my_orders_actions`).
  워드커머스 동작 목록에는 `target` 을 넣을 자리가 없어 **새 창은 front.js 가 붙인다**
- 번호는 네 자리씩 띄되 **끝에 한두 자리가 혼자 남으면 앞에 붙인다** (`6890 1748 1661 9` ✗ →
  `6890 1748 16619`). 복사하는 값은 숫자 그대로
- 택배사 조회 주소는 우체국 · CJ · 한진 · 로젠 · 롯데. 이 가게 기본은 우체국
- **도구 → 송장 진단**(`includes/tracking-admin.php`) — 최근 주문에서 「손님에게 보이는 것」과
  「후보 메타」를 나란히 놓는다. 맞는 칸을 고르면 못 박아 그 다음부터는 훑지 않는다.
  **읽기 전용** — 쓰는 것은 옵션 두 개뿐이다

검증: `php design/php-tests/run.php` (송장 23개).

## 담은 뒤의 가입 벽을 문으로 · 깔때기는 두 길이다 (2026-09-17)

**「담기 → 결제 51%」는 비회원과 회원을 섞어 센 숫자였다.** 이 가게는 비로그인으로 결제 화면에
들어갈 수 없다 (`/checkout/` → 302 → `/register/`). 두 길은 애초에 같은 깔때기가 아니다.

- **`Front\join_wall()`** — 비회원이 담은 그 자리에서 왜 가입이 필요한지 말한다.
  장바구니 서랍은 `결제하기` 대신 `가입하고 주문하기`(`redirect_to` 로 결제 화면에 돌아온다) ·
  이미 회원이면 로그인 줄. **장바구니 화면은 키플 페이지라 PHP 로 끼울 자리가 없어** front.js 가
  합계 상자 위에 붙인다 — **글자는 서버가 만든다**(`window.DHR.joinWall`, 로그인하면 빈 문자열)
- **빈 장바구니에는 안 그린다.** 담긴 것이 없는데 「가입하고 주문하기」는 할 말이 아니다
  (서랍의 `render()` 가 `items.length` 로 끈다)
- 사진 가림을 문으로 바꾼 것(2026-09-10)과 같은 생각이다: **왜 · 무엇을 받고 · 얼마나 걸리는지.**
  성인인증 자체는 그대로 지킨다
- **깔때기 화면에서 「앞 단계 대비 %」를 뺐다.** 손님이 첫 화면을 거치지 않고 상세로 바로
  들어와 269% · 500% · 284% 가 찍혔다 — 내가 만든 칸의 설계 잘못이다. 대신
  **비회원 길**(가입 벽까지) · **회원 길**(주문까지) 두 표와, 뜻이 있는 고리 다섯 개를
  **나눈 두 수와 함께** 적는다 (`12 / 25 · 48%`). 사람이 적을 때 비율만 보면 안 된다
- 「가입 완료」는 서버가 회원으로 세지만(한 순간 전까지 비회원이었다) **비회원 길에서 읽어야**
  뜻이 맞는다 — `paths()` 의 `from`
- 비회원이 결제 화면에 잡히면 경고를 세운다 — 있을 수 없는 숫자다

검증: `php design/php-tests/run.php` (가입 벽 6개 · 깔때기 11개) ·
프로덕션 실측(390 · 1280px, 담기 → 서랍 → 안내 → 지우기까지).

### 쿠폰 재검수 · 장바구니 가입 벽은 안 그려지고 있었다 (2026-09-18)

사장님 요청으로 쿠폰을 프로덕션에서 다시 눌러 봤다 (비로그인 · 테스트 장바구니는 매번 치웠다).
**쿠폰 쪽은 전부 맞다** — 적용 200 · 할인 1,000 · 총액 반영 · 걸린 쿠폰 목록과 `빼기` ·
없는 코드 거절 문구 · 중복 적용 차단 · 합계의 `쿠폰 할인` 줄 분리까지 1.2초 안에 다 선다.

- **내 측정이 두 번 틀렸다.** ①목록 셀렉터를 `.dhr-cpn__list` 로 봤는데 실제는 `.dhr-cpn__on`
  ②적용하면 `location.reload()` 가 돌므로 성공 안내(`.dhr-cpn__msg`)는 당연히 사라지는데
  그것을 「안내가 없다」로 읽었다. **없는 버그 두 개를 보고할 뻔했다** — 화면 동작을 잴 때는
  그 코드가 무엇을 하는지(새로고침 여부)부터 보고 기다릴 시점을 정한다
- **진짜 버그는 다른 데 있었다.** 어제 붙인 장바구니 화면의 가입 벽이 **한 번도 안 그려졌다.**
  「이미 붙였는지」를 `document.querySelector('.dhr-wall')` 로 봤는데 **푸터의 장바구니 서랍이
  가진 같은 안내**에 걸려 늘 일찍 return 했다. 어제는 서랍만 보고 「설정값이 실려 있으니 된다」고
  넘어갔다 — **설정이 실린 것과 화면에 그려진 것은 다르다** (정렬 때의 「보이는 것 ≠ 하는 것」과
  같은 실수다). `side.querySelector()` 로 좁히고, 자리는 합계 다음 · `주문하기` 바로 위로 뒀다
- **상품 상세의 키플 쿠폰 박스가 0개**인데 `?dhr_raw=1`(우리 템플릿을 뺀 화면)에서도 0개다 →
  **우리 탓이 아니다.** 그 상품에 「다운로드 가능한 쿠폰」이 지정돼 있지 않은 것 (9/14 에는 4개였다)
- **결제 화면은 여전히 못 본다** (로그인 필요). 장바구니까지는 확인됐다

검증: `scratchpad/live/cpn-audit3.mjs`(적용 · 빼기 · 없는 코드 · 합계 줄) ·
`wall-cart.mjs`(가입 벽이 실제로 상자 안에 있는지) · `cpn-final.mjs`(390 · 1280px 스크린샷).

### 공용 쿠폰 코드 — 여럿이 같이 쓰되 1인 1회 (2026-09-18)

사장님: 「여러 사람이 같이 쓰는 코드가 필요한데 대신 한 사람당 한 번만」.

**`many` 모드(여러 사람이 같은 코드 · 한 사람당 한 번)는 이미 있었다.** 빠진 것은 둘이다.

- **공용 코드 직접 정하기** (`clean_fixed_code()`) — 오픈채팅 · 단체 문자에 뿌리는 코드는
  무작위 글자보다 **외울 수 있는 말**이라야 손님이 옮겨 적다 틀리지 않는다. 빈칸 · 기호는
  떼고(손님이 「덕후 9월」로 띄어 적으면 못 찾는다) 영문은 대문자로 통일, 4~20자.
  **적으면 한 장만 만든다** — 금액 줄이 여럿이거나 `x5` 면 어느 금액이 나갈지 우리가
  정하게 되므로 만들지 않고 말해 준다. 이미 있는 코드도 거절
- **총 사용 횟수 상한** (`uses`) — `many` 의 기본은 무제한이다. **공개된 곳에 뿌리는 코드는
  상한이 있어야 한다** (코드가 밖으로 퍼져도 거기서 끊긴다 · 선착순 N명). 0 이면 제한 없음.
  「한 장에 한 번만」을 고르면 이 칸을 무시하고 1
- `usage_limit_per_user` 는 어느 쪽이든 1. **한 사람인지는 로그인 계정으로 본다** —
  이 가게는 비로그인 결제가 안 되므로 샐 구멍이 없다
- **코드는 영문 + 숫자를 권한다**고 화면에 적었다. 한글 코드도 만들어지지만 **라이브에서
  확인한 적이 없다** — 쓰려면 한 장 만들어 장바구니에서 넣어 봐야 한다

검증: `php design/php-tests/run.php` (쿠폰 13개 추가). **`WC_Coupon` 스텁을 새로 만들어
「무엇이 실제로 저장되는지」를 잰다** — 전에는 `create()` 가 검사 밖이었다.

### 쿠폰 사용 현황은 어디서 보나 (2026-09-21)

두 곳이다. ①**마케팅 → 쿠폰 한 번에 만들기** 맨 아래 「만든 묶음」 표 — 묶음마다 `쓴 횟수 / 한도`,
「목록 다시 보기」를 누르면 코드마다. ②**마케팅 → 쿠폰**(워드커머스 기본 목록)의 `사용 / 한도` 열.
어느 주문에 쓰였는지는 그 주문 상세의 쿠폰 줄에 있다. **분석 → 쿠폰 은 쓰지 않는다** — 매출 화면과
같은 이유로 `processing · completed` 만 세어 이 가게에서는 0 에 가깝다.

- 우리 화면은 「사용됨」 한 낱말이었다 — 한 사람에 한 장인 쿠폰에는 맞지만 **64명이 같이 쓰는
  코드는 몇 명이 썼는지가 곧 현황**이다. `used_text()` 가 `17 / 64명` · `3회` · `— / 64명` · `(다 씀)`
  으로 적는다. 묶음 표는 `usage_count` · `usage_limit` 을 합쳐 한 질의로 읽는다

## 노보 품절 — 물 들어올 때 노 젓기 (2026-09-21)

사장님: **다른 사이트에서 노보 품절이 나고 있다.** 네이버 DataLab(주 단위 상대값)으로 확인:
「노보 액상」 검색이 7~8월 ≈58 → 9/7 주 **87**, 노보마트 ≈72 → **100**, 액상덕후 ≈18 → **63**.
노보를 찾아 돌아다니는 사람이 실제로 있고 우리 이름 검색도 3배다. 그런데 사이트 어디에도
「여기는 재고 있다」고 말하는 자리가 없었다 — 홈 띠는 12일 전 「자동 할인 종료」 공지였다.

- **홈 띠** `Front\announce()` → `노보 액상 전 라인 재고 있음 · 낱병 13종 · 10+1 묶음`
- **브랜드 페이지** `/brand/novo/`: `Seo\brand_notes()`(필터 `duckhoo_brand_notes`)가 브랜드별
  한 줄을 준다 — 제목 꼬리 「전 라인 재고 보유」 · 소개 첫 문장 「액상덕후는 노보 전 라인 재고를 보유…」(「다른 곳에서 품절이어도」는 사장님이 뺌 — 남 얘기).
  네이버 검색 결과에 그대로 찍히는 글자다. **품절이 풀리면 이 배열에서 노보를 뺀다**
- **노보 분류 배너** `Novo\stock_banner()` — 한도가 꺼져 있을 때 「노보 전 라인 재고 있음」
  글자판(`nvb--stock`, 필터 `duckhoo_novo_stock_banner`). **사장님 이미지 배너는 여기서 안 쓴다** —
  「1인 1세트」가 적혀 있다. 출고 조건은 `/shipping/` 과 같은 말(평일 오후 4시 · 입금 확인)
- **홈 첫 자리**: 분류 타일 · 헤더 칩 · 데스크톱 메뉴에 노보를 맨 앞으로, 히어로 0순위를 노보
  10+1 묶음으로. 히어로 제목은 브랜드를 떼면 「10+1 | 금액 120,000원」이 되어 **숫자로 시작하면
  브랜드를 앞에 붙이고 금액 꼬리를 뗀다** → 「노보 리퀴드 10+1」
- **남은 수량 숫자는 화면에서 뺐다** — 사장님: 사이트 재고 ≠ 실재고. 틀린 숫자는 없는 것보다
  나쁘다. `Novo\show_stock()`(옵션 `duckhoo_novo_show_stock`, 도구 → 노보 이벤트 체크박스)을
  켜기 전까지 카드 · 상세는 「재고 있음」만. 관리자 표는 늘 숫자를 보여 준다 (맞추라고 있는 표)
- **하루 한도는 건드리지 않았다** — 「1인 하루 3세트」를 제안했다가 사장님이 **안 한다**고 못 박음.
  `limit_on` 은 그대로 false

**SEO 로는 브랜드 페이지가 아니라 분류 페이지가 답이었다.** 네이버 「노보 액상」 30개 중 우리 것은
`/product-category/novo-liquid/` 하나(21번째)이고 `/brand/novo/` 는 없다. 그 페이지에 찍힌 제목이
「노보 액상 가격 **8종** | 10병 특가 **병당 7,000원**」 — 사장님이 예전에 AIOSEO 에 손으로 쓴 글이라
플러그인이 「손으로 쓴 건 그대로」 규칙으로 두고 있었다. 15종 · 병당 10,900원이 맞다. 검색에서 7,000원을
보고 들어온 사람이 13,000원을 보면 나간다. `Seo\cat_notes()`(필터 `duckhoo_cat_notes`, 지금 `novo-liquid`
하나)에 든 분류는 제목 · 설명을 우리가 쓴다 — **값은 적어 두지 않고 `cat_prices()` 가 상품에서 읽는다**
(재고 있는 것만 세고, 낱병 최저가 · 묶음 최저가 · 병당). 「손으로 쓴 글은 그대로」의 유일한 예외 —
틀린 값이라서. 1~20위가 거의 노보마트라 순위는 며칠에 안 바뀐다; 이번 주는 문자 · 오픈채팅이 빠르다.

사장님 몫: 실재고를 상품 → 재고 수량에 맞추고 체크박스 켜기 · 세그먼트 쿠폰 문자에 「노보 재고 있음」
한 줄 · 공지사항 글(초안 `design/novo-notice-2026-09.md`).

검증: `php design/php-tests/run.php` (노보 배너 · 수량 옵션 · 브랜드 제목 8개 갱신) ·
`scratchpad/live/novo-stock-shot.mjs`(홈 띠 · 분류 배너 390/1280) · curl 로 `<title>` · og · 타일 순서.

### 후기 카드 — 본명 · 둥근 아이콘 · 레이아웃 (2026-09-18)

사장님이 테스트 후기를 쓰고 캡처: **본명이 그대로**, 왼쪽에 **뜻 모를 둥근 무늬**, 별점이
글 오른쪽에 붕 떠 있고 「5 중에서 5로 평가됨」 글자가 새어 나왔다. `includes/review-ui.php` +
`shell.css` 의 후기 카드 부분.

- **이름은 서버에서 가린다** (`get_comment_author` 필터 · `김시원` → `김**`). 관리자 화면은
  그대로 — 사장님은 누가 썼는지 봐야 승인 · 적립을 판단한다. 후기(`review`) 타입에만 건다
- **둥근 무늬는 그라바타 identicon** — 이메일 해시로 자동 생성되는 그림이라 손님이 고른 것도
  아니고 바깥 요청만 한 번 더 나간다 → `pre_get_avatar` 가 빈 문자열을 돌려줘 아예 안 그린다
- 날짜 `9월 18, 2026` → `2026.09.18`. **`get_comment_date` 필터 안에서 `get_comment_date()` 를
  부르면 무한 재귀**다 — `mysql2date()` 로 직접 만든다
- **별점은 워드커머스 폰트(`font-family: WooCommerce`, 글자 `sssss`)를 쓰는데 테마가 그 폰트를
  안 싣고 `display:none` · `float:right` 까지 걸어 뒀다.** 유니코드 ★ 로 다시 그리고
  `float:none`, 읽어 주는 글자 `span` 은 `font-size:0` 으로 화면에서만 뺀다 (스크린리더는 읽는다)
- 카드 순서: 별 → 이름 + 날짜(오른쪽) → 글 → 사진. 카드 밖 회색 상자 ·
  `.comment-text` 의 테두리 · 여백을 0 으로
- **「(인증된 구매자)」 알약은 뺐다** (사장님 2026-09-18: 이상해 보인다). 산 사람만 쓰는 가게라
  후기마다 같은 말을 되풀이하는 것이다. CSS 로 감추지 않고 워드커머스 설정
  `woocommerce_review_rating_verification_label` 을 필터로 `no` 로 돌려줘 **마크업 자체가 안 나온다**.
  되살리기: `duckhoo_review_verified_label` 을 true 로

**확인 (2026-09-18, 프로덕션 #4327 노보 10+1 · 사장님 후기 1개, 390 · 1280px)**: 이름 `김**` ·
아바타 없음 · 별 5개가 카드 맨 위(`float:none`, 읽어 주는 글자 0px) · 인증 알약 · 날짜 오른쪽 끝 ·
사진 175/220px · 가로 넘침 0. 비로그인 안내 「상품을 구매한 로그인 고객만…」은 카드와 같은
왼쪽선(35px). 데스크톱에서 오른쪽 구매 서랍이 카드 위를 덮는 것은 서랍 설계 그대로다.

검증: `scratchpad/live/rev-final.mjs`(두 화면폭 측정 + `#reviews` 스크린샷) · `rev-note.mjs`(안내 줄 정렬).

## 담아 둔 뒤 값이 바뀌면 **옛 금액으로 주문된다** (2026-09-18)

사장님 신고: 주문 `#202609180004850` 이 **120,000원**으로 들어왔는데 같은 구성을 지금
담으면 **180,000원**이다. 60,000원이 덜 청구됐다.

**주문에 실린 값이 답을 그대로 말한다.**

```
_wd_base_price: 70000                 ← 담을 때 굳은 기준가 (옛 값)
팟 말론 0.4옴(2EA) +10,000 × 5         50,000
─────────────────────────────────────
120,000원                             ← 실제 청구
```

프로덕션 상품 페이지(#146)를 받아 보면 지금 `basePrice` 는 **130,000** 이고 `70000` 은
**한 곳도 없다** (정가 187,000 · 판매가 130,000). **상품은 이미 고쳐졌고 그 손님의
장바구니에만 옛 기준가가 박혀 있었다.** 이 가게는 회원 장바구니가 오래 남으므로
며칠 전 담은 줄이 그대로 결제된다. 그 상품은 원래 「병당 8,000원 · 금액 80,000원」
(주소에 남아 있다)에서 130,000 으로 오른 이력이 있다.

**테마의 `dh-price-guard` 는 이것을 못 잡는다.** 그쪽은 「옵션 행(JSON)이 말하는 금액」과
「워드커머스가 실제로 물린 금액」이 갈리는지만 본다 — 여기서는 **둘 다 옛 기준가로
계산돼 120,000 으로 서로 맞아떨어진다.** 기준가 자체가 낡았는지는 아무도 안 봤다.

`includes/price-check.php` 가 그 한 가지만 본다 — **담을 때 굳은 기준가 vs 지금 판매가.**

- 장바구니 · 결제에서 안내하고(`woocommerce_check_cart_items`) 주문을 막는다
  (`woocommerce_checkout_create_order`). **워드커머스 검증 훅만 쓴다** — 담기는 데이터를
  고쳐서 통과시키지 않는다. 손님이 다시 담으면 지금 값으로 제대로 잡힌다
- **세일 상품 보호가 핵심이다.** 테마가 기준가로 무엇을 넣는지 우리는 확인할 수 없다 —
  판매가가 아니라 **정가**를 넣는 구현이라면 세일 중인 상품(이 가게는 대부분)이 전부
  걸려 **결제가 통째로 막힌다.** 그래서 **판매가 · 정가 어느 쪽과도 다를 때만** 낡은
  것으로 본다. 사고의 70,000 은 130,000 · 187,000 어느 쪽도 아니라 그대로 걸린다
- 기준가를 못 찾으면 막지 않는다 (옵션 없는 평범한 상품). 차이 100원 미만도 넘어간다
- 기준가가 어디 있는지 모르므로 **세 군데를 차례로** 본다 — 줄의 값 → 옵션 JSON →
  이름에 `base_price` 가 든 칸 (`Points\used()` 와 같은 방법)
- **도구 → 금액 점검** (`includes/price-check-admin.php`) — 이미 들어온 주문 중 어긋난
  것을 **한 번의 질의**로 찾아 차액과 함께 보여 준다 (주문 항목 표를 직접 읽는다 ·
  HPOS 를 켜도 그대로다). **읽기 전용** — 옛 주문의 금액은 그때 받은 값 그대로 둔다

**교훈: 이 사이트에서 상품 값을 올리면 이미 담긴 장바구니가 옛 값으로 남는다.**
값을 바꾼 뒤에는 `도구 → 금액 점검` 을 한 번 보는 것이 좋다.

### 배포 40분 만에 사장님 결제를 막았다 — 장바구니 객체의 가격은 옵션이 합쳐진 값이다 (2026-09-18)

사장님이 **지금 값으로 새로 담은** 노보 블랙 10+1 을 결제하려는데 「가격이 바뀌었습니다 …
다시 담아 주세요」가 떴다. 다시 담아도 똑같다. **헛발질이다 — 막지 말아야 할 주문을 막았다.**
바로 `blocking()` 을 꺼 두고 관리자 전용 진단(`?dhr_price=1`)을 붙여 사장님이 붙여 준 값:

```
찾은 기준가 130,000 (wd_base_price)      ← 맞다
장바구니 객체 판매가 160,000 · 정가 187,000  ← 여기가 틀렸다
새로 읽은 판매가 130,000 · 정가 187,000
줄 소계 160,000
```

**테마가 장바구니 줄의 상품 객체(`$item['data']`)의 가격을 옵션까지 합친 값으로 바꿔 둔다.**
130,000 짜리에 팟 3개(+30,000)를 더하면 `get_price()` 가 160,000 을 돌려준다. 그것과 기준가
130,000 을 견줬으니 새로 담은 줄이 전부 「낡은 것」으로 잡혔다. 옵션이 없는 상품만 통과했다.

- `stale()` 은 **`wc_get_product( $item['product_id'] )` 로 상품을 새로 읽어** 그 판매가와
  견준다. `data` 는 번호가 없을 때만 (거의 없다). 사고의 70,000 은 여전히 걸린다
- **막기는 도로 켰다** (`blocking()` 기본 true). 끄기는 `duckhoo_price_check_block`
- `?dhr_price=1` 을 장바구니 · 결제 주소에 붙이면 관리자에게만 줄마다 기준가 · 객체 값 ·
  새로 읽은 값 · 판정을 찍는다. 늘 떠 있지 않다

**교훈 둘.** ①**장바구니 안의 `data` 객체를 믿지 않는다** — 이 테마는 거기에 옵션 값을
더해 둔다. 값이 필요하면 상품을 새로 읽는다. ②**막는 코드는 「정상 장바구니가 통과하는지」를
옵션이 붙은 상품으로 재고 배포한다.** `price-guard.mjs` 는 옵션 없는 단품으로만 쟀다 —
그래서 통과했고, 실제 사고 상품은 옵션이 5개다. **헛발질로 막는 것이 새는 것보다 나쁘다.**

검증: `php design/php-tests/run.php` (금액 점검 22개 — 객체 160,000 · 새로 읽은 130,000 인
그 경우가 통과하는지, 70,000 은 여전히 걸리는지) ·
`scratchpad/live/price-guard.mjs`(정상 장바구니가 안 막히는지 — 오류 0 · 주문 버튼 살아 있음).

### 노보 15종 상품별 SEO — 제목 · 메타 설명 · 빠진 글 (2026-09-21)

사장님: 「각각 제품에 대한 seo 작업도 해야하는데 이참에」. 프로덕션 15종 + 전체 186개의
`<title>` · 메타 설명 · `.dhp-about` 을 전부 다시 받아 셌다.

**메타 설명 — 코덱스 규격 꼬리 13개는 우리 글로.** 2026-09-11 코덱스 SEO 작업이 노보 13개에
「노보 X 액상 30ml, 니코틴 9.8mg 입호흡(MTL) 전용. 액상덕후에서 3만원 이상 무료배송, (신규) 가입 시
적립금 8,800원(증정).」 을 규격대로 찍어 두었다 — **맛이 한 글자도 없고 다섯 개는 남의 맛**이다
(쿠바시가 · 블랙 타박멘솔 · 블랙 엠에스블랜드에 「데저트」, 엠에스블랜드에 「타박멘솔 판매량 1위」,
노보 리퀴드 10+1 에 「병당 7,000원」). 「손으로 쓴 글은 그대로」 규칙에 걸려 우리 글이 검색 결과에
못 나가고 있었다. `templated()` 에 그 꼬리 둘(`무료배송, 신규 가입 시 적립금` · `무료배송, 가입 시 적립금`)
을 더했다 — **전체를 세어 보니 이 꼬리는 노보 13개뿐**이고, 펠릭스 · 네스티의 손글 7개는
「신규가입 적립금」(빈칸 없음)이라 안 걸린다. 지금 가게 전체: 우리 글 179 · 손글 7 · 옛 템플릿 0.

**제목 — 노보만 우리가 정한다** (`product_title()`, 필터 `duckhoo_product_title_brands` 기본 `['노보']`).
AIOSEO 기본 「[노보] 타박멘솔 (9.8mg / 30ml) - 액상덕후」 에는 「액상」 · 「입호흡」 · 값이 없다 →
`노보 타박멘솔 입호흡 액상 9.8mg 30ml 13,000원 | 액상덕후`, 묶음은
`노보 액상 10+1 묶음 11병 120,000원 입호흡 | 액상덕후`. **값 · 병 수는 상품에서 그때그때 읽는다** —
적어 두면 값이 바뀔 때 거짓이 된다. og:title · twitter:title 은 `aioseo_title` 을 안 거친다 →
`aioseo_facebook_tags` · `aioseo_twitter_tags` 배열에서 **있는 칸만** 바꾼다 (`social_title()`).
다른 브랜드까지 넓히려면 필터에 이름을 더하면 된다 — 펠릭스 · 네스티는 사장님이 제목까지 손으로
써 두어 그대로 둔다.

**빠진 글 3개**: 엠에스블랜드 · 블랙 엠에스블랜드 · 블랙 타박멘솔은 상세 · 메타 · JSON-LD 가 다 비어
있었다. 엠에스블랜드 맛은 공개된 사용 후기가 한결같이 말하는 것만 적었다 — 구수한 연초 맛 · 단맛 없음 ·
쿨링 없음 (`seo-texts.php`, 상자 `_dhr_text` 에 쓰면 그쪽이 먼저). **사장님 확인이 필요한 글**이다.

검증: `php design/php-tests/run.php` (템플릿 꼬리 · 제목 · og 12개 추가) · 프로덕션 15종 전부
curl — `<title>` · og:title · twitter:title · meta description · og:description · `.dhp-about` · JSON-LD
가 같은 글, 펠릭스 · `/shop/` 제목은 그대로.

## 모든 화면 안내 띠 — 응대 시간 · 주말 출고 · 노보 가격 인상 (2026-09-21)

사장님: **문의가 너무 많이 온다.** ①응대 시간이 평일 10–19 → **11–18** 로 바뀌었고 ②금요일 마감 뒤 ·
토 · 일 주문은 **다음 주 월요일 오후 4시 출고**이며 ③**노보 가격 인상**을 모든 손님이 보게 하자.

- **값은 한 곳에** — `Front\hours()`(필터 `duckhoo_hours`) · `ship_rule()`(`duckhoo_ship_rule`).
  응대 시간이 푸터 · 안내 페이지 세 장에 각각 박혀 있어 하나만 고치면 나머지가 옛 말로 남는 구조였다.
  푸터 고객센터 · `/shipping/` `/terms/` `/privacy/` · 상품 상세 혜택 줄(`benefits()`) · 사는 방법(`trust()`) ·
  「언제 출발하나요?」 · 노보 분류 배너가 전부 이것을 읽는다
- **안내 띠 `.dhn`** (`Front\notices()` → `notice_bar_html()`, 필터 `duckhoo_notices`) — `header_html()` 이
  `</header>` 바로 뒤에 찍으므로 홈 · 테마 화면 전부에 나온다. 폰은 세 줄(높이 133px · 「안내」 알약 대신 왼쪽 주황 선 — 알약 50px 을 빼야 제목이 한 줄씩 들어간다) · 데스크톱은 한 줄에 셋(51px).
  제목을 누르면 그 아래로 글이 펼쳐지고(한 번에 하나), × 는 **그날 하루만** 닫는다 (`localStorage['dhr-nb']`,
  `data-dhn` = 글의 해시라 글이 바뀌면 닫아 둔 사람에게도 다시 보인다). 서버가 늘 그리므로 JS 가 죽어도 보인다.
  항목에 `until`(Y-m-d)을 주면 지나면 저절로 빠진다 — 끝난 안내가 첫 화면에 남는 것이 이 가게에서 두 번 문제였다.
  **제목은 폰 257px 에서 한 줄**이라야 한다 — `11:00–18:00` 꼴은 두 줄이 되어 `11–18시` 로 줄였다 (`hours_short()`).
  정확한 시각은 펼친 글에 있다
- **노보 가격 인상 글은 숫자를 적어 두지 않는다.** `Novo\price_lines()` 가 재고 있는 노보 상품에서 라인 × 낱병/묶음의
  최저가를 읽어 「현재 판매가는 노보 낱병 13,000원 · … 」로 엮는다 (`price_notice_text()`). 값이 또 바뀌어도 거짓이
  안 된다. 노보 분류 배너에도 같은 줄(`.nvb__price`). **도구 → 노보 이벤트**에 글(옵션 `duckhoo_novo_price_notice`,
  비우면 자동) · 종료일(`duckhoo_novo_price_until`) 칸. 「가격이 바뀌기 전에 담아 두신 상품은 비우고 다시 담아 주세요」
  한 줄은 금액 점검(2026-09-18)이 옛 기준가 장바구니를 막기 때문이다
- **안내 페이지(`pages.php`)는 DB 에 있어 파일을 고쳐도 안 따라온다.** `VERSION` 2 — `ensure()` 가
  **우리가 넣은 뒤 사람이 손대지 않은 페이지만** 새 글로 바꾼다 (`_dhr_pages_hash` 가 지금 글과 같거나, 해시가 없는
  옛 페이지는 `post_modified_gmt == post_date_gmt`). 관리자에서 고친 글은 덮지 않는다. **`admin_init` 에 걸려 있어
  사장님이 관리자 화면을 한 번 열어야 돈다** — 그 전까지 `/shipping/` 본문은 옛 글이다 (헤더 띠 · 상품 상세는 이미 새 글)
- 테마 화면에서 테마의 `button` 규칙(!important)이 띠의 제목 · × 를 덮으므로 shell.css 가
  `html body.dhr.dhr-wrap.dhr-wrap .dhn__k` 로 도로 편다. 확인: 상품 · 장바구니 · 노보 분류 · 홈, 390/1280 — 가로 넘침 0

검증: `php design/php-tests/run.php` (응대 시간 · 출고 규칙 · 안내 띠 · 현재 판매가 · 페이지 갱신 18개) ·
`scratchpad/live/shot.mjs`(네 화면 × 두 폭 스크린샷 · 펼침 · 닫기 · 새로고침 뒤에도 닫힘 · 폭 측정).
**스크린샷은 Node 22 내장 fetch 로 받는다** — `NODE_USE_ENV_PROXY=1 NODE_EXTRA_CA_CERTS=/root/.ccr/ca-bundle.crt`
를 주고 Playwright `route` 에서 `fetch` 로 대신 받아 `fulfill` 한다 (undici 를 따로 깔 필요가 없다).
사장님 팝업(`#pop-dim`)은 우리가 지운 뒤에도 스니펫이 다시 만든다 — 누르기 직전에 다시 지우고 `force` 로 누른다.

### 노보 분류 배너를 테마에 맞춰 다시 지었다 (2026-09-21, 사장님 피드백)

사장님: 「어감도 이상하고 보기도 이상하고 … 색감도 이상하고 … 지금 테마랑 안 어울림. 재고 있다고는
좋은데 가격 인상도 같이 적어 주고.」 처음 것은 노보 브랜드 색(감청 `#12328F` · 노랑 `#FFE02E`)으로
그린 글자판이었다 — **상품 카드 · 안내 띠 · 머리판이 전부 흰 카드 · 검정 · 번트 오렌지인 사이트에서
그 둘만 다른 옷을 입고 있었다.** `Novo\stock_banner()` 를 `.nvs` 로 다시 짰다.

- **흰 카드 · 검정 제목 · 강조는 번트 오렌지** (`<em>` 은 색만, 기울임 없음). 왼쪽 「노보 · 노보 블랙
  전 라인 재고 있습니다」 + 낱병 종수(상품에서 센다) · 출고 규칙, 오른쪽 따뜻한 상자 「가격 인상 안내」 +
  현재 판매가 네 줄(`price_lines()`) + 「담아 두신 상품은 비우고 다시 담아 주세요」. 880px 부터 두 칸
- **문구는 문장으로.** 「전 라인 재고 있음 · 전 맛을 지금 바로 주문하실 수 있습니다」 같은 명사형 나열이
  어색했다 → 「낱병 13종과 10+1 묶음 모두 지금 바로 주문하실 수 있습니다」
- **폰에서 값 네 줄은 한 줄씩** (라벨 왼쪽 · 값 오른쪽, `nowrap`). 두 칸이면 「120,000 원」의 원이
  아랫줄로 떨어졌다
- 옛 `.nvb`(감청 글자판)는 한도를 켰을 때의 제한 배너라 남겨 뒀다 — 지금은 안 그려진다.
  다시 켤 일이 생기면 그것도 이 옷으로 바꿔야 한다
- 필터 `duckhoo_novo_stock_banner` 의 열쇠: `eb · head[2] · lead · notes[] · price_head · price_lead ·
  price[] · price_note`

### 안내 띠 두 번째 판 — 접지 않는 카드 세 장 (2026-09-21, 사장님 「너무 별로」)

첫 판(옅은 주황 띠 + 제목 세 줄 아코디언)은 경고 띠처럼 보였고, 펼친 글이 한 칸 아래로 좁게 흘러 지저분했다.
**문의를 줄이는 것이 목적이면 누르지 않아도 읽혀야 한다** — 접는 것을 없앴다.

- 카드 하나 = 주황 원 아이콘 · 이름표(`eb`: 고객센터 · 배송 · 노보 액상) · 굵은 한 줄(`k`) · 회색 한 줄(`s`) ·
  이어지는 곳(`more` → `url`). 카드 전체가 링크. 흰 카드 · 검정 · 번트 오렌지 — 상품 카드 · 노보 배너와 같은 옷
- 폰: 옆으로 넘기는 카드 줄(`scroll-snap`, 84% 폭이라 다음 카드가 살짝 보인다), 띠 높이 182px.
  데스크톱: 한 줄에 셋, 185px. × 는 오른쪽 위 작은 흰 원, 그날 하루만 닫힌다 (그대로)
- 글은 카드 안에서 끝나야 한다 — 긴 문장(`t`)은 필터 호환용으로만 남기고 그리지 않는다.
  노보 값은 `Novo\price_brief()` 가 「낱병 13,000원 · 블랙 낱병 13,500원 · 10+1 120,000원 · 블랙 10+1 130,000원」으로 줄인다
- `hours_short()` · `duckhoo_ship_rule_chip` 은 첫 판의 잔재로 남아 있다 (테스트가 본다). 카드 제목은
  `평일 11:00–18:00` 처럼 정확한 시각을 그대로 쓴다 — 폭이 넉넉해졌다

검증: `php design/php-tests/run.php` (카드 · 링크 · 접는 것 없음) · `scratchpad/live/shot.mjs`(네 화면 × 두 폭 ·
닫기 · 새로고침 뒤에도 닫힘 · 가로 넘침 0).

### 안내 띠 세 번째 판 — 사이트 안내 줄과 같은 옷 (2026-09-22, 사장님 「폰트도 어색하고 톤이 안 어울려」)

둘째 판(아이콘 카드 세 장)은 주황 원 · 주황 이름표 · 주황 링크가 카드마다 셋씩 — 사이트 어디에도 없는 옷이었다.
**이미 있는 안내 줄 `.dhr-gate`(흰 상자 · 왼쪽 주황 4px 선 · 글자만)와 똑같이** 다시 지었다 (`.dhn__row`).

- 흰 상자 한 장에 줄 셋. 줄 = 이름표(굵게, 검정) · 핵심(굵게) · 나머지(회색) · 이어지는 곳(주황 + 꺾쇠).
  아이콘 · 색 원 없음. 줄 전체가 링크. 폰트는 `body.dhr` 과 같은 스택을 `.dhn` 에 못 박았다 — 테마 화면에서
  다른 글꼴이 섞이는 것을 막는다
- 데스크톱 161px · 폰 215px. **폰에서는 회색 덧글을 줄인다** — 노보 값(`dhn__row--novo-price .dhn__s`)은
  분류 배너 · 상세에 있으니 뺀다. 「자세히」 글자도 빼고 꺾쇠만
- 줄마다 `dhn__row--<id>` 클래스가 붙어 하나만 따로 손볼 수 있다
- 첫 판 · 둘째 판의 잔재: `hours_short()` · `duckhoo_ship_rule_chip` · `price_brief()` — 마지막 것은 지금도 쓴다

검증: `php design/php-tests/run.php` · `scratchpad/live/shot.mjs`(홈 · 상품 · 장바구니 · 노보 분류 × 390/1280, 닫기 · 새로고침).

## 「PDF 파일 보는 느낌」— 글꼴이 아니라 자간 · 굵기였다 (2026-09-22)

사장님: 「전체적인 웹사이트 폰트가 PDF 파일 보는 느낌」. 라이브에서 재 보니 **Pretendard 400–900 은
전부 잘 실려 있었다.** 문제는 조판이다 — 본문까지 음수 자간(−0.01em, 제목 −0.025~−0.045em)에
800 · 900 굵기가 깔리고 `antialiased` 까지 걸려 인쇄물처럼 빽빽했다. 디자인 규칙(음수 자간 금지)을
우리 CSS 자신이 어기고 있었다 (front.css 37곳 · shell.css 53곳).

- **우리 CSS 의 음수 자간은 전부 0**, 굵기는 **900→800 · 800→700 · 700→600** 한 단계씩, 본문 행간
  1.55 → 1.6, `-webkit-font-smoothing: antialiased` 제거 (perl 한 번으로 세 파일을 통째로)
- **사장님 Customizer 「추가 CSS」(DB · `wp-custom-css`, 홈에도 실린다)** 가 `body · button · input ·
  .woocommerce` 에 −0.01em, `h1~h6` 에 −0.025em, 버튼에 −0.01em, antialiased 를 **전부 !important** 로
  깔아 둔다. 그 파일은 못 고치므로 front.css 맨 위 `html body.dhr,html body.dhr *{letter-spacing:0 !important;
  -webkit-font-smoothing:auto !important}` (0,2,1) 로 이긴다 — 추가 CSS 는 (0,0,1)~(0,1,2) 라 진다.
  **양수 자간(대문자 눈썹 `.eb2` · `.sh-eb` · `.nvs__eb` · 계좌 번호 `.fbank .n`)은 파일 끝에서 같은
  특이도 + !important 로 되살린다** — 뒤에 있어 이긴다. shell.css 의 양수 자간도 !important 를 붙였다
- 그 추가 CSS 가 `font-family: 'PretendardVariable', 'Pretendard'` 를 부르는데 **PretendardVariable 은
  아무도 싣지 않는다** → 정적 Pretendard 로 떨어진다. 사장님 기기에 그 글꼴이 깔려 있으면 다르게 보일 수 있다
- 확인(390 · 1280, 홈 · 상품 · 장바구니): 화면 위 2,000px 안에 음수 자간 요소 **0개**, 본문 자간 normal ·
  행간 25.6px · smoothing auto, 가로 넘침 0. 굵기 800 이상 요소는 홈 17 · 상품 7 · 장바구니 6

**교훈: 글꼴이 이상하다는 말은 먼저 컴퓨티드 스타일로 잰다** (`getComputedStyle` 의 letterSpacing ·
fontWeight · webkitFontSmoothing). 글꼴 파일이 실렸는지만 보면 「다 정상」으로 끝난다.

## 추석 연휴 출고 안내 (2026-09-22)

사장님: 9/24(목)–27(일) 택배 출고 없음 · 23(수)은 출고되지만 배송 안 됨 · 연휴 뒤 28(월)은 밀린 물량으로
출고 지연 가능 — **「진짜 다 볼 수 있게」**. `Front\holiday()` (필터 `duckhoo_holiday`: name · show_from ·
ship_only · from · to · backlog) 의 **날짜에서 글을 엮는다** (`kday()` → `9월 24일(목)`). 다음 연휴는 날짜만 바꾼다.

- **세 자리에 같은 글**: 안내 띠 맨 앞 줄(`dhn__row--holiday`, 이름표만 주황) · 홈 검은 띠(`announce()` 가
  연휴 동안 「추석 연휴 9월 24일(목)–27일(일) 택배 출고 없음」, 지나면 노보 문구로) · 상품 상세 혜택 줄
  (`benefits()` — 평소 출고 두 줄 대신). 상품 페이지는 사는 자리라 띠를 닫아 둔 사람도 여기서 본다
- **연휴 중에는 평소 출고 규칙 줄(「금요일 16시 이후 · 주말은 월요일 16시」)을 뺀다** — 그 주에는 틀린 말이다
- 글이 날짜에 따라 바뀐다: 23일까지 「23일(수) 출고분은 연휴 뒤에 도착」이 붙고, 24~27일은 28일 지연 안내만,
  **28일 당일은 「연휴 동안 밀린 물량으로 출고가 늦어질 수 있습니다」**, 29일부터 저절로 빠진다
- 글이 바뀔 때마다 `data-dhn` 해시가 달라져 **띠를 닫아 둔 사람에게도 다시 보인다**
- 폰에서 띠는 215 → 260px (연휴 줄이 세 줄). 연휴 동안만이라 그대로 둔다
- `/shipping/` 페이지에는 안 넣었다 (DB 글 · 연휴가 지나면 도로 고쳐야 한다). 띠의 「배송 안내」가 거기로 간다

검증: `php design/php-tests/run.php` (연휴 10개 — 날짜별 문구 · 줄 순서 · 해시 변화 · 홈 띠 40자 · 필터로 끄기) ·
`scratchpad/live/ty.mjs`(홈 · 상품 · 장바구니 × 390/1280 — 자간 · 굵기 · 띠 · 혜택 줄).

## 성인인증은 두 겹으로 서 있다 (2026-09-24, 코드 찾기로 확인)

사장님 걱정 「성인인증이 제대로 되고 있는지」 · 「PASS 본인인증이 성인인증 아닌가」 → 테마 코드를 `도구 → 코드 찾기`
(`미성년` · `wd_is_adult`)로 확인했다. **PASS 본인확인은 「누구인가」이고 성인인증은 「19세 이상인가」다** —
미성년자도 자기 명의 폰이면 PASS 는 통과한다. 판정은 받는 쪽이 생년월일로 해야 하는데, 테마가 하고 있다.

| 자리 | 코드 |
|---|---|
| 본인인증 결과 확인 AJAX | `functions.php:9923` — 생년월일 없으면 차단 · `wd_is_adult( $birth, 19 )` 아니면 「만 19세 미만은 가입할 수 없습니다」 |
| 가입 폼 제출 | `page-join-form.php:116` — CI · 이름 · 휴대폰 · 생년월일 없으면 차단 · 같은 판정 |
| 정보 수정 재인증 | `functions.php:10001` — 미성년자면 즉시 `wp_delete_user` + 로그아웃 |

- `wd_is_adult()` 는 **만 나이**(생일 기준). 청소년보호법의 「그 해 1월 1일」보다 엄격한 쪽 — 구멍이 아니라 그대로 둔다
- 인증은 드림시큐리티 MobileOK **운영 서버**(`cert.mobile-ok.com`)로 PASS 앱 · usage `01005`. 이름 · 연락처 칸은 readonly, 서버가 채운다
- 오늘 프로덕션 재검: 비로그인 `/checkout/` 302 · 토큰 없이/위조 토큰으로 가입 제출 → 거부 · 로그인 쿠키 0.
  **우리 `Signup\guard()` 가 먼저 잡을 수 있다** — 전화번호가 이미 쓰이는 번호면 `?dhr_dup=1` 로 튕긴다. 시험할 때는 무작위 번호로
- **남은 구멍은 옛 회원뿐** — 이 판정이 걸리기 전에 가입한 회원은 로그인만 되면 결제까지 간다. 인증 결과(CI)의 저장 키를
  알면 「도구 → 성인인증 점검」(인증 기록 있음/없음 수 · 목록)을 만들 수 있다. 다음 주문 때 재인증시킬지는 사장님 판단
- **새 성인인증 시스템은 필요 없다.** 어느 기관을 붙여도 생년월일을 받아 사이트가 판정하는 구조는 같다

## 입금 자동확인이 놓치는 이름 — 「금고홍길동」 (2026-09-24, 코드 찾기로 확인)

사장님: 안내를 아무리 붙여도 입금자명 불일치가 난다. 원인은 손님이 아니라 **`keyple-bank-auto-confirm` 의 비교 방식**이다.

- `class-order-matcher.php` `name_equals()` — 공백 · 특수문자 제거 후 **완전 일치**. `strip_bank_prefix()` 가 은행 접두어를
  떼는데 목록에 **「금고」가 없다** (「새마을금고」 「새마을」은 있다). 새마을금고에서 보내면 통장에 「금고홍길동」으로 찍히므로
  영원히 안 맞는다. 「국민」 「신협」 「농협」 「카뱅」은 있어서 그쪽 손님만 잘 잡혔다
- 두 글자로 가입한 옛 회원(「길동」)이 「홍길동」으로 보내는 것도 같은 자리에서 떨어진다. **새 가입은 이름이 readonly 라
  두 글자가 못 들어온다** — 옛 회원 데이터 문제다
- 매칭 플러그인이 **둘**이다: `keyple-bank-auto-confirm`(문자 → `Keyple_Bank_Order_Matcher`, 주문 메모 「입금 문자 자동매칭 성공: …」)과
  `keyple-order-status`(`Keyple_Payment_Matcher::process_deposit`, 메모 「[keyple] 자동 매칭 성공 — …」, 필터 `keyple_os_validate_match` 있음).
  실제로 도는 쪽은 자동확인된 주문의 메모로 가른다
- 고치는 길: ①키플 파일 302행 목록에 `'금고',` 한 낱말 (사장님 몫 — 우리는 키플 파일을 안 건드린다) ②우리 플러그인의 2차 매칭 —
  「입금자명 불일치」로 떨어진 문자만, 접두어를 더 넓게 떼고 **주문자명이 들어 있고** 금액이 같고 입금전 후보가 **하나**일 때만
  입금확인 + 주문 메모. 표시만 하는 모드로 먼저. 만들려면 `apply_filters` · `candidates` · `update_sms` 검색 결과가 필요하다
- 결제 화면 `form-checkout.php:267` 에도 「회원님의 성함과 입금자명이 동일해야 합니다」가 있다 — 안내는 이미 열 곳이 넘는다.
  **안내를 더 붙이는 것은 답이 아니다**

### 도구 → 성인인증 점검 (2026-09-24, `includes/verify-admin.php`)

테마가 가입 때 판정을 마친 회원에게 **`wd_phone_verified = 1`** 을 적는다 (`functions.php:10252` 「게이트 판정 기준」,
함께 `wd_verified_name/phone/birth/ci/di`). 그 표시가 **없는 회원** = 판정이 걸리기 전 가입 · 옛 사이트에서 넘어온 사람.
**읽기 전용** — 회원 전체(직원 역할 제외) · 인증 있음/없음 · 없음 중 주문 이력 · 없음 중 최근 90일 주문 ·
이름 두 글자(성 없음) 수를 카드로, 없는 회원 목록을 주문 많은 순으로. 회원 한 번 · 주문 한 번, 질의 두 개
(HPOS 면 `wc_orders`, 아니면 posts + `_customer_user`). 필터 `duckhoo_verified_meta_key` · `duckhoo_verify_staff_roles` ·
`duckhoo_verify_dead_statuses` · `duckhoo_verify_list_max`(400).
테마는 로그인 직후 「cutoff 이전 가입 + 미인증」에게 안내 팝업만 띄운다 (10266행 주석) — 결제까지 막는지는 `wd_phone_verified` 검색으로 본다.
검증: `php design/php-tests/run.php` (성인인증 점검 6개).

### 옛 회원은 72명 → 6명 — 아임웹 명단 대조 · 재인증 문 (2026-09-24)

사장님: 인증 기록 없는 72명은 **사이트 이전 때 만들어진 계정**(아임웹 → 워드프레스). 「이메일은 달라도 전화번호는
같으니 대조해서 다른 사람만 재인증」. 성인인증 점검 화면에 **아임웹 명단 대조** 칸을 붙였다 — 엑셀 전체 복사 → 붙여
넣기 → 줄마다 휴대폰(하이픈 · `+82` 정규화) · 이메일만 뽑아 인증 없는 회원과 견준다 (`parse_roster()` · `cross()`,
**번호 먼저 · 없으면 이메일**). 붙여 넣은 글은 저장하지 않는다. 결과: 4,010명 명단 · **66명 걸림 · 6명 남음**
(전부 5월 11~14일 이전 기간 가입 · 지금 사는 사람 없음).

- **「대조하고 표시 남기기」** — 걸린 회원에게 `_dhr_legacy_verified = imweb` · `_at` 을 적는다 (`Verify\mark_legacy()`,
  두 번 안 적음). 회원 정보에 쓰는 유일한 자리 — 사장님 「딱 저 6명만 인증하면 될듯」으로 허락
- **재인증 문** `includes/verify-gate.php` — `wd_phone_verified` 도 `_dhr_legacy_verified` 도 없는 회원만
  결제 화면(`is_checkout()`, order-received · order-pay 제외)에서 테마의 재인증 화면 `/profile-edit/?dhr_reverify=1` 로
  보낸다 (`template_redirect` 5). 그 화면 위에 「주문 전에 본인확인을 한 번만」 안내 · 끝나면 결제로 돌아가는 링크 (「옛 사이트」라는 말은 손님에게 안 보이게 뺌 — 사장님 2026-09-24)(`.dhr-gate`, `wp_body_open` 7).
  재인증이 끝나면 테마가 `wd_phone_verified` 를 적으므로 다음부터 안 묻고, 미성년자면 테마가 그 자리에서 탈퇴시킨다.
  **다른 3,355명에게는 아무 일도 없다.** 끄기: `duckhoo_reverify_gate` → false · 주소: `duckhoo_reverify_url`
- 점검 화면 카드에 「결제 전 재인증 대상」(둘 다 없는 수)을 더했다. 목록의 옛 사이트 확인 회원은 초록 표시

검증: `php design/php-tests/run.php` (명단 읽기 · 대조 · 표시 · 문 여닫기 9개).

### 도구 → 입금 2차 판정 (2026-09-24, `includes/bank-second.php`, 읽기 전용)

사장님이 `class-order-matcher.php` 전체를 보내 줬다 (코드 찾기 「파일 통째로 보기」로 — 낱말 넷을 따로 찾게 하는
대신 붙인 칸). 확정된 것: 필터는 `keyple_bank_awaiting_statuses` 하나뿐, 이름 비교에 끼어들 자리 없음 ·
주문 쪽 이름 후보는 청구/배송 성+이름 · 이름+성 · formatted · `_deposit_payer_name` 메타 · 후보 주문은 금액 완전
일치 + 입금전/확인필요/on-hold/pending + `keyple_bank_match_window`(14일) · `update_sms()` 는 protected,
**`manual_link( $sms_id, $order_id, 'confirm' )` 이 public** (관리자 수동 입금확인과 같은 길) · 문자 표는
`Keyple_Bank_Installer::table( TABLE_SMS )`, 칸 `depositor_name · amount · match_status · match_reason · order_id`.
헬퍼 앱 화면의 「자동매칭 성공 · 이름+금액 일치」 「확인필요: 입금자명 불일치」가 이 플러그인 문구라 도는 쪽도 확정.

- **판정 규칙은 끝 일치** — 주문자명(정규화)이 입금자명(정규화 · 접두어 뗀 것)의 **끝**에 붙어야 한다.
  「금고홍길동」 ← 「홍길동」 · 「길동」 둘 다 잡고, 「농협이상섭」 ← 「이상」은 안 잡는다 (들어 있는가가 아니라 끝인가).
  금액 같은 후보 중 **딱 하나**일 때만 「이 주문」, 둘 이상이면 「후보 여럿」. 접두어는 키플 목록 + 금고 · 저축 · 축협 · 씨티 …
  (`duckhoo_bank_prefixes`). 문자 표의 시각 · 원문 칸 이름은 몰라서 `SHOW COLUMNS` 로 있는 것을 고른다 (1시간 캐시)
- **자동으로 입금확인까지 하는 판은 이 세션 권한에서 막혔다** (돈이 걸린 주문 상태를 자동으로 바꾸는 코드 — 「Modify
  Shared Resources」). 그래서 **읽기 전용**: 최근 14일 「주문 못 붙인 확인필요 문자」를 카드(이 주문 · 후보 여럿 · 못 찾음 ·
  해석 실패)와 표로 보여 주고, 맞다고 보이면 사장님이 키플 문자 목록에서 연결 · 입금확인을 누른다. 자동화하려면
  `manual_link(…,'confirm')` 을 부르는 5분 크론 한 겹이면 되지만 **사장님 결정 + 권한**이 필요하다
- 「해석 실패」(금액 0)는 이름 비교가 아니라 `class-sms-parser.php` 문제 — 원문 칸을 표에 같이 찍는다

검증: `php design/php-tests/run.php` (2차 판정 9개 — 접두어 · 끝 일치 · 후보 하나/여럿/없음 · 이름 후보 · 해석 실패).

### 입금자명 건은 여기서 멈춘다 (사장님 2026-09-24 「수동으로 확인하는 게 더 편할 수도」 · 「성인인증만 하면 됐다」)

- 14일에 24건(하루 두 건 미만)이라 **손으로 처리하는 쪽**으로 정했다. 자동 입금확인은 만들지 않는다
- `도구 → 입금 2차 판정` 은 읽기 전용 참고 화면으로 그대로 둔다 (안 봐도 된다). 실측: 24건 = 이름 불일치 9 · 해석 실패 10 · 다른 사유 5,
  「이 주문」 0 — 이미 손으로 처리된 주문이라 입금전 후보가 없었던 것. 「이미 처리된 주문과 맞음」 카드로 규칙 검증만 가능
- 해석 실패가 이름 불일치보다 많다 (10/24). 키플 파서 `extract_amount()` 가 「입금 N」 · 「N원」 두 꼴만 읽어서다 —
  키플 파일이라 손대지 않는다. 화면이 원문 · 금액 후보를 같이 찍는다
- **회원 이름 ≠ 인증기관 이름 30명의 주문자명 맞추기는 안 한다** (사장님 결정). 필요해지면 성인인증 점검 화면에 버튼 하나면 된다
- **성인인증은 끝** — 가입 두 겹 판정 · 옛 회원 66명 아임웹 확인 표시 · 남은 6명 결제 전 재인증 문

## 오늘 할 일 — 사장님의 아침 한 장 (2026-09-24)

사장님: 「매일매일 할 일을 네가 나에게 주면 그에 대한 업무를 보는 게 좋을 것 같다」. 사람이 매일 적어 주는
목록은 빠지는 날이 생긴다 → **사이트가 그날 실제 숫자로 목록을 만든다.** `includes/today.php`,
관리자 메뉴 **오늘 할 일**(대시보드 바로 아래, 남은 건수 배지) + 워드프레스 대시보드 위젯 + **매일 10시 30분 메일**(`MAIL_AT`, 사장님 11시 출근 — 시각을 바꾸면 `schedule()` 이 잡힌 크론을 풀고 다시 잡는다)
(관리자 메일 · 열린 항목이 하나라도 있을 때만 · 화면의 체크박스로 끔 · `duckhoo_today_mail` · 받는 곳 `duckhoo_today_mail_to`).

- **읽기만 한다.** 주문 · 회원에 아무것도 쓰지 않는다. 쓰는 것은 「했음」 체크(`duckhoo_today_done`, 그날치만) 와
  메일 켬/끔 옵션 둘뿐. 숫자는 5분 transient
- **순서 = 급한 순서**: 입금(입금전 5일 넘음 · 확인필요 · 주문 못 붙인 문자 14일) → 출고(입금확인 = 오늘 보낼 것 ·
  배송준비중인데 송장 없음 · 배송준비중 2주 넘음) → 손님(답 없는 1:1 문의 30일 · 승인 대기 후기) → 가게(품절 표시 ·
  재고 5개 이하 · 사흘 안 만료 쿠폰 · 월요일엔 매출·깔때기 보기) → 어제 숫자(주문 · 확정 매출 · 새 회원).
  0건은 「없음」으로 접고, 「입금 기다리는 주문」은 숫자만(할 일이 아니다 — `info`)
- **모르면 안 그린다.** kboard 표(`kboard_board_content` · `kboard_comments`)는 이 저장소에 없어 `SHOW COLUMNS` 로
  칸을 확인하고, 없으면 문의 항목 자체를 뺀다(`n = -1`). 답 = administrator · shop_manager 가 단 댓글.
  게시판 번호는 `/inquiries/` 본문의 `[kboard id=N]` 에서
- 출고 줄의 안내는 시각 · 요일 · 연휴(`Front\holiday_notice()`)를 본다 — 16시 뒤엔 「내일 출고분」, 주말엔 「월요일 오후 4시」
- `build( $facts, $ctx )` · `mail_text()` 는 순수 함수 — 테스트가 이것을 본다. 숫자 모으기는 `facts()` 한 곳.
  배지 · 위젯 · 화면 모두 `Throwable` 을 잡는다 — 배지 하나 때문에 관리자 전체가 죽으면 안 된다
- 크론(`duckhoo_today_mail`)이 프론트 요청에서도 돌아야 해서 `require` 는 admin 블록 **밖**이다

**테스트 러너가 반쯤 꺼져 있었다.** `run.php` 의 `echo … exit` 가 1843행에 있어 그 뒤에 붙인 성인인증 점검 ·
재인증 문 · 입금 2차 판정 46개는 **한 번도 돈 적이 없었다** (「모두 통과」는 앞부분 이야기였다). 끝으로 옮기니
그것들도 다 통과했고 오늘 것 21개까지 1,039개. **새 절을 붙일 때는 `grep -c "^· "` 로 건수가 늘었는지 본다.**

검증: `php design/php-tests/run.php` (오늘 할 일 21개) · `scratchpad/today-render.php` + `shot-today.mjs`
(가짜 주문으로 화면 · 위젯 · 메일을 통째로 그려 390/1280 스크린샷, 넘침 0).

## 클로드 아침 브리핑 (2026-09-24)

사장님: 「클로드가 나에게 알림 메시지나 정리한 걸 보내줬으면 해 — 어떤 작업이 매출에 도움이 되는지, 어떤 작업을
해야 더 편하게 일하는지, 손님이 더 편하게 주문 · 탐방하는지」. 오늘 할 일 화면은 **숫자**를 주고, 이것은 **판단**을 준다.

- **숫자는 플러그인이 낸다**: `includes/brief.php` → `GET /wp-json/duckhoo/v1/brief`, 헤더 `X-DHR-Key`(또는 Bearer).
  키는 옵션 `duckhoo_brief_key` — 오늘 할 일 화면 맨 아래 상자에서 보고 · 새로 만든다. `hash_equals`, 키가 없으면 403.
  **집계만** 나간다(오늘 할 일의 건수 · 열린 항목 · 어제 · 7일/전 7일/이달의 주문 · 확정 매출 · 취소 · 가입 · 객단가 ·
  깔때기 7/30일 · 상품 수 · 후기 수 · 홈 띠 · 노보 현재가 · 안내 띠). 이름 · 연락처 · 주문 번호는 없다. 10분 캐시
- **판단은 루틴이 한다**: 클로드 루틴 `액상덕후 아침 브리핑` (`trig_01WVq3kGkHnwQ5A8DNCUdXvG`, 매일 02:00 UTC =
  **11:00 KST** — 사장님 출근 시각, 매번 새 세션, 폰 푸시 + 메일). 프롬프트가 하는 일: CLAUDE.md 와 `design/briefs/` 최근 5개를 읽고 →
  위 주소를 `$DUCKHOO_BRIEF_KEY` 로 읽고 → 공개 화면(홈 · shop · 노보 분류 · 브랜드) 200 · 끝난 행사 문구 점검 →
  「오늘 숫자 · 지금 막힌 것 · 오늘 추천 작업 하나(사장님 할 일 / 클로드 할 일)」 25줄, 월요일엔 깔때기 두 길 + 이번 주
  프로젝트 하나 → `design/briefs/YYYY-MM-DD.md` 에 남기고 push(배포 안 됨). 읽기 전용 · 코드 안 고침 · 손님 연락 없음
- **사장님이 한 번 할 것**: 관리자 오늘 할 일 화면의 키를 클로드 환경 설정(세션 제목 줄의 환경 메뉴 → Edit)의 환경 변수
  **`DUCKHOO_BRIEF_KEY`** 에 넣는다. 그 전까지 브리핑은 첫 줄에 「열쇠가 아직 안 들어왔다」고 적고 공개 화면만 본다.
  **키를 대화창에 붙이지 않는다** — 저장소 · 대화 어디에도 키를 적지 않는다
- 루틴은 MCP 커넥터 없이 돈다 (curl · git 만). 네이버 순위 같은 것을 넣으려면 루틴 UI 에서 커넥터를 붙여야 한다
- 고치기: 루틴 프롬프트는 `update_trigger` 로, 시각은 cron 으로. 끄기: 루틴 비활성화 (플러그인 쪽은 그대로 둬도 된다)
- 「사장님이 안 한다고 한 것」(하루 한도 · 입금 자동확인 · 이름 동기화)은 루틴 프롬프트에 「다시 제안하지 말 것」으로 적어 뒀다.
  새로 못 박힌 결정이 생기면 **CLAUDE.md 에 적는 것으로 충분하다** — 루틴이 마지막 500줄을 읽는다

검증: `php design/php-tests/run.php` (브리핑 6개) · 프로덕션 `curl -I …/wp-json/duckhoo/v1/brief` 가 키 없이 401/403.

### 브리핑은 디스코드로 (2026-09-24, 사장님 「폰 알림은 작업자 폰으로 안 온다」)

루틴의 푸시 · 메일은 클로드 계정 주인에게만 간다. 사장님이 고른 것은 **디스코드** (카카오는 비즈니스 채널 · 대행사가
필요해 뺐고, 텔레그램은 새 앱이라 뺐다).

- **웹훅 주소는 사이트가 쥔다** — 오늘 할 일 화면 「디스코드로 받기」 상자(옵션 `duckhoo_discord_webhook`, 필터
  `duckhoo_discord_webhook`). `discord_ok()` 가 `https://discord(app).com/api/webhooks/…` 꼴만 받는다 — 글이 다른 곳으로
  새지 않게. 「시험 메시지 보내기」 버튼
- **브리핑 세션은 웹훅을 모른다.** 다 쓴 글을 `POST /wp-json/duckhoo/v1/brief` (같은 키, `text`)로 보내면 사이트가
  `Today\discord_send()` 로 옮긴다. 받는 곳을 나중에 바꿔도 루틴 프롬프트는 그대로다. 응답 `{ok, chunks, discord}` —
  `discord:false` 면 웹훅이 아직 없는 것
- 10시 30분 오늘 할 일도 같은 웹훅으로 간다 (`send_mail()` 이 메일과 디스코드 둘 다). 디스코드 한 메시지 2,000자 제한 →
  `discord_chunks()` 가 줄 단위로 1,900자씩 나눈다. `allowed_mentions` 를 비워 @everyone 이 안 울리게
- 루틴 프롬프트: 브리핑을 쓴 뒤 그 전문을 POST 한다. `ok:false` 면 마지막 메시지에 한 줄 적는다 (푸시 · 메일은 그대로 나간다)

검증: `php design/php-tests/run.php` (디스코드 6개 — 주소 꼴 · 조각 나누기 · 웹훅 없으면 0) · `scratchpad/today-render.php`.
